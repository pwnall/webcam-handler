[package]
name = "webcam-handler-fake"
description = "The webcam-handler fake camera backend: replays captured device profiles, scripts faults, and synthesizes frames that respond to control values."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[lib]
name = "fake"

[lints]
workspace = true

[dependencies]
# Device-node paths in the schema's error and node types are `camino::Utf8PathBuf`.
camino.workspace = true
# `image::GrayImage` is what the imaging crate's fixture generators produce and consume.
image.workspace = true
webcam-handler-imaging.workspace = true
webcam-handler-schema.workspace = true

[dev-dependencies]
webcam-handler-testkit.workspace = true
