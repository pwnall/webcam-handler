{
  "_": [
    "The claims this rung makes, and how many assertions each one is.",
    "",
    "Two readers, and they are the reason this file exists rather than a number in a sentence.",
    "When the rung RUNS, crates/daemon/tests/web_browser.rs compares this list against",
    "Playwright's own JSON report — every title, in order, and every title's count of",
    "`expect` steps — so a claim added to a spec and not to this file is red, and so is a",
    "claim that quietly stopped asserting half of what it used to. When the rung SKIPS, this",
    "is where the skip line gets the number it reports: `N claims, M assertions, not run`.",
    "A skip that cannot say what it did not do is the skip that reads as a pass.",
    "",
    "The counts are measured, never estimated: they come out of a green run's report, and",
    "the harness names this file in its failure message when they drift."
  ],
  "claims": [
    {
      "title": "a sparse menu becomes a select carrying the device's own indices",
      "assertions": 5,
      "what": "PF:2 — the device's own menu indices reach a <select>, holes and all"
    },
    {
      "title": "an INACTIVE control is still usable and names what owns it",
      "assertions": 8,
      "what": "PF:3 — an INACTIVE control renders enabled, badged and named, beside a READ_ONLY one that does not [PF:12]"
    },
    {
      "title": "a current outside its declared range renders with no min and no max",
      "assertions": 6,
      "what": "PF:4 — an out-of-range current is shown, and no attribute bounds it"
    },
    {
      "title": "a control type this build cannot name shows its raw discriminant",
      "assertions": 5,
      "what": "PF:1 — an unnameable control type renders its discriminant and payload"
    },
    {
      "title": "a clamp snaps the slider back and says both numbers",
      "assertions": 6,
      "what": "PF:6/D3 — a clamped write moves the slider back and shows both numbers"
    },
    {
      "title": "a menu whose value the device did not report selects nothing",
      "assertions": 8,
      "what": "a control with no current renders a selected, disabled placeholder rather than the browser's first option"
    },
    {
      "title": "the bitmask field refuses what it cannot write, and says when a write is in flight",
      "assertions": 10,
      "what": "an emptied or unparsable bitmask writes nothing and says so, and an in-flight write is painted rather than only recorded"
    },
    {
      "title": "the preview paints successive frames and keeps painting across a photo",
      "assertions": 19,
      "what": "the MJPEG <img> paints successive parts, keeps painting across a photo (N83), and the photo's report names its formats as MJPG-style fourccs rather than as four numbers (N109)"
    },
    {
      "title": "the preview paints a recording's own frames and the page says who owns the camera",
      "assertions": 18,
      "what": "N117 — the ruling from a viewer's side: the <img> keeps painting a recording's own frames on the request it already had, and #recording-status says who owns the camera and for how long"
    },
    {
      "title": "the calibration view tracks a sweep it did not start",
      "assertions": 9,
      "what": "the calibration view paints a sweep started on another socket"
    },
    {
      "title": "the client loads with no credential and the camera is still refused",
      "assertions": 8,
      "what": "N82 — the module graph loads and runs anonymously, the two camera routes do not, and the page names the two candidates a page with no token actually has"
    },
    {
      "title": "a page in another origin cannot reach the camera, token and all",
      "assertions": 5,
      "what": "N93 — a sandboxed iframe's <img> and WebSocket are refused 403 by the daemon, with this run's token in both URLs"
    },
    {
      "title": "the page reports a lost socket and works again on the next one",
      "assertions": 10,
      "what": "the socket's loss is reported, the preview is ended, and the next socket works"
    },
    {
      "title": "a socket that closed refuses the next call instead of parking it",
      "assertions": 10,
      "what": "a call made after the close is refused at once — WHATWG's silent `send()` on a CLOSED socket would otherwise park it for ever — and the click that used to undo the close is answered"
    },
    {
      "title": "a subscription that died with the socket stops saying it is watching",
      "assertions": 5,
      "what": "E16 §1's class again: every stream is told the socket ended, and no writer of #connection contradicts the one sentence that covers it"
    },
    {
      "title": "a preview the page walked away from is a preview the daemon stopped",
      "assertions": 7,
      "what": "docs/11 H4 — a camera the page stops previewing is a camera the daemon stops streaming, read through the viewer cap that is the only count a browser can see"
    },
    {
      "title": "the control panel on screen belongs to the camera on screen",
      "assertions": 14,
      "what": "docs/11 M32 — the panel is emptied in the same task as the click, and an answer about a camera the page has walked away from is dropped rather than painted"
    },
    {
      "title": "a refused camera list at startup is a sentence rather than a silence",
      "assertions": 6,
      "what": "docs/11 M33 — a refused wch_list at startup reaches the banner instead of becoming an unhandled rejection under the word `connected`"
    },
    {
      "title": "a recording answer in flight is not written under the next camera's picture",
      "assertions": 5,
      "what": "docs/11 L36 — an answer that arrives after the handle was retired writes nothing into #recording-status"
    },
    {
      "title": "a socket severed without a FIN stops reading as connected",
      "assertions": 7,
      "what": "docs/11 L38 — the idle heartbeat ends a connection that stopped carrying anything without ever closing, on a clock the test owns"
    },
    {
      "title": "an idle socket is kept by an answer, and a slow call is not a dead socket",
      "assertions": 13,
      "what": "L38's healthy path: the heartbeat is answered over the route the page opens (A9 — the dependency claim the rung had never run), a call that merely timed out is refused as a timeout and leaves the connection alone, and silence still ends it"
    },
    {
      "title": "a stale session list is not painted under the camera on screen",
      "assertions": 8,
      "what": "docs/11 M32's third element — a wch_calibrate_list answer about a camera the page has walked away from paints nothing, in the success arm and the refusal arm"
    },
    {
      "title": "the browser this rung drove is the pinned one",
      "assertions": 6,
      "what": "the Playwright package and the Chromium build that ran are the pinned ones"
    }
  ]
}
