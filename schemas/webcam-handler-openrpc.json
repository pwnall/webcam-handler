{
  "components": {
    "errors": {
      "busy": {
        "code": -32013,
        "data": {
          "holders": [
            {
              "comm": "cheese",
              "pid": 4242
            }
          ],
          "kind": "busy",
          "path": "/dev/video0"
        },
        "message": "/dev/video0 is busy: held by cheese (pid 4242)"
      },
      "camera_ambiguous": {
        "code": -32016,
        "data": {
          "candidates": [
            "cam:webcam",
            "cam:webcam-2"
          ],
          "kind": "camera_ambiguous",
          "requested": "cam:web"
        },
        "message": "\"cam:web\" matches 2 cameras: cam:webcam, cam:webcam-2"
      },
      "camera_unknown": {
        "code": -32015,
        "data": {
          "kind": "camera_unknown",
          "requested": "cam:nope"
        },
        "message": "no camera matches \"cam:nope\""
      },
      "control_inactive": {
        "code": -32019,
        "data": {
          "automation": "white_balance_automatic",
          "control": "white_balance_temperature",
          "kind": "control_inactive"
        },
        "message": "control white_balance_temperature is inactive: white_balance_automatic owns it and this build could not switch it off — set white_balance_automatic to manual and write again"
      },
      "control_read_only": {
        "code": -32018,
        "data": {
          "control": "privacy",
          "kind": "control_read_only"
        },
        "message": "control privacy is read-only on this device"
      },
      "control_unknown": {
        "code": -32017,
        "data": {
          "did_you_mean": [
            "brightness"
          ],
          "kind": "control_unknown",
          "requested": "brightnes"
        },
        "message": "no control named \"brightnes\"; did you mean brightness?"
      },
      "device_gone": {
        "code": -32012,
        "data": {
          "kind": "device_gone",
          "path": "/dev/video0"
        },
        "message": "camera /dev/video0 is gone (unplugged, or its driver unbound)"
      },
      "device_io": {
        "code": -32028,
        "data": {
          "errno": 22,
          "kind": "device_io",
          "message": "Invalid argument",
          "operation": "VIDIOC_S_EXT_CTRLS"
        },
        "message": "VIDIOC_S_EXT_CTRLS failed (errno 22): Invalid argument"
      },
      "fingerprint_mismatch": {
        "code": -32022,
        "data": {
          "fields": [
            "card",
            "usb_id"
          ],
          "kind": "fingerprint_mismatch"
        },
        "message": "camera fingerprint differs from the session's in: card, usb_id"
      },
      "format_unsupported": {
        "code": -32020,
        "data": {
          "available": [
            "MJPG",
            "YUYV"
          ],
          "kind": "format_unsupported",
          "requested": "NV12"
        },
        "message": "format NV12 is unavailable; MJPG, YUYV would be accepted"
      },
      "holder_gone": {
        "code": -32027,
        "data": {
          "kind": "holder_gone",
          "pid": 4242
        },
        "message": "pid 4242 no longer holds this device; refusing to signal it"
      },
      "illegal_transition": {
        "code": -32024,
        "data": {
          "from": "untouched",
          "kind": "illegal_transition",
          "op": "select"
        },
        "message": "untouched: cannot select"
      },
      "permission_denied": {
        "code": -32014,
        "data": {
          "hint": "add yourself to the `video` group, then log out and back in",
          "kind": "permission_denied",
          "path": "/dev/video0"
        },
        "message": "cannot open /dev/video0: add yourself to the `video` group, then log out and back in"
      },
      "schema_version_foreign": {
        "code": -32025,
        "data": {
          "found": 99,
          "kind": "schema_version_foreign",
          "supported": 1
        },
        "message": "document schema version 99 is not supported (this build reads 1)"
      },
      "session_conflict": {
        "code": -32023,
        "data": {
          "detail": "another session for this camera and task is already sweeping",
          "kind": "session_conflict"
        },
        "message": "session conflict: another session for this camera and task is already sweeping"
      },
      "settle_timeout": {
        "code": -32021,
        "data": {
          "frames_seen": 3,
          "kind": "settle_timeout",
          "waited_ms": 5000
        },
        "message": "frames did not settle within 5000 ms (3 frames seen)"
      },
      "storage_io": {
        "code": -32029,
        "data": {
          "errno": 28,
          "kind": "storage_io",
          "message": "No space left on device",
          "path": "/home/you/.local/state/webcam-handler"
        },
        "message": "/home/you/.local/state/webcam-handler: No space left on device"
      },
      "store_locked": {
        "code": -32026,
        "data": {
          "holder": {
            "comm": "webcam-handler-",
            "pid": 909
          },
          "kind": "store_locked",
          "protocol": "held_for_lifetime"
        },
        "message": "the state directory is locked by webcam-handler- (pid 909); daemon owns the state (and likely the camera) — use webcam-handler-client"
      }
    },
    "schemas": {
      "Adjustment": {
        "description": "One way the device's answer differs from the request.",
        "oneOf": [
          {
            "description": "The driver chose a different pixel format.",
            "properties": {
              "field": {
                "const": "pixel_format",
                "type": "string"
              },
              "negotiated": {
                "$ref": "#/components/schemas/PixelFormat",
                "description": "What the driver set."
              },
              "requested": {
                "$ref": "#/components/schemas/PixelFormat",
                "description": "What was asked for."
              }
            },
            "required": [
              "field",
              "requested",
              "negotiated"
            ],
            "type": "object"
          },
          {
            "description": "The driver chose a different frame size.",
            "properties": {
              "field": {
                "const": "size",
                "type": "string"
              },
              "negotiated_height": {
                "description": "Negotiated height.",
                "format": "uint32",
                "minimum": 0,
                "type": "integer"
              },
              "negotiated_width": {
                "description": "Negotiated width.",
                "format": "uint32",
                "minimum": 0,
                "type": "integer"
              },
              "requested_height": {
                "description": "Requested height.",
                "format": "uint32",
                "minimum": 0,
                "type": "integer"
              },
              "requested_width": {
                "description": "Requested width.",
                "format": "uint32",
                "minimum": 0,
                "type": "integer"
              }
            },
            "required": [
              "field",
              "requested_width",
              "requested_height",
              "negotiated_width",
              "negotiated_height"
            ],
            "type": "object"
          },
          {
            "description": "The driver chose a different frame interval.",
            "properties": {
              "field": {
                "const": "interval",
                "type": "string"
              },
              "negotiated": {
                "$ref": "#/components/schemas/FrameInterval",
                "description": "What the driver set."
              },
              "requested": {
                "$ref": "#/components/schemas/FrameInterval",
                "description": "What was asked for."
              }
            },
            "required": [
              "field",
              "requested",
              "negotiated"
            ],
            "type": "object"
          }
        ]
      },
      "Applied": {
        "description": "The result of a write: what was asked, what the device actually holds, and why they\ndiffer if they do (design D3/E4).\n\nEvery layer above preserves both fields. A layer that collapses them to one value is\ndropping the fact the whole doctrine exists to keep (rubric A10).",
        "properties": {
          "applied": {
            "$ref": "#/components/schemas/ControlValue",
            "description": "The value read back afterwards."
          },
          "control": {
            "$ref": "#/components/schemas/ControlId",
            "description": "Which control."
          },
          "requested": {
            "$ref": "#/components/schemas/ControlValue",
            "description": "The value we asked for."
          },
          "slug": {
            "$ref": "#/components/schemas/ControlSlug",
            "description": "Its slug, so output is readable without a second lookup."
          },
          "warnings": {
            "description": "Why they differ, when they do.",
            "items": {
              "$ref": "#/components/schemas/WriteWarning"
            },
            "type": "array"
          }
        },
        "required": [
          "control",
          "slug",
          "requested",
          "applied"
        ],
        "type": "object"
      },
      "AutomationOff": {
        "description": "How to switch an automation control off.",
        "oneOf": [
          {
            "description": "Write this exact value (booleans: `0`).",
            "properties": {
              "kind": {
                "const": "value",
                "type": "string"
              },
              "value": {
                "description": "The value that means \"off\".",
                "format": "int64",
                "type": "integer"
              }
            },
            "required": [
              "kind",
              "value"
            ],
            "type": "object"
          },
          {
            "description": "Select the menu item whose *name* matches — never the index.\n\n`Manual Mode` happens to be index 1 on both seed cameras, and that is a\ncoincidence rather than a contract: menus are sparse and per-device [PF:2].",
            "properties": {
              "kind": {
                "const": "menu_item_named",
                "type": "string"
              },
              "patterns": {
                "description": "Lowercase substrings, tried in order; the first match wins.",
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            },
            "required": [
              "kind",
              "patterns"
            ],
            "type": "object"
          }
        ]
      },
      "AutomationPair": {
        "description": "One manual control and the automation that overrides it.",
        "properties": {
          "automation": {
            "$ref": "#/components/schemas/ControlSlug",
            "description": "The control that must be switched off first."
          },
          "manual": {
            "$ref": "#/components/schemas/ControlSlug",
            "description": "The control a caller wants to drive by hand."
          },
          "off": {
            "$ref": "#/components/schemas/AutomationOff",
            "description": "How to switch it off."
          },
          "provenance": {
            "$ref": "#/components/schemas/Provenance",
            "description": "Whether we read this in a spec or saw it on this device."
          }
        },
        "required": [
          "manual",
          "automation",
          "off",
          "provenance"
        ],
        "type": "object"
      },
      "BackendKind": {
        "description": "Which backend a camera came from.\n\nClosed on purpose: each composition root holds one exhaustive `match` over this\nvocabulary constructing the backend it links, so adding a variant stops both\nbuilds until the new backend is wired (design §2.11 step 3).",
        "oneOf": [
          {
            "const": "v4l2",
            "description": "Real hardware through V4L2.",
            "type": "string"
          },
          {
            "const": "fake",
            "description": "Captured device profiles, replayed. A test instrument, not a product backend.",
            "type": "string"
          }
        ]
      },
      "Base64Bytes": {
        "description": "Bytes on a JSON wire: base64, the standard alphabet, padded (design D10).\n\nA newtype rather than a bare `String` field, for two reasons, and the second one is a\nprivacy rule.\n\nFirst, the encoding is a *transport* decision, and this type is where it happens once.\n`webcam-handler-api` is the only crate in the workspace that declares the `base64`\ndependency at all, which is what keeps `webcam-handler-schema` free of it.\n\nSecond: **a frame may contain a person** (AGENTS.md; rubric A12). A derived `Debug` on\nanything holding photo bytes would print them into a log line or a panic message, and\nbase64 of a face is still a face — so `Debug` here prints the byte count and nothing\nelse, exactly as `schema::capture::Frame` does, and every document that embeds this\ninherits that property rather than having to remember it.\n\nThe alphabet is standard and padded rather than URL-safe: these bytes ride in a JSON\nstring, never in a URL, and padded standard base64 is what a JSON-RPC consumer's own\nlibrary decodes by default.",
        "type": "string"
      },
      "BlockedReason": {
        "description": "Why a control cannot be calibrated.",
        "oneOf": [
          {
            "description": "The device says READ_ONLY [PF:12].",
            "properties": {
              "kind": {
                "const": "read_only",
                "type": "string"
              }
            },
            "required": [
              "kind"
            ],
            "type": "object"
          },
          {
            "description": "The device says DISABLED.",
            "properties": {
              "kind": {
                "const": "disabled",
                "type": "string"
              }
            },
            "required": [
              "kind"
            ],
            "type": "object"
          },
          {
            "description": "The control is INACTIVE and no automation partner was discovered to release it.",
            "properties": {
              "kind": {
                "const": "inactive_without_partner",
                "type": "string"
              }
            },
            "required": [
              "kind"
            ],
            "type": "object"
          },
          {
            "description": "The control's type has no ordered range to sweep (compound, opaque, string).",
            "properties": {
              "control_type": {
                "description": "The type, as text, so the message is readable without a lookup.",
                "type": "string"
              },
              "kind": {
                "const": "not_sweepable",
                "type": "string"
              }
            },
            "required": [
              "kind",
              "control_type"
            ],
            "type": "object"
          },
          {
            "description": "Something else, described. Free-text is deliberate: the alternative is a closed\nvocabulary that turns an unanticipated reason into the wrong one.",
            "properties": {
              "detail": {
                "description": "What happened.",
                "type": "string"
              },
              "kind": {
                "const": "other",
                "type": "string"
              }
            },
            "required": [
              "kind",
              "detail"
            ],
            "type": "object"
          }
        ]
      },
      "CameraDetail": {
        "description": "What `info` answers: one camera and the formats its capture node offers.",
        "properties": {
          "formats": {
            "description": "Formats, with sizes and the intervals available at each size nested under them\n[PF:9]. Empty for a camera with no capture node, which is a real shape.",
            "items": {
              "$ref": "#/components/schemas/FormatInfo"
            },
            "type": "array"
          },
          "info": {
            "$ref": "#/components/schemas/CameraInfo",
            "description": "Identity, nodes, grouping."
          }
        },
        "required": [
          "info",
          "formats"
        ],
        "type": "object"
      },
      "CameraFingerprint": {
        "description": "Best-effort identity across replug and reboot.\n\nSerial numbers are in here but not trusted: the Chicony reports `\"0001\"` and the\nOBSBOT reports none at all [PF:8]. Matching is conservative — a mismatch on\n`calibrate apply` is a refusal naming the differing fields, not a warning.",
        "properties": {
          "bus_path": {
            "description": "The USB interface path (`3-1:1.0`) or, for non-USB devices, the driver's bus info.",
            "type": "string"
          },
          "card": {
            "description": "The `QUERYCAP` card name.",
            "type": "string"
          },
          "driver": {
            "description": "The driver name.",
            "type": "string"
          },
          "serial": {
            "description": "The serial, only when the device reports a distinguishing one [PF:8].",
            "type": [
              "string",
              "null"
            ]
          },
          "usb_id": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/UsbId"
              },
              {
                "type": "null"
              }
            ],
            "description": "`VID:PID` when the device is on USB."
          }
        },
        "required": [
          "bus_path",
          "card",
          "driver"
        ],
        "type": "object"
      },
      "CameraId": {
        "description": "The name RPC calls and CLI arguments use: `cam:<card-slug>[-<n>]`.\n\nReproducible across runs while the attached topology is unchanged, and stable for one\nengine's lifetime even across replug. Never persisted as identity — that is\n`CameraFingerprint`'s job.",
        "type": "string"
      },
      "CameraInfo": {
        "description": "A camera: a group of nodes, an identity, and the driver's own description of itself.",
        "properties": {
          "backend": {
            "$ref": "#/components/schemas/BackendKind",
            "description": "Which backend produced this. Recorded so `--json` output is self-describing and\na fake-backend run can never be mistaken for a hardware one."
          },
          "bus_info": {
            "description": "The driver's bus info string.",
            "type": "string"
          },
          "card": {
            "description": "The `QUERYCAP` card name, verbatim.",
            "type": "string"
          },
          "driver": {
            "description": "The driver name.",
            "type": "string"
          },
          "fingerprint": {
            "$ref": "#/components/schemas/CameraFingerprint",
            "description": "The identity that survives a replug."
          },
          "id": {
            "$ref": "#/components/schemas/CameraId",
            "description": "The id callers use."
          },
          "nodes": {
            "description": "Every node in the group, capture and metadata alike.",
            "items": {
              "$ref": "#/components/schemas/DeviceNode"
            },
            "type": "array"
          }
        },
        "required": [
          "id",
          "fingerprint",
          "card",
          "driver",
          "bus_info",
          "nodes",
          "backend"
        ],
        "type": "object"
      },
      "CameraList": {
        "description": "What `list` answers.",
        "properties": {
          "cameras": {
            "description": "The cameras, in enumeration order.",
            "items": {
              "$ref": "#/components/schemas/CameraInfo"
            },
            "type": "array"
          },
          "hints": {
            "description": "Anything worth saying about what is *not* in the list.",
            "items": {
              "$ref": "#/components/schemas/ListHint"
            },
            "type": "array"
          }
        },
        "required": [
          "cameras"
        ],
        "type": "object"
      },
      "CameraSelector": {
        "description": "Which camera, in any of the spellings webcam-handler understands: cam:<id>; /dev/videoN; bus:<interface-path>; usb:<vid>:<pid>; serial:<text>.",
        "type": "string"
      },
      "CapReached": {
        "description": "Which bound ended the recording.\n\n`ALL` is generated from this definition, and the tests walk it: a cap that no\nrecording can reach is a bound that is not a bound, and a hand-written list of the\nthree would let one quietly stop being enforced (rubric rule 6). Since P6b the walk\nis two-dimensional — `CapReached::ALL` × `VideoFormat::ALL` — because the two\ncontainers count their own bytes and a cap enforced in one of them is not a cap.",
        "oneOf": [
          {
            "const": "size",
            "description": "The byte cap: `RecordingCaps::max_bytes` in `webcam-handler-imaging`.",
            "type": "string"
          },
          {
            "const": "frames",
            "description": "The frame-count cap: `RecordingCaps::max_frames`.",
            "type": "string"
          },
          {
            "const": "span",
            "description": "The wall-of-the-driver's-clock cap: `RecordingCaps::max_span`.",
            "type": "string"
          }
        ]
      },
      "ChosenBy": {
        "description": "Who a caller may claim to be when recording a chosen value (design D8).\n\nTwo spellings, and the third selector D8 names — `metric:<name>` — is deliberately\nnot among them: a metric is not something a caller can *claim* to be, it is\nsomething the tool computed and scored. On a command line clap's group enforced\nthat (`--metric` conflicts with `--by`); on the wire nothing would, so this\nvocabulary makes the lie unrepresentable instead of merely refusable.\n\nGenerated, so `ChosenBy::selector`'s exhaustive match forces a third claimant to\nbe *mapped* rather than quietly accepted, and `ALL` is the whole accepted\nvocabulary for both the flag parser and the wire.",
        "oneOf": [
          {
            "const": "agent",
            "description": "An agent reviewed the sample photos and picked one.",
            "type": "string"
          },
          {
            "const": "human",
            "description": "A human did.",
            "type": "string"
          }
        ]
      },
      "ContainerRefusal": {
        "description": "The container half of an `Error::FormatUnsupported`: the file a caller named, the format\nthe camera actually delivered, and the containers that would have taken it.\n\n**The lever this refusal turns is the file extension, and until 2026-08-16 the payload\nnamed a different one.** D7 puts `FormatUnsupported { available }` on the record path in so\nmany words, so a `record -o take.y4m` against a camera that negotiated MJPG answered\n`requested: \"MJPG\"` with `available: [\"YUYV\", \"NV12\", \"GREY\"]` — the *container's* list.\nBoth fields mislead an unattended reader, and each in its own way:\n\n- `requested` held the **negotiated** format. The caller never typed MJPG; a ranking chose\n  it. A reader repairing `requested` repairs a request it did not make.\n- `available` held formats that need not exist **on that camera at all**. Measured through\n  the shipped binary over `corpus/profiles/chicony-rgb.json`, which enumerates MJPG and\n  YUYV: two of the three offered as remedies are formats that sensor has never had. That is\n  note **N129**'s misdirection — a refusal telling a caller to retry with something the\n  device does not have — surviving in the payload after the *message* was repaired for it\n  (note **N211**).\n\nSo the container case carries its own payload, `SizeRefusal`'s shape one cause along, and\nwhat it names is always true and always actionable: this build's containers are a closed set\nof two, so \"which file extension would have taken these frames\" is a question the schema can\nanswer without knowing anything about the camera. Build one through\n`Error::container_unsupported`.",
        "properties": {
          "carried_by": {
            "description": "Every container this build writes that *would* carry `negotiated`.\n\nThe remedy, and it is a fact about this build rather than about the camera — which is\nwhat makes it safe to act on unattended. Empty when no container carries the format at\nall, and that is the honest answer to \"what should I do\": nothing, this build cannot\nrecord these frames, and a second attempt with another extension meets the same wall.",
            "items": {
              "$ref": "#/components/schemas/VideoFormat"
            },
            "type": "array"
          },
          "container": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/VideoFormat"
              },
              {
                "type": "null"
              }
            ],
            "description": "The container the sink path's extension named.\n\n`None` when the path carried no extension, which means the caller left the container to\nthe negotiated format (`video::RecordRequest::container`) and this build writes\nno container that carries it. A different extension is then not the remedy, because the\ncaller never named one."
          },
          "negotiated": {
            "$ref": "#/components/schemas/PixelFormat",
            "description": "The format the device negotiated — the thing that could not be written.\n\n**Not what the caller asked for**, and that distinction is the whole reason this field\nexists rather than reusing `requested`: a recording's format is chosen by D5's ranking\nunless the caller named one, so the name in a container refusal is usually the device's\nanswer and not the caller's question."
          }
        },
        "required": [
          "negotiated",
          "carried_by"
        ],
        "type": "object"
      },
      "ControlDesc": {
        "description": "Everything the device says about one control.",
        "properties": {
          "current": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/ControlValue"
              },
              {
                "type": "null"
              }
            ],
            "description": "The current value **as read, unvalidated** — outside the range is a fact about\nthe device, not an error to correct [PF:4].\n\nAbsent for two different reasons, and telling them apart is `type` and `flags`\nrather than this field. A control that **declares** it has no value — a button, a\ncontrol class, a write-only or a disabled control, or a compound whose declared\npayload size is one no reader will fetch — was never going to have one. Anything\nelse with no value here was *asked and did not answer*: the device declined the\nread. The two are one predicate apart on this same document, so a consumer with no\nRust toolchain can make the distinction from the fields beside this one."
          },
          "default": {
            "description": "The declared default — which may sit outside `range` [PF:5].",
            "format": "int64",
            "type": "integer"
          },
          "dims": {
            "description": "Array dimensions, when the control is one.",
            "items": {
              "format": "uint32",
              "minimum": 0,
              "type": "integer"
            },
            "type": "array"
          },
          "elem_size": {
            "description": "Bytes per element.",
            "format": "uint32",
            "minimum": 0,
            "type": "integer"
          },
          "elems": {
            "description": "Element count for arrays and compound controls (1 for scalars).",
            "format": "uint32",
            "minimum": 0,
            "type": "integer"
          },
          "flags": {
            "$ref": "#/components/schemas/ControlFlags",
            "description": "Flags, raw and decoded."
          },
          "id": {
            "$ref": "#/components/schemas/ControlId",
            "description": "The kernel's control id."
          },
          "menu": {
            "additionalProperties": false,
            "description": "Menu items by index. Sparse [PF:2]; empty for non-menu controls.",
            "patternProperties": {
              "^\\d+$": {
                "$ref": "#/components/schemas/MenuItem"
              }
            },
            "type": "object"
          },
          "name": {
            "description": "The kernel's name, verbatim.",
            "type": "string"
          },
          "range": {
            "$ref": "#/components/schemas/ControlRange",
            "description": "The declared range."
          },
          "slug": {
            "$ref": "#/components/schemas/ControlSlug",
            "description": "The name through the D2 transform."
          },
          "type": {
            "$ref": "#/components/schemas/ControlType",
            "description": "The control's type, `Unknown` included."
          }
        },
        "required": [
          "id",
          "name",
          "slug",
          "type",
          "range",
          "default",
          "flags",
          "elems",
          "elem_size"
        ],
        "type": "object"
      },
      "ControlFlags": {
        "description": "A control's flags: the raw word and the decoded subset, both preserved.",
        "properties": {
          "known": {
            "description": "The bits this version can name, in declaration order.",
            "items": {
              "$ref": "#/components/schemas/KnownFlag"
            },
            "type": "array"
          },
          "raw": {
            "description": "Exactly what the kernel reported.",
            "format": "uint32",
            "minimum": 0,
            "type": "integer"
          },
          "unknown_bits": {
            "description": "The bits it cannot. Not an error, not dropped — next year's flag is data.",
            "format": "uint32",
            "minimum": 0,
            "type": "integer"
          }
        },
        "required": [
          "raw",
          "known",
          "unknown_bits"
        ],
        "type": "object"
      },
      "ControlId": {
        "description": "A V4L2 control id (`V4L2_CID_*`), as the kernel numbers it.",
        "format": "uint32",
        "minimum": 0,
        "type": "integer"
      },
      "ControlRange": {
        "description": "A control's declared range. Declared: the current value and the default are both\nfree to sit outside it, and on real hardware they do [PF:4, PF:5].",
        "properties": {
          "max": {
            "description": "Declared maximum.",
            "format": "int64",
            "type": "integer"
          },
          "min": {
            "description": "Declared minimum.",
            "format": "int64",
            "type": "integer"
          },
          "step": {
            "description": "Declared step. Zero and negative steps occur; treat them as 1 when planning\n(see `ControlRange::effective_step`) rather than dividing by them.",
            "format": "int64",
            "type": "integer"
          }
        },
        "required": [
          "min",
          "max",
          "step"
        ],
        "type": "object"
      },
      "ControlReport": {
        "description": "What `controls` answers.",
        "properties": {
          "camera": {
            "$ref": "#/components/schemas/CameraId",
            "description": "Which camera these came from — so a saved `--json` document is self-describing."
          },
          "controls": {
            "description": "Every control the device enumerated, including the ones this build cannot\ninterpret [PF:1].",
            "items": {
              "$ref": "#/components/schemas/ControlDesc"
            },
            "type": "array"
          },
          "pairs": {
            "description": "The auto/manual pairs in effect for this camera (design D3): the declared table\nfiltered to controls this device has, merged with anything a probe measured on it.\n\nEach pair carries its own `pairing::Provenance`, because \"the UVC spec\nsays so\" and \"this camera did it while we watched\" are different claims and E1\nmakes the second one win.",
            "items": {
              "$ref": "#/components/schemas/AutomationPair"
            },
            "type": "array"
          }
        },
        "required": [
          "camera",
          "controls"
        ],
        "type": "object"
      },
      "ControlRole": {
        "description": "What part a control plays in restore ordering.",
        "oneOf": [
          {
            "const": "automation",
            "description": "An automation control — restored first, so it is in place before the manual\nvalues it governs.",
            "type": "string"
          },
          {
            "const": "manual",
            "description": "Everything else.",
            "type": "string"
          }
        ]
      },
      "ControlSession": {
        "description": "One control's whole story within a session.",
        "properties": {
          "samples": {
            "description": "Every sample taken, in order.",
            "items": {
              "$ref": "#/components/schemas/Sample"
            },
            "type": "array"
          },
          "status": {
            "$ref": "#/components/schemas/ControlStatus",
            "description": "Where it stands."
          }
        },
        "required": [
          "status"
        ],
        "type": "object"
      },
      "ControlSlug": {
        "description": "A control's slug: its kernel name through the D2 transform, the spelling agents and\n`v4l2-ctl` users already know.",
        "type": "string"
      },
      "ControlStatus": {
        "description": "Where one control stands in a session (design D8's closed vocabulary).",
        "oneOf": [
          {
            "description": "Nothing has happened to it yet.",
            "properties": {
              "status": {
                "const": "untouched",
                "type": "string"
              }
            },
            "required": [
              "status"
            ],
            "type": "object"
          },
          {
            "description": "Its automation partner has been disabled and its value parked.",
            "properties": {
              "automation": {
                "description": "The automation controls that were switched off.",
                "items": {
                  "$ref": "#/components/schemas/ControlSlug"
                },
                "type": "array"
              },
              "parked_value": {
                "description": "The value the control holds while parked.",
                "format": "int64",
                "type": [
                  "integer",
                  "null"
                ]
              },
              "status": {
                "const": "auto_disabled",
                "type": "string"
              }
            },
            "required": [
              "status",
              "automation"
            ],
            "type": "object"
          },
          {
            "description": "A sweep is in progress.",
            "properties": {
              "adjustments": {
                "description": "Every way the planner's values differ from what `plan` literally asked for.\n\nD3's `{requested, applied}` doctrine one layer up, on the *document* rather than\non the event, because the event is gone the moment the process that watched it is\nand `calibrate sweep --json` answers this document (note **N145**). Empty means the\nspec was executed as written.",
                "items": {
                  "$ref": "#/components/schemas/SweepAdjustment"
                },
                "type": "array"
              },
              "done": {
                "description": "Samples taken.",
                "format": "uint32",
                "minimum": 0,
                "type": "integer"
              },
              "plan": {
                "$ref": "#/components/schemas/SweepSpec",
                "description": "The plan being executed — the spec as the caller wrote it, not the values it\nbecame. What the planner did to it is `adjustments` below."
              },
              "precision": {
                "default": 0,
                "description": "The smallest gap between the values this sweep will visit — the skill's\n\"calibration precision\", and the number a refinement pass divides down.\n\n**The planned stride, which is a different fact from the achieved one** — the\n`precision` a `calibrated` status carries is measured from the values the camera\nactually held. It is here because it is the answer to the question a caller asks\n*before* the photographs exist: `--precision 1` over a 10 000-wide range plans a\nstride of 40, and an agent told only `total` has no way to find that out until it\nhas compared 251 pictures at a resolution it believes is forty times finer than it\nis (note **N145**). Zero on a document an older build wrote, which is why it is\n`#[serde(default)]` rather than required.",
                "format": "int64",
                "type": "integer"
              },
              "status": {
                "const": "sweeping",
                "type": "string"
              },
              "total": {
                "description": "Samples planned.",
                "format": "uint32",
                "minimum": 0,
                "type": "integer"
              }
            },
            "required": [
              "status",
              "plan",
              "done",
              "total"
            ],
            "type": "object"
          },
          {
            "description": "A value has been chosen.",
            "properties": {
              "precision": {
                "description": "The final sampling step — the skill's \"calibration precision\". Multi-pass\nrefinement (coarse then fine) is representable because this is recorded.",
                "format": "int64",
                "type": "integer"
              },
              "score": {
                "description": "The score that value earned, when a metric produced one.",
                "format": "double",
                "type": [
                  "number",
                  "null"
                ]
              },
              "selector": {
                "$ref": "#/components/schemas/Selector",
                "description": "Who chose it."
              },
              "status": {
                "const": "calibrated",
                "type": "string"
              },
              "value": {
                "description": "The chosen value.",
                "format": "int64",
                "type": "integer"
              }
            },
            "required": [
              "status",
              "value",
              "precision",
              "selector"
            ],
            "type": "object"
          },
          {
            "description": "Deliberately set aside.",
            "properties": {
              "reason": {
                "description": "Why.",
                "type": "string"
              },
              "status": {
                "const": "deferred",
                "type": "string"
              }
            },
            "required": [
              "status",
              "reason"
            ],
            "type": "object"
          },
          {
            "description": "Cannot be calibrated on this device.",
            "properties": {
              "reason": {
                "$ref": "#/components/schemas/BlockedReason",
                "description": "Why."
              },
              "status": {
                "const": "blocked",
                "type": "string"
              }
            },
            "required": [
              "status",
              "reason"
            ],
            "type": "object"
          }
        ]
      },
      "ControlType": {
        "description": "A control's type.\n\nClosed for the types we can interpret, open-ended for the rest: `Unknown` carries the\nraw discriminant so a control we cannot read still enumerates, displays, round-trips,\nand is reported by name. The Chicony's `Region of Interest Rectangle` (type `0x0107`)\nis the seed case — the crate that panicked on it is why we own this layer [PF:1].",
        "oneOf": [
          {
            "description": "`V4L2_CTRL_TYPE_INTEGER`",
            "properties": {
              "kind": {
                "const": "integer",
                "type": "string"
              }
            },
            "required": [
              "kind"
            ],
            "type": "object"
          },
          {
            "description": "`V4L2_CTRL_TYPE_BOOLEAN`",
            "properties": {
              "kind": {
                "const": "boolean",
                "type": "string"
              }
            },
            "required": [
              "kind"
            ],
            "type": "object"
          },
          {
            "description": "`V4L2_CTRL_TYPE_MENU` — items are named strings, sparsely indexed [PF:2].",
            "properties": {
              "kind": {
                "const": "menu",
                "type": "string"
              }
            },
            "required": [
              "kind"
            ],
            "type": "object"
          },
          {
            "description": "`V4L2_CTRL_TYPE_BUTTON` — write-only trigger, no value.",
            "properties": {
              "kind": {
                "const": "button",
                "type": "string"
              }
            },
            "required": [
              "kind"
            ],
            "type": "object"
          },
          {
            "description": "`V4L2_CTRL_TYPE_INTEGER64`",
            "properties": {
              "kind": {
                "const": "integer64",
                "type": "string"
              }
            },
            "required": [
              "kind"
            ],
            "type": "object"
          },
          {
            "description": "`V4L2_CTRL_TYPE_CTRL_CLASS` — a class header, not a real control.",
            "properties": {
              "kind": {
                "const": "control_class",
                "type": "string"
              }
            },
            "required": [
              "kind"
            ],
            "type": "object"
          },
          {
            "description": "`V4L2_CTRL_TYPE_STRING`",
            "properties": {
              "kind": {
                "const": "string",
                "type": "string"
              }
            },
            "required": [
              "kind"
            ],
            "type": "object"
          },
          {
            "description": "`V4L2_CTRL_TYPE_BITMASK`",
            "properties": {
              "kind": {
                "const": "bitmask",
                "type": "string"
              }
            },
            "required": [
              "kind"
            ],
            "type": "object"
          },
          {
            "description": "`V4L2_CTRL_TYPE_INTEGER_MENU` — items are integers, sparsely indexed.",
            "properties": {
              "kind": {
                "const": "integer_menu",
                "type": "string"
              }
            },
            "required": [
              "kind"
            ],
            "type": "object"
          },
          {
            "description": "`V4L2_CTRL_TYPE_U8` — compound array of bytes.",
            "properties": {
              "kind": {
                "const": "u8",
                "type": "string"
              }
            },
            "required": [
              "kind"
            ],
            "type": "object"
          },
          {
            "description": "`V4L2_CTRL_TYPE_U16` — compound array.",
            "properties": {
              "kind": {
                "const": "u16",
                "type": "string"
              }
            },
            "required": [
              "kind"
            ],
            "type": "object"
          },
          {
            "description": "`V4L2_CTRL_TYPE_U32` — compound array.",
            "properties": {
              "kind": {
                "const": "u32",
                "type": "string"
              }
            },
            "required": [
              "kind"
            ],
            "type": "object"
          },
          {
            "description": "`V4L2_CTRL_TYPE_AREA` — `{width, height}`.",
            "properties": {
              "kind": {
                "const": "area",
                "type": "string"
              }
            },
            "required": [
              "kind"
            ],
            "type": "object"
          },
          {
            "description": "`V4L2_CTRL_TYPE_RECT` — `{left, top, width, height}`; the PF:1 control.",
            "properties": {
              "kind": {
                "const": "rect",
                "type": "string"
              }
            },
            "required": [
              "kind"
            ],
            "type": "object"
          },
          {
            "description": "Anything else the kernel emits: carried as an opaque payload.\n\nThe payload's size is on the descriptor (`elem_size` × `elems`), not here — one\nhome per fact.",
            "properties": {
              "kind": {
                "const": "unknown",
                "type": "string"
              },
              "raw": {
                "description": "The kernel's discriminant, preserved exactly.",
                "format": "uint32",
                "minimum": 0,
                "type": "integer"
              }
            },
            "required": [
              "kind",
              "raw"
            ],
            "type": "object"
          }
        ]
      },
      "ControlValue": {
        "description": "A control value, as read or as written.\n\nThree shapes, not fifteen: scalars are integers whatever their declared type, and\ninterpretation is the descriptor's job. Payload types are opaque bytes, which is\nexactly enough to round-trip a control we cannot read.",
        "oneOf": [
          {
            "description": "Integer, boolean, menu index, bitmask — anything scalar.",
            "properties": {
              "kind": {
                "const": "int",
                "type": "string"
              },
              "value": {
                "format": "int64",
                "type": "integer"
              }
            },
            "required": [
              "kind",
              "value"
            ],
            "type": "object"
          },
          {
            "description": "`V4L2_CTRL_TYPE_STRING`.",
            "properties": {
              "kind": {
                "const": "text",
                "type": "string"
              },
              "value": {
                "type": "string"
              }
            },
            "required": [
              "kind",
              "value"
            ],
            "type": "object"
          },
          {
            "description": "A compound or unrecognized payload, byte-exact.",
            "properties": {
              "kind": {
                "const": "bytes",
                "type": "string"
              },
              "value": {
                "items": {
                  "format": "uint8",
                  "maximum": 255,
                  "minimum": 0,
                  "type": "integer"
                },
                "type": "array"
              }
            },
            "required": [
              "kind",
              "value"
            ],
            "type": "object"
          }
        ]
      },
      "ControlWrite": {
        "description": "One requested write: which control, and the value to put in it.\n\nA named pair rather than a `(ControlSlug, ControlValue)` tuple because this is what\n`set` takes on the T5 wire (D10), and a tuple crosses JSON as\n`[\"brightness\", {\"kind\":\"int\",\"value\":50}]` — positional, unnameable in the emitted\nbundle, and a shape a hand-written client has to be told about out of band.\n\nThe answer to one of these is an `Applied`, which is where E4's `{requested,\napplied}` pair lives; nothing here records what the device took, because at this point\nnothing has asked it.\n\n**How far this spelling reaches, and why it stops there.** It is the shape of a requested\nwrite from the command line inward: `cli_core::Assignment` is a clap newtype over one of\nthese, and `cli_core::Executor::set` takes a slice of them — so `webcam-handler-cli` and\n`webcam-handler-client` hand their shared command surface the *same* value, and P4f's\nparity gate compares two paths whose input has one shape. `engine::pairing` and\n`engine::write` still take `(ControlSlug, ControlValue)` pairs, and that is a deliberate\nstop rather than an oversight: the planner is a pure core on the mutation floor, its\ncallers build targets inline in dozens of places, and nothing downstream of the executor\nserializes anything — a named pair buys a wire document nothing there emits. The conversion\nto the planner's spelling is `ControlWrite::target`, which is on the type rather than at\nits call sites because P4c gave it a second caller: `webcam-handler-cli set` and\n`webcam-handler-daemon`'s `wch_set` both cross this boundary, and two spellings of \"which\nfields of a requested write are the target\" is how the two surfaces P4f compares start to\ndiffer. Note N35 carries the rest. Contrast `pairing::ProbeSkip`, which *was*\npushed all the way into `engine::discover`: there the tuple had one producer and one\nconsumer, so one spelling cost four lines.",
        "properties": {
          "control": {
            "$ref": "#/components/schemas/ControlSlug",
            "description": "Which control."
          },
          "value": {
            "$ref": "#/components/schemas/ControlValue",
            "description": "The value to write. Requested, not applied (E4) — what the device took comes back\nin `Applied::applied`."
          }
        },
        "required": [
          "control",
          "value"
        ],
        "type": "object"
      },
      "DeviceNode": {
        "description": "One `/dev/videoN` node belonging to a camera.",
        "properties": {
          "capabilities": {
            "description": "The whole-device `capabilities` word, kept because it differs from `device_caps`\non multi-node devices and the difference is the point.",
            "format": "uint32",
            "minimum": 0,
            "type": "integer"
          },
          "device_caps": {
            "description": "The node's own `device_caps`.",
            "format": "uint32",
            "minimum": 0,
            "type": "integer"
          },
          "kind": {
            "$ref": "#/components/schemas/NodeKind",
            "description": "What it is for."
          },
          "path": {
            "description": "The device path.",
            "type": "string"
          }
        },
        "required": [
          "path",
          "kind",
          "device_caps",
          "capabilities"
        ],
        "type": "object"
      },
      "DeviceProfile": {
        "description": "A captured device profile.",
        "properties": {
          "invariant": {
            "$ref": "#/components/schemas/ProfileInvariant",
            "description": "What does not change."
          },
          "provenance": {
            "$ref": "#/components/schemas/ProfileProvenance",
            "description": "Where this came from."
          },
          "schema_version": {
            "description": "The document version.",
            "format": "uint32",
            "minimum": 0,
            "type": "integer"
          },
          "state": {
            "$ref": "#/components/schemas/ProfileState",
            "default": {
              "flags": {},
              "values": {}
            },
            "description": "What does."
          }
        },
        "required": [
          "schema_version",
          "provenance",
          "invariant"
        ],
        "type": "object"
      },
      "DiscoveryReport": {
        "description": "What `discover_pairs` answers (design D3, D4, note N9).\n\nMore than a `ControlReport`, because D3's second layer *writes to the camera*: it toggles\nautomation-shaped controls and puts them back. `webcam-handler-cli` prints what the probe\ndeclined and what it could not restore on standard error, and a caller on the other end of\na socket that could not see those two facts would be running a write with its restoration\nreport withheld — which is AGENTS rule 8 (\"tests assert restoration\") turned into a wire\nproperty.\n\n**Two of the three fields come straight from `engine::discover::Discovery`; the third is\nassembled.** `skipped` and `restored` are that type's own. `controls` is not: `Discovery`\ncarries `pairs`, the measured relationships, and turning those into a `ControlReport`\nmeans re-reading the control set *after* the probe put the camera back and merging declared\nwith measured so measured wins (E1) — which is `engine::pairing::applicable(&controls,\n&merge(declared_pairs(), measured))`, exactly what `webcam-handler-cli controls\n--discover-pairs` does today in `crates/cli`'s `InProcess::controls`. Whoever routes this\nmethod next assembles it the same way or the two surfaces disagree about the provenance on\ntheir pairs; note N34 records that the assembly wants one home before it has two callers.",
        "properties": {
          "controls": {
            "$ref": "#/components/schemas/ControlReport",
            "description": "The control set as it stands after the probe put the camera back, carrying the\ndeclared and measured pairs merged with measured winning (E1)."
          },
          "restored": {
            "$ref": "#/components/schemas/RestoreReport",
            "description": "What putting the camera back achieved. Reported rather than assumed — and\n`OwnedByAutomation` is a success, not a failure (note N9)."
          },
          "skipped": {
            "description": "The automation-shaped controls the probe did not toggle, and why.\n\nA probe silent about what it passed over reads as a probe that found nothing\nthere, and \"this camera has no pairs\" and \"this probe did not look at half of\nthem\" are answers a caller acts on very differently.",
            "items": {
              "$ref": "#/components/schemas/ProbeSkip"
            },
            "type": "array"
          }
        },
        "required": [
          "controls",
          "restored"
        ],
        "type": "object"
      },
      "Error": {
        "description": "Everything that can go wrong, as a value the caller can branch on.",
        "oneOf": [
          {
            "description": "The device disappeared — unplugged, or the driver unbound.",
            "properties": {
              "kind": {
                "const": "device_gone",
                "type": "string"
              },
              "path": {
                "description": "The node that vanished.",
                "type": "string"
              }
            },
            "required": [
              "kind",
              "path"
            ],
            "type": "object"
          },
          {
            "description": "Another process holds the device. Distinct from \"the camera cannot do this\" (E3).\n\nBuild one through `Error::busy` or `Error::busy_here`: which of the two says\nwhether the holder is somebody else or this very process, and that is the difference\nbetween \"go and find out who has it\" and \"wait for what I am doing\".",
            "properties": {
              "holders": {
                "description": "Who has it, as far as this user could see.\n\nEmpty means *unidentified*, not *nobody*: a holder belonging to another user\nis invisible in `/proc` without privilege, and a process that exited between\nthe `EBUSY` and the walk leaves nothing to find.",
                "items": {
                  "$ref": "#/components/schemas/Holder"
                },
                "type": "array"
              },
              "kind": {
                "const": "busy",
                "type": "string"
              },
              "path": {
                "description": "The node in use.",
                "type": "string"
              },
              "this_process": {
                "anyOf": [
                  {
                    "$ref": "#/components/schemas/Occupation"
                  },
                  {
                    "type": "null"
                  }
                ],
                "description": "What **this** process is doing with the node, when this process is the holder.\n\nA daemon refusing its own caller walked no `/proc` and needs to walk none: it\nknows. Until 2026-08-17 it said so by leaving `holders` empty — deliberately,\nand argued in `daemon::record`'s header, because *\"naming it would invite a\nclient to kill the daemon it is talking to\"* — and the rendering then told the\ncaller the node was *\"held by an unidentified process\"*, which is the one thing\nit was not. Measured through the shipped binaries: a `photo` during a take\nanswered `{\"kind\":\"busy\",\"holders\":[]}` and printed that sentence, so an agent's\nnext move was to hunt for a process to kill (docs/11 **M19**, note **N217**).\n\nThis field is how the daemon says \"it is me, and here is what for\" **without**\nhanding over a pid — the withholding stays, and what it withholds stops reading\nas ignorance. `None` is every other producer, and means exactly what the empty\n`holders` list has always meant."
              }
            },
            "required": [
              "kind",
              "path",
              "holders"
            ],
            "type": "object"
          },
          {
            "description": "The device node exists but we may not open it. The hint lives here, once.",
            "properties": {
              "hint": {
                "description": "What to do about it.",
                "type": "string"
              },
              "kind": {
                "const": "permission_denied",
                "type": "string"
              },
              "path": {
                "description": "The node.",
                "type": "string"
              }
            },
            "required": [
              "kind",
              "path",
              "hint"
            ],
            "type": "object"
          },
          {
            "description": "No camera matched the selector — an id, a prefix, or any of D14's other spellings.",
            "properties": {
              "kind": {
                "const": "camera_unknown",
                "type": "string"
              },
              "requested": {
                "description": "What the caller asked for.",
                "type": "string"
              }
            },
            "required": [
              "kind",
              "requested"
            ],
            "type": "object"
          },
          {
            "description": "Several cameras matched the prefix.",
            "properties": {
              "candidates": {
                "description": "Which cameras it could mean.",
                "items": {
                  "$ref": "#/components/schemas/CameraId"
                },
                "type": "array"
              },
              "kind": {
                "const": "camera_ambiguous",
                "type": "string"
              },
              "requested": {
                "description": "What the caller asked for.",
                "type": "string"
              }
            },
            "required": [
              "kind",
              "requested",
              "candidates"
            ],
            "type": "object"
          },
          {
            "description": "No control by that slug or id.",
            "properties": {
              "did_you_mean": {
                "description": "The closest slugs this camera does have.",
                "items": {
                  "$ref": "#/components/schemas/ControlSlug"
                },
                "type": "array"
              },
              "kind": {
                "const": "control_unknown",
                "type": "string"
              },
              "requested": {
                "description": "What the caller asked for.",
                "type": "string"
              }
            },
            "required": [
              "kind",
              "requested",
              "did_you_mean"
            ],
            "type": "object"
          },
          {
            "description": "The control exists and cannot be written [PF:12].",
            "properties": {
              "control": {
                "$ref": "#/components/schemas/ControlSlug",
                "description": "Which control."
              },
              "kind": {
                "const": "control_read_only",
                "type": "string"
              }
            },
            "required": [
              "kind",
              "control"
            ],
            "type": "object"
          },
          {
            "description": "An automation partner currently owns the control [PF:3]. Actionable: the\nvariant names the control to disable.",
            "properties": {
              "automation": {
                "anyOf": [
                  {
                    "$ref": "#/components/schemas/ControlSlug"
                  },
                  {
                    "type": "null"
                  }
                ],
                "description": "The automation control to disable first, when one was discovered."
              },
              "control": {
                "$ref": "#/components/schemas/ControlSlug",
                "description": "Which control."
              },
              "kind": {
                "const": "control_inactive",
                "type": "string"
              }
            },
            "required": [
              "kind",
              "control"
            ],
            "type": "object"
          },
          {
            "description": "The requested pixel format is not one that would be accepted here.\n\n**Whose list `available` is depends on who refused, and the message must not\nguess.** It said \"this camera offers …\" until 2026-08-15, which is true of D6's\nsource-format refusal and false of the other two callers: `engine::preview`\nanswers with the format the negotiation actually produced, and D7's record path\nanswers with what the *container* would have carried. Measured at the Chicony IR\nsensor, which offers GREY and nothing else — a `record -o x.avi` refusal read\n\"format GREY is unavailable; this camera offers MJPG, JPEG\", naming two formats\nthat camera has never had. An unattended caller obeying it would retry\n`--pixel-format MJPG` against a device with no MJPG, which is the defect the D13\nregistry exists to prevent rather than commit (note **N129**).\n\nThe variant itself is right where it is: design D7 names\n`FormatUnsupported { available }` for the container case in so many words, so this\nis a rendering repair and not a re-litigation of which refusal applies. The\nsentence now says only what is true of every caller — these are the formats that\n*would* be accepted — and which lever to pull is the guide's `Do` column, where a\ncontainer mismatch is answered by changing the container rather than the format.\n\n**And the same lesson had to be learnt again about the *size*** (note **N138**).\nA request refused because no mode could deliver the size it named had nowhere to\nsay so: it reported `requested: None` and the sentence came out *\"format\n(unspecified) is unavailable; MJPG, YUYV would be accepted\"* — about formats, naming\nthe caller's own format as acceptable, never mentioning size. `SizeRefusal` is the\nslot N134 said this variant would need, and `size` is now what distinguishes the two\ncauses; build one through `Error::format_unsupported` or\n`Error::size_unsupported` rather than by hand, so they stay exclusive.\n\n**And a third time about the *container*** (note **N211**), which is where N129's\nrepair had stopped: the message stopped saying \"this camera offers\" but `available`\nwent on holding the container's own format list, so `record -o take.y4m` over\n`corpus/profiles/chicony-rgb.json` — a camera enumerating MJPG and YUYV — answered\n`requested: \"MJPG\"` with `available: [\"YUYV\", \"NV12\", \"GREY\"]`. Two of the three\nremedies are formats that sensor has never had, and the one the caller is told it\nasked for was chosen by D5's ranking rather than typed. `ContainerRefusal` is that\ncause's own payload and `container` is what distinguishes it; the door in is\n`Error::container_unsupported`.\n\nSo the variant has three causes and one lever each — a format, a size, a file\nextension — and which one applies is a field rather than a sentence to parse.",
            "properties": {
              "available": {
                "description": "What would be accepted — the camera's formats, the negotiated one, or the\ncontainer's, according to which of the three callers refused.",
                "items": {
                  "$ref": "#/components/schemas/PixelFormat"
                },
                "type": "array"
              },
              "container": {
                "anyOf": [
                  {
                    "$ref": "#/components/schemas/ContainerRefusal"
                  },
                  {
                    "type": "null"
                  }
                ],
                "description": "The container that could not carry the stream, present **exactly when** the file\nrather than the camera is what this refusal is about.\n\nExclusive with `size` for the same reason `size` is exclusive with `requested`:\na refusal naming two levers is a refusal an unattended caller pulls at random.\nThe three doors — `Error::format_unsupported`, `Error::size_unsupported`,\n`Error::container_unsupported` — are what keep them apart, and a value built\nby hand around them is a defect rather than a shortcut."
              },
              "kind": {
                "const": "format_unsupported",
                "type": "string"
              },
              "requested": {
                "anyOf": [
                  {
                    "$ref": "#/components/schemas/PixelFormat"
                  },
                  {
                    "type": "null"
                  }
                ],
                "description": "The format that was asked for, when the caller named one **and the format is\nwhat could not be met**. `None` when the caller named none, and never a\nstand-in for \"the size was the problem\" — `size` says that outright."
              },
              "size": {
                "anyOf": [
                  {
                    "$ref": "#/components/schemas/SizeRefusal"
                  },
                  {
                    "type": "null"
                  }
                ],
                "description": "The size that could not be delivered, present **exactly when** the size rather\nthan the format is what this refusal is about.\n\nAbsent for every other producer of this variant — the D6 source-format refusal,\n`engine::preview`'s unrenderable negotiation, and D7's container refusal all\nrefuse a *format* or a file — which is what makes `size.is_some()` a reliable\ndiscriminator rather than a hint. `engine::preview::negotiate` reads it as one:\nits cap can be dropped and retried, and a format it named and the device lacks\ncannot."
              }
            },
            "required": [
              "kind",
              "available"
            ],
            "type": "object"
          },
          {
            "description": "Frames kept arriving but the settle policy never converged [PF:11].",
            "properties": {
              "frames_seen": {
                "description": "How many frames arrived in that time — zero means a different problem.",
                "format": "uint32",
                "minimum": 0,
                "type": "integer"
              },
              "kind": {
                "const": "settle_timeout",
                "type": "string"
              },
              "waited_ms": {
                "description": "How long we waited.",
                "format": "uint64",
                "minimum": 0,
                "type": "integer"
              }
            },
            "required": [
              "kind",
              "waited_ms",
              "frames_seen"
            ],
            "type": "object"
          },
          {
            "description": "The camera is not the one this session was recorded against.",
            "properties": {
              "fields": {
                "description": "Which fields disagree.",
                "items": {
                  "type": "string"
                },
                "type": "array"
              },
              "kind": {
                "const": "fingerprint_mismatch",
                "type": "string"
              }
            },
            "required": [
              "kind",
              "fields"
            ],
            "type": "object"
          },
          {
            "description": "Another session already owns this (camera, task) pair.",
            "properties": {
              "detail": {
                "description": "What conflicts.",
                "type": "string"
              },
              "kind": {
                "const": "session_conflict",
                "type": "string"
              }
            },
            "required": [
              "kind",
              "detail"
            ],
            "type": "object"
          },
          {
            "description": "The calibration state machine refused a transition (design D8).\n\n**The condition first and the instruction last**, and the order is the whole of what\nthis rendering decides. The template was `\"cannot {op} from state {from}\"` when D8 was\nthe only producer and `op` was a verb phrase — `\"select\"`, `\"sweep privacy\"` — where a\ntrailing `from state untouched` reads. The variant now has producers across most of the\nworkspace and most of them put a **multi-clause instruction** in `op`, because that is\nthe field a caller acts on; appending anything to such a sentence garbles it. How many\nthere are, and in how many crates, is deliberately not written here: a prose count of\ncode is a claim something reconciles or it is not made (notes **N153**, **N158**), and\nthe last one written here — \"eleven producers across five crates\" — was wrong in both\nhalves and in the same direction, which note **N303** measured rather than restating.\nWhat is reconciled is this crate's own producers, by\n`an_illegal_transitions_instruction_is_the_last_thing_it_says`, which reads the\nconstruction sites out of the source beside it. Measured through the shipped binaries\nbefore this changed:\n\n```text\ncannot write a photo to …/x.tiff; this build writes .jpg, .png, .ppm from state unwritable_extension(tiff)\n```\n\nSo `from` becomes the label it has always been — a machine-shaped condition,\n`unwritable_extension(tiff)`, `no_session(nightly)`, `not_one_process(0)` — and `op`\nends the message, where a sentence of any length can end.\n`an_illegal_transitions_instruction_is_the_last_thing_it_says` is what keeps a suffix\nfrom growing back (docs/11 **L29**, note **N212**).",
            "properties": {
              "from": {
                "description": "The state we were in.",
                "type": "string"
              },
              "kind": {
                "const": "illegal_transition",
                "type": "string"
              },
              "op": {
                "description": "The operation attempted.\n\nA whole instruction rather than a verb, for most of the producers: the rendering\nabove puts it last precisely so it can be one.",
                "type": "string"
              }
            },
            "required": [
              "kind",
              "from",
              "op"
            ],
            "type": "object"
          },
          {
            "description": "A persisted document was written by a version we do not understand (D9).",
            "properties": {
              "found": {
                "description": "The version on disk.",
                "format": "uint32",
                "minimum": 0,
                "type": "integer"
              },
              "kind": {
                "const": "schema_version_foreign",
                "type": "string"
              },
              "supported": {
                "description": "The version this build reads.",
                "format": "uint32",
                "minimum": 0,
                "type": "integer"
              }
            },
            "required": [
              "kind",
              "found",
              "supported"
            ],
            "type": "object"
          },
          {
            "description": "The state directory's advisory lock is held elsewhere (D9).\n\nBoth fields come from the same lock record and are `None` together: an\nunidentified holder is reported as unidentified, never as a plausible pid — and\nnever as a protocol somebody would then act on.",
            "properties": {
              "holder": {
                "anyOf": [
                  {
                    "$ref": "#/components/schemas/Holder"
                  },
                  {
                    "type": "null"
                  }
                ],
                "description": "Who holds it, when we could tell."
              },
              "kind": {
                "const": "store_locked",
                "type": "string"
              },
              "protocol": {
                "anyOf": [
                  {
                    "$ref": "#/components/schemas/LockProtocol"
                  },
                  {
                    "type": "null"
                  }
                ],
                "description": "Which of D9's protocols the holder follows, when we could tell.\n\nThe field exists so the refusal can say whether waiting is worth anything:\n`LockProtocol::advice` is the one place that turns it into words."
              }
            },
            "required": [
              "kind"
            ],
            "type": "object"
          },
          {
            "description": "`terminate_holder` was asked to kill a pid that no longer holds the device.",
            "properties": {
              "kind": {
                "const": "holder_gone",
                "type": "string"
              },
              "pid": {
                "description": "The pid named in the request.",
                "format": "int32",
                "type": "integer"
              }
            },
            "required": [
              "kind",
              "pid"
            ],
            "type": "object"
          },
          {
            "description": "The kernel refused an operation for a reason with no more specific home.\n\nDeliberately typed rather than stringly: `operation` and `errno` are what a\ncaller (or a bug report) actually needs. See note N4.",
            "properties": {
              "errno": {
                "description": "`errno`, when there was one.",
                "format": "int32",
                "type": [
                  "integer",
                  "null"
                ]
              },
              "kind": {
                "const": "device_io",
                "type": "string"
              },
              "message": {
                "description": "The system's description.",
                "type": "string"
              },
              "operation": {
                "description": "The ioctl or syscall attempted.",
                "type": "string"
              }
            },
            "required": [
              "kind",
              "operation",
              "message"
            ],
            "type": "object"
          },
          {
            "description": "A filesystem operation on the state directory failed (disk full, and friends).",
            "properties": {
              "errno": {
                "description": "`errno`, when there was one.",
                "format": "int32",
                "type": [
                  "integer",
                  "null"
                ]
              },
              "kind": {
                "const": "storage_io",
                "type": "string"
              },
              "message": {
                "description": "The system's description.",
                "type": "string"
              },
              "path": {
                "description": "The path involved.",
                "type": "string"
              }
            },
            "required": [
              "kind",
              "path",
              "message"
            ],
            "type": "object"
          }
        ]
      },
      "ErrorKind": {
        "description": "The discriminant of `Error`, as a value.\n\n`ALL` is generated from this definition, so a variant cannot be added without\njoining every completeness check that walks it (rubric rule 6: no hand lists).",
        "oneOf": [
          {
            "const": "device_gone",
            "description": "See `Error::DeviceGone`.",
            "type": "string"
          },
          {
            "const": "busy",
            "description": "See `Error::Busy`.",
            "type": "string"
          },
          {
            "const": "permission_denied",
            "description": "See `Error::PermissionDenied`.",
            "type": "string"
          },
          {
            "const": "camera_unknown",
            "description": "See `Error::CameraUnknown`.",
            "type": "string"
          },
          {
            "const": "camera_ambiguous",
            "description": "See `Error::CameraAmbiguous`.",
            "type": "string"
          },
          {
            "const": "control_unknown",
            "description": "See `Error::ControlUnknown`.",
            "type": "string"
          },
          {
            "const": "control_read_only",
            "description": "See `Error::ControlReadOnly`.",
            "type": "string"
          },
          {
            "const": "control_inactive",
            "description": "See `Error::ControlInactive`.",
            "type": "string"
          },
          {
            "const": "format_unsupported",
            "description": "See `Error::FormatUnsupported`.",
            "type": "string"
          },
          {
            "const": "settle_timeout",
            "description": "See `Error::SettleTimeout`.",
            "type": "string"
          },
          {
            "const": "fingerprint_mismatch",
            "description": "See `Error::FingerprintMismatch`.",
            "type": "string"
          },
          {
            "const": "session_conflict",
            "description": "See `Error::SessionConflict`.",
            "type": "string"
          },
          {
            "const": "illegal_transition",
            "description": "See `Error::IllegalTransition`.",
            "type": "string"
          },
          {
            "const": "schema_version_foreign",
            "description": "See `Error::SchemaVersionForeign`.",
            "type": "string"
          },
          {
            "const": "store_locked",
            "description": "See `Error::StoreLocked`.",
            "type": "string"
          },
          {
            "const": "holder_gone",
            "description": "See `Error::HolderGone`.",
            "type": "string"
          },
          {
            "const": "device_io",
            "description": "See `Error::DeviceIo`.",
            "type": "string"
          },
          {
            "const": "storage_io",
            "description": "See `Error::StorageIo`.",
            "type": "string"
          }
        ]
      },
      "FormatInfo": {
        "description": "One pixel format and everything available in it.\n\nNesting is load-bearing: the OBSBOT offers MJPG well past where YUYV stops on the same\ncable — 3840×2160 against 640×480 when PF:9 measured it, 1920×1440 against 640×480\ntoday — so a flat size list would be a lie [PF:9]. The numbers moved because the\ndevice stopped advertising its 4K mode and the corpus was re-captured [PF:23]; the\ngap they illustrate is what the nesting is for, and that did not.",
        "properties": {
          "description": {
            "description": "The driver's description string.",
            "type": "string"
          },
          "flags": {
            "description": "The `v4l2_fmtdesc` flags word.",
            "format": "uint32",
            "minimum": 0,
            "type": "integer"
          },
          "pixel_format": {
            "$ref": "#/components/schemas/PixelFormat",
            "description": "The format."
          },
          "sizes": {
            "description": "Sizes, each with its own intervals.",
            "items": {
              "$ref": "#/components/schemas/FrameSizeInfo"
            },
            "type": "array"
          }
        },
        "required": [
          "pixel_format",
          "description",
          "flags",
          "sizes"
        ],
        "type": "object"
      },
      "FrameInterval": {
        "description": "A frame interval (the reciprocal of a frame rate — the kernel's own convention).",
        "oneOf": [
          {
            "description": "One exact interval, as a fraction of a second.",
            "properties": {
              "denominator": {
                "description": "Denominator.",
                "format": "uint32",
                "minimum": 0,
                "type": "integer"
              },
              "kind": {
                "const": "discrete",
                "type": "string"
              },
              "numerator": {
                "description": "Numerator.",
                "format": "uint32",
                "minimum": 0,
                "type": "integer"
              }
            },
            "required": [
              "kind",
              "numerator",
              "denominator"
            ],
            "type": "object"
          },
          {
            "description": "A range with a step.",
            "properties": {
              "kind": {
                "const": "stepwise",
                "type": "string"
              },
              "max_denominator": {
                "description": "Longest interval denominator.",
                "format": "uint32",
                "minimum": 0,
                "type": "integer"
              },
              "max_numerator": {
                "description": "Longest interval numerator.",
                "format": "uint32",
                "minimum": 0,
                "type": "integer"
              },
              "min_denominator": {
                "description": "Shortest interval denominator.",
                "format": "uint32",
                "minimum": 0,
                "type": "integer"
              },
              "min_numerator": {
                "description": "Shortest interval numerator.",
                "format": "uint32",
                "minimum": 0,
                "type": "integer"
              }
            },
            "required": [
              "kind",
              "min_numerator",
              "min_denominator",
              "max_numerator",
              "max_denominator"
            ],
            "type": "object"
          },
          {
            "description": "An interval described with a `v4l2_frmivaltypes` value this build cannot interpret.\nCarried for `FrameSize::Unknown`'s reason.",
            "properties": {
              "kind": {
                "const": "unknown",
                "type": "string"
              },
              "raw": {
                "description": "The kernel's `type` discriminant, preserved exactly.",
                "format": "uint32",
                "minimum": 0,
                "type": "integer"
              }
            },
            "required": [
              "kind",
              "raw"
            ],
            "type": "object"
          },
          {
            "description": "The device names no interval at all: it cleared `V4L2_CAP_TIMEPERFRAME` in its\n`G_PARM` reply, which is the driver saying frame-interval negotiation is not\nsomething this node does.\n\n**The fourth answer, and it exists because the third was being used as one.** The\n`unknown` kind carries *the kernel's own `type` discriminant*, and a device that\nnamed no shape has no discriminant to carry — the `{\"kind\": \"unknown\", \"raw\": 0}`\nthe V4L2 backend used to produce here was a number this build invented, and `0` is\nnot even a spare value: it is what a driver that filled in nothing would write.\nD2 represents what it cannot interpret; manufacturing the evidence for it is the\none thing D2 forbids.\n\n**This means the bit and nothing else.** A driver that *sets* the bit and then\nwrites a fraction with a zero in it has said something, and what it said is carried\nas a `discrete` interval holding that fraction — a rate nobody can compute, which\n`fps` answers `null` for. Reporting that as `unstated` would be a second invented\nclaim about the device where the first one was removed.\n\nDistinct from an *absent* interval, which this vocabulary has no spelling for and\ndoes not need: a negotiated stream always reports one, and this is what it reports\nwhen the device would not say. There is no rate here — the same as `unknown` — so a\nconsumer that wants frames per second has to handle both.\n\n**As a request** it means the caller stated no interval, exactly as omitting the\nfield does; the negotiation reads back whatever the device is running at. It is not\na request for \"no interval\", because there is no such thing to ask a device for.",
            "properties": {
              "kind": {
                "const": "unstated",
                "type": "string"
              }
            },
            "required": [
              "kind"
            ],
            "type": "object"
          }
        ]
      },
      "FrameSize": {
        "description": "A frame size the device offers.",
        "oneOf": [
          {
            "description": "One exact size.",
            "properties": {
              "height": {
                "description": "Height in pixels.",
                "format": "uint32",
                "minimum": 0,
                "type": "integer"
              },
              "kind": {
                "const": "discrete",
                "type": "string"
              },
              "width": {
                "description": "Width in pixels.",
                "format": "uint32",
                "minimum": 0,
                "type": "integer"
              }
            },
            "required": [
              "kind",
              "width",
              "height"
            ],
            "type": "object"
          },
          {
            "description": "A range with a step.",
            "properties": {
              "kind": {
                "const": "stepwise",
                "type": "string"
              },
              "max_height": {
                "description": "Largest height.",
                "format": "uint32",
                "minimum": 0,
                "type": "integer"
              },
              "max_width": {
                "description": "Largest width.",
                "format": "uint32",
                "minimum": 0,
                "type": "integer"
              },
              "min_height": {
                "description": "Smallest height.",
                "format": "uint32",
                "minimum": 0,
                "type": "integer"
              },
              "min_width": {
                "description": "Smallest width.",
                "format": "uint32",
                "minimum": 0,
                "type": "integer"
              },
              "step_height": {
                "description": "Height step.",
                "format": "uint32",
                "minimum": 0,
                "type": "integer"
              },
              "step_width": {
                "description": "Width step.",
                "format": "uint32",
                "minimum": 0,
                "type": "integer"
              }
            },
            "required": [
              "kind",
              "min_width",
              "max_width",
              "step_width",
              "min_height",
              "max_height",
              "step_height"
            ],
            "type": "object"
          },
          {
            "description": "A size described with a `v4l2_frmsizetypes` value this build cannot interpret.\n\nD2's rule, applied to sizes: the entry exists, so it is carried. Dropping it would\nmake a *short* size list look like a complete one, and \"this camera does not offer\n4K\" is exactly the capability claim we would be inventing.",
            "properties": {
              "kind": {
                "const": "unknown",
                "type": "string"
              },
              "raw": {
                "description": "The kernel's `type` discriminant, preserved exactly.",
                "format": "uint32",
                "minimum": 0,
                "type": "integer"
              }
            },
            "required": [
              "kind",
              "raw"
            ],
            "type": "object"
          }
        ]
      },
      "FrameSizeInfo": {
        "description": "A frame size together with the intervals available *at that size*.",
        "properties": {
          "intervals": {
            "description": "The intervals offered at it.",
            "items": {
              "$ref": "#/components/schemas/FrameInterval"
            },
            "type": "array"
          },
          "size": {
            "$ref": "#/components/schemas/FrameSize",
            "description": "The size."
          }
        },
        "required": [
          "size",
          "intervals"
        ],
        "type": "object"
      },
      "HintKind": {
        "description": "Why a listing might not say what the user expected.\n\nD1: \"an empty enumeration is diagnosed, not shrugged at\". Hints are *data*, not\nprose the CLI prints and the daemon forgets — an agent reading `--json` gets the\nsame diagnosis a human reading the table does.",
        "oneOf": [
          {
            "const": "driverless_usb_video_device",
            "description": "A USB device presents a video-class interface and no driver has bound a V4L2\nnode to it — the skill's `lsusb` triage, built in [PF:14].",
            "type": "string"
          },
          {
            "const": "node_unreadable",
            "description": "A device node exists and could not be interrogated, so the camera it belongs\nto is not in the listing.\n\nThe alternative — listing the camera with only the nodes that answered —\nwould let a busy capture node read as \"this camera cannot capture\", which is\nthe availability-as-capability conversion E3 exists to prevent.",
            "type": "string"
          }
        ]
      },
      "Holder": {
        "description": "A process holding a device open, as diagnosed from `/proc/*/fd` — the `fuser`\nreplacement. Killing one is a separate explicit command, never a side effect\n(design §5, D10).",
        "properties": {
          "comm": {
            "description": "Its `comm`, when `/proc` let us read it.",
            "type": [
              "string",
              "null"
            ]
          },
          "pid": {
            "description": "The holding process.",
            "format": "int32",
            "type": "integer"
          }
        },
        "required": [
          "pid"
        ],
        "type": "object"
      },
      "HotplugEvent": {
        "description": "A camera appearing or disappearing.",
        "oneOf": [
          {
            "description": "A device node appeared. Re-enumeration decides what camera it belongs to —\nthe event names a node, never a camera, because grouping is not a node property.",
            "properties": {
              "kind": {
                "const": "added",
                "type": "string"
              },
              "path": {
                "description": "The node that appeared.",
                "type": "string"
              }
            },
            "required": [
              "kind",
              "path"
            ],
            "type": "object"
          },
          {
            "description": "A device node disappeared.",
            "properties": {
              "kind": {
                "const": "removed",
                "type": "string"
              },
              "path": {
                "description": "The node that vanished.",
                "type": "string"
              }
            },
            "required": [
              "kind",
              "path"
            ],
            "type": "object"
          }
        ]
      },
      "IntervalSource": {
        "description": "Where the frame interval in the finished file came from.\n\nThe distinction D7's CFR carve-out exists to preserve, carried out of the muxer so\nthe caller never has to guess whether the number it is reading was observed.",
        "oneOf": [
          {
            "const": "measured",
            "description": "The mean of the delivered frame timestamps — the close-time rewrite happened.\n\n**Either container**, since P6d. It was AVI's alone for two phases, because a Y4M\nheader is written before the first frame and its rate field was variable-width text;\nnote **N106** and its amendment record what the padded field cost and what measured\nit (evidence **E17**).",
            "type": "string"
          },
          {
            "const": "negotiated",
            "description": "What the camera was asked for: fewer than two frames arrived, or their timestamps\ndescribed no usable span, so nothing was measured and the header kept the number the\nnegotiation named.",
            "type": "string"
          },
          {
            "const": "provisional",
            "description": "Neither: the negotiation named no interval and none was measured, so the header\ncarries `imaging::video::PROVISIONAL_INTERVAL_US`.",
            "type": "string"
          }
        ]
      },
      "IntervalStats": {
        "description": "The interval distribution of one stream, in microseconds (design D16).\n\n**Two tiers, and the split is the bound.** `mean`, `min` and `max` are streaming moments\nand cover every interval the stream produced. The order statistics — `p50`, `p99` and the\njitter — are exact over the intervals the accumulator *retained*, which is every one of\nthem up to `limits::MAX_RECORDING_FRAMES` and the first `retained` of them after\nthat. `retained < observed` is how the answer states its own truncation, which is the\nonly honest way to bound a percentile: a stream longer than the recording cap is a stream\nthis tool has already declined to record whole.",
        "properties": {
          "jitter_us": {
            "description": "Mean absolute deviation from the mean, over the retained ones.\n\nMean absolute deviation rather than a standard deviation because this is integer\narithmetic end to end: a variance in microseconds squared overflows a `u64` at a\nfour-second interval and needs a square root to become a number anybody reads.",
            "format": "uint64",
            "minimum": 0,
            "type": "integer"
          },
          "max_us": {
            "description": "The longest, over every one observed.",
            "format": "uint64",
            "minimum": 0,
            "type": "integer"
          },
          "mean_us": {
            "description": "The mean interval, over every one observed.",
            "format": "uint64",
            "minimum": 0,
            "type": "integer"
          },
          "min_us": {
            "description": "The shortest, over every one observed.",
            "format": "uint64",
            "minimum": 0,
            "type": "integer"
          },
          "observed": {
            "description": "How many intervals the stream produced.",
            "format": "uint64",
            "minimum": 0,
            "type": "integer"
          },
          "p50_us": {
            "description": "The median, over the retained ones.",
            "format": "uint64",
            "minimum": 0,
            "type": "integer"
          },
          "p99_us": {
            "description": "The 99th percentile, over the retained ones — the number a stall shows up in.",
            "format": "uint64",
            "minimum": 0,
            "type": "integer"
          },
          "retained": {
            "description": "How many of them the accumulator kept exactly.",
            "format": "uint32",
            "minimum": 0,
            "type": "integer"
          }
        },
        "required": [
          "observed",
          "retained",
          "mean_us",
          "min_us",
          "max_us",
          "p50_us",
          "p99_us",
          "jitter_us"
        ],
        "type": "object"
      },
      "KnownFlag": {
        "description": "The control flag bits this version names.\n\nFlags are carried as raw bits *plus* this decoded set, because the set grows:\n`HasWhichMinMax` (0x1000) arrived with the same kernel work as the RECT support\nbehind PF:1 and older references do not list it [PF:12]. Bits outside this\nvocabulary survive in `ControlFlags::unknown_bits`.",
        "oneOf": [
          {
            "const": "disabled",
            "description": "`V4L2_CTRL_FLAG_DISABLED` — permanently unsupported on this device.",
            "type": "string"
          },
          {
            "const": "grabbed",
            "description": "`V4L2_CTRL_FLAG_GRABBED` — busy because streaming is in progress.",
            "type": "string"
          },
          {
            "const": "read_only",
            "description": "`V4L2_CTRL_FLAG_READ_ONLY` — the Chicony's `Privacy` control [PF:12].",
            "type": "string"
          },
          {
            "const": "update",
            "description": "`V4L2_CTRL_FLAG_UPDATE` — writing it changes other controls' properties.",
            "type": "string"
          },
          {
            "const": "inactive",
            "description": "`V4L2_CTRL_FLAG_INACTIVE` — an automation partner owns it right now [PF:3].",
            "type": "string"
          },
          {
            "const": "slider",
            "description": "`V4L2_CTRL_FLAG_SLIDER` — a UI hint.",
            "type": "string"
          },
          {
            "const": "write_only",
            "description": "`V4L2_CTRL_FLAG_WRITE_ONLY` — reading it is meaningless.",
            "type": "string"
          },
          {
            "const": "volatile",
            "description": "`V4L2_CTRL_FLAG_VOLATILE` — the value changes without us writing it.",
            "type": "string"
          },
          {
            "const": "has_payload",
            "description": "`V4L2_CTRL_FLAG_HAS_PAYLOAD` — the value is a compound payload.",
            "type": "string"
          },
          {
            "const": "execute_on_write",
            "description": "`V4L2_CTRL_FLAG_EXECUTE_ON_WRITE` — writing has an effect beyond the value.",
            "type": "string"
          },
          {
            "const": "modify_layout",
            "description": "`V4L2_CTRL_FLAG_MODIFY_LAYOUT` — writing it reshapes the format.",
            "type": "string"
          },
          {
            "const": "dynamic_array",
            "description": "`V4L2_CTRL_FLAG_DYNAMIC_ARRAY` — element count varies at runtime.",
            "type": "string"
          },
          {
            "const": "has_which_min_max",
            "description": "`V4L2_CTRL_FLAG_HAS_WHICH_MIN_MAX` — widely set on recent kernels [PF:12].",
            "type": "string"
          }
        ]
      },
      "ListHint": {
        "description": "One diagnosis attached to a listing.",
        "properties": {
          "kind": {
            "$ref": "#/components/schemas/HintKind",
            "description": "What kind of problem this is."
          },
          "subject": {
            "description": "What it is about — a USB device path such as `1-2`.",
            "type": "string"
          }
        },
        "required": [
          "kind",
          "subject"
        ],
        "type": "object"
      },
      "LockProtocol": {
        "description": "Which of D9's two locking protocols the holder of the state directory follows.\n\nBoth take the same advisory lock the same way; what differs is how long it is held,\nand that difference is the whole of what a refused caller needs. A lock held for a\nprocess's lifetime will not be free in a moment, so retrying is pointless and the\nanswer is another program; a lock held for one operation will be free shortly, so\nretrying is the whole answer.\n\n**Here rather than in `webcam-handler-engine::store`, where the locking lives**,\nbecause `Error::StoreLocked` carries it: a refusal that cannot say which protocol\nholds the lock cannot tell the caller which of those two answers applies, and\n`webcam-handler-schema` cannot depend on the engine. The engine re-exports this\ntype, so the locking code still names one definition (design §2.10) — see note N40.",
        "oneOf": [
          {
            "const": "held_for_lifetime",
            "description": "The daemon's: taken once at startup and held until the process exits, because\nthe daemon owns the state directory for as long as it is running.",
            "type": "string"
          },
          {
            "const": "per_operation",
            "description": "A daemonless `webcam-handler-cli`'s: taken for one mutating operation and released\nat its end, because a CLI that held it between invocations would lock out the\ndaemon it does not know about.",
            "type": "string"
          }
        ]
      },
      "LogEntry": {
        "description": "One line of `log.ndjson`.",
        "oneOf": [
          {
            "description": "The session was created.",
            "properties": {
              "event": {
                "const": "started",
                "type": "string"
              },
              "goal": {
                "description": "The goal recorded at creation.",
                "type": "string"
              }
            },
            "required": [
              "event",
              "goal"
            ],
            "type": "object"
          },
          {
            "description": "The pre-sweep snapshot was persisted.",
            "properties": {
              "controls": {
                "description": "How many controls it holds.",
                "format": "uint",
                "minimum": 0,
                "type": "integer"
              },
              "event": {
                "const": "snapshot_taken",
                "type": "string"
              }
            },
            "required": [
              "event",
              "controls"
            ],
            "type": "object"
          },
          {
            "description": "The automation pairs were probed empirically (design D3 layer 2, PF:3).\n\nA log line because the probe is a *write*: it toggles automation controls and puts\nthem back, so a session's history that omitted it would omit the first thing that\never moved the camera. `skipped` is on the record for the reason the probe reports\nit at all — a probe silent about what it passed over reads as a probe that found\nnothing there.",
            "properties": {
              "event": {
                "const": "pairs_discovered",
                "type": "string"
              },
              "measured": {
                "description": "How many pairs this device demonstrated.",
                "format": "uint",
                "minimum": 0,
                "type": "integer"
              },
              "skipped": {
                "description": "How many automation-shaped controls the probe declined to toggle.",
                "format": "uint",
                "minimum": 0,
                "type": "integer"
              }
            },
            "required": [
              "event",
              "measured",
              "skipped"
            ],
            "type": "object"
          },
          {
            "description": "An automation control was switched off.",
            "properties": {
              "automation": {
                "$ref": "#/components/schemas/ControlSlug",
                "description": "The automation control switched off."
              },
              "event": {
                "const": "automation_disabled",
                "type": "string"
              },
              "manual": {
                "$ref": "#/components/schemas/ControlSlug",
                "description": "The manual control this was for."
              }
            },
            "required": [
              "event",
              "manual",
              "automation"
            ],
            "type": "object"
          },
          {
            "description": "A sweep began.",
            "properties": {
              "adjustments": {
                "description": "Every way the planner's values differ from the spec, in the order it made them.\n\nA history reader's copy of what the `sweep_started` progress event carried. An\nagent that was not watching — or that came back to a session an hour later — has\nnowhere else to learn that the sweep it is about to compare 251 photographs from\nwas strided to fit a cap.",
                "items": {
                  "$ref": "#/components/schemas/SweepAdjustment"
                },
                "type": "array"
              },
              "control": {
                "$ref": "#/components/schemas/ControlSlug",
                "description": "Which control."
              },
              "event": {
                "const": "sweep_started",
                "type": "string"
              },
              "precision": {
                "default": 0,
                "description": "The smallest gap between the values it will visit — the stride the planner\narrived at, which is not always the one the caller asked for (note **N145**).\n\nOn the durable line as well as on the live event, because this is the number that\nsays what every photograph in the session directory is worth, and the terminal\nthat showed the live event is gone by the time anybody reads `calibrate status`.\nZero on a line an older build wrote.",
                "format": "int64",
                "type": "integer"
              },
              "total": {
                "description": "How many samples are planned.",
                "format": "uint32",
                "minimum": 0,
                "type": "integer"
              }
            },
            "required": [
              "event",
              "control",
              "total"
            ],
            "type": "object"
          },
          {
            "description": "A sample was recorded.",
            "properties": {
              "applied": {
                "description": "The applied value.",
                "format": "int64",
                "type": "integer"
              },
              "control": {
                "$ref": "#/components/schemas/ControlSlug",
                "description": "Which control."
              },
              "event": {
                "const": "sample_taken",
                "type": "string"
              },
              "requested": {
                "description": "The requested value.",
                "format": "int64",
                "type": "integer"
              }
            },
            "required": [
              "event",
              "control",
              "requested",
              "applied"
            ],
            "type": "object"
          },
          {
            "description": "A sweep finished.",
            "properties": {
              "control": {
                "$ref": "#/components/schemas/ControlSlug",
                "description": "Which control."
              },
              "event": {
                "const": "sweep_finished",
                "type": "string"
              },
              "samples": {
                "description": "How many samples were actually taken.",
                "format": "uint32",
                "minimum": 0,
                "type": "integer"
              }
            },
            "required": [
              "event",
              "control",
              "samples"
            ],
            "type": "object"
          },
          {
            "description": "A sweep stopped before its plan did, and why.\n\nThe one event here whose subject is a *refusal*, and it earns its place by what a\nsession directory looks like without it. The recorded `SampleTaken` lines bound\n**when** a sweep stopped — three samples of sixteen and then nothing — and say\nnothing at all about **why**: a camera that was unplugged, a sensor that never\nsettled [PF:11], and a full disk leave byte-identical histories, and design's own\nrule that availability is not capability is exactly the distinction that would be\nlost. The live `sweep_interrupted` progress event carries the same fact to whoever\nwas watching; this is the copy that survives the process,\nbecause `calibrate status` is read after the terminal is gone.\n\nThe absence of a *line* is not evidence of anything: a process that was killed\nleaves none (design §6's crash story is the pre-sweep snapshot, not the log). What\nthe line says is that the sweep stopped; whether anybody knows **why** is the\n`failure` field's question, and since note **N149** that field answers it honestly\nin both directions rather than by always naming something.",
            "properties": {
              "control": {
                "$ref": "#/components/schemas/ControlSlug",
                "description": "Which control."
              },
              "detail": {
                "description": "What happened, in words. Always present — a line whose `failure` is absent still\nsays what this process knows, which is what it repaired and why it could not name\na cause.",
                "type": "string"
              },
              "event": {
                "const": "sweep_interrupted",
                "type": "string"
              },
              "failure": {
                "anyOf": [
                  {
                    "$ref": "#/components/schemas/ErrorKind"
                  },
                  {
                    "type": "null"
                  }
                ],
                "description": "The refusal's discriminant, so a reader can branch without parsing prose — and\n**absent when the reason is not known to whoever wrote the line** (note\n**N149**).\n\nTwo producers write this event and they know different things.\n`engine::calibrate`'s interruption path is inside the sweep that stopped, so it\nrecords the refusal that stopped it — `DeviceGone`, `SettleTimeout`, `StorageIo`.\n`engine::lifecycle::free_stranded_sweeps` runs in a *later* process, repairing a\ncontrol a killed sweep left behind, and it has nothing to read: the process that\nknew died without writing. It leaves this absent, and `detail` carries the story.\n\nThe rejected alternative was to name the refusal the *next* verb met\n(`IllegalTransition`, from `may_begin_sweep`). That is a real refusal, but it is a\nrefusal about a different call at a different time, and D13's discriminant is what\nan unattended agent dispatches on — `docs/agent-guide.md` answers\n`illegal_transition` with \"fix the request\", and there is nothing to fix: the\nprocess was killed and the control has already been given back. One field with two\nmeanings costs more than one field that can be absent."
              },
              "taken": {
                "description": "How many samples were recorded before it stopped.",
                "format": "uint32",
                "minimum": 0,
                "type": "integer"
              },
              "total": {
                "description": "How many the plan held.",
                "format": "uint32",
                "minimum": 0,
                "type": "integer"
              }
            },
            "required": [
              "event",
              "control",
              "taken",
              "total",
              "detail"
            ],
            "type": "object"
          },
          {
            "description": "A value was chosen.",
            "properties": {
              "control": {
                "$ref": "#/components/schemas/ControlSlug",
                "description": "Which control."
              },
              "event": {
                "const": "selected",
                "type": "string"
              },
              "selector": {
                "$ref": "#/components/schemas/Selector",
                "description": "Who chose it."
              },
              "value": {
                "description": "The chosen value.",
                "format": "int64",
                "type": "integer"
              }
            },
            "required": [
              "event",
              "control",
              "value",
              "selector"
            ],
            "type": "object"
          },
          {
            "description": "The session's values were applied to a camera.",
            "properties": {
              "controls": {
                "description": "How many controls were written.",
                "format": "uint",
                "minimum": 0,
                "type": "integer"
              },
              "event": {
                "const": "applied",
                "type": "string"
              }
            },
            "required": [
              "event",
              "controls"
            ],
            "type": "object"
          },
          {
            "description": "The camera was put back as found.",
            "properties": {
              "event": {
                "const": "restored",
                "type": "string"
              },
              "restored": {
                "description": "How many controls came back exactly.",
                "format": "uint",
                "minimum": 0,
                "type": "integer"
              },
              "unrestored": {
                "description": "How many did not.",
                "format": "uint",
                "minimum": 0,
                "type": "integer"
              }
            },
            "required": [
              "event",
              "restored",
              "unrestored"
            ],
            "type": "object"
          }
        ],
        "properties": {
          "at": {
            "$ref": "#/components/schemas/Stamp",
            "description": "When it happened."
          }
        },
        "required": [
          "at"
        ],
        "type": "object"
      },
      "MenuItem": {
        "description": "One menu item. Menus are sparse: `VIDIOC_QUERYMENU` returns `EINVAL` on the holes,\nand the holes are real [PF:2].",
        "oneOf": [
          {
            "description": "A named item (`V4L2_CTRL_TYPE_MENU`), e.g. `\"Manual Mode\"`.",
            "properties": {
              "kind": {
                "const": "name",
                "type": "string"
              },
              "name": {
                "description": "The kernel's name for this index.",
                "type": "string"
              }
            },
            "required": [
              "kind",
              "name"
            ],
            "type": "object"
          },
          {
            "description": "An integer item (`V4L2_CTRL_TYPE_INTEGER_MENU`).",
            "properties": {
              "kind": {
                "const": "value",
                "type": "string"
              },
              "value": {
                "description": "The kernel's value for this index.",
                "format": "int64",
                "type": "integer"
              }
            },
            "required": [
              "kind",
              "value"
            ],
            "type": "object"
          }
        ]
      },
      "MetricName": {
        "description": "A built-in image quality metric.",
        "oneOf": [
          {
            "const": "sharpness",
            "description": "Variance of the Laplacian: higher is sharper.",
            "type": "string"
          },
          {
            "const": "clipped_highlights",
            "description": "Fraction of pixels at or near full white. Higher means more blown highlights.",
            "type": "string"
          },
          {
            "const": "clipped_shadows",
            "description": "Fraction of pixels at or near full black.",
            "type": "string"
          },
          {
            "const": "mean_luma",
            "description": "Mean luma, 0.0–1.0. Neither high nor low is \"better\" — it is context.",
            "type": "string"
          },
          {
            "const": "rms_contrast",
            "description": "Root-mean-square contrast.",
            "type": "string"
          }
        ]
      },
      "NegotiatedStream": {
        "description": "What the device actually agreed to.",
        "properties": {
          "adjustments": {
            "description": "Every way this differs from what was asked. Empty means the request was honored\nexactly — and an empty list is a claim, so it is reported rather than assumed.",
            "items": {
              "$ref": "#/components/schemas/Adjustment"
            },
            "type": "array"
          },
          "bytes_per_line": {
            "description": "Bytes per row, as the driver reports it (0 for compressed formats).",
            "format": "uint32",
            "minimum": 0,
            "type": "integer"
          },
          "height": {
            "description": "Frame height.",
            "format": "uint32",
            "minimum": 0,
            "type": "integer"
          },
          "interval": {
            "$ref": "#/components/schemas/FrameInterval",
            "description": "The negotiated frame interval."
          },
          "pixel_format": {
            "$ref": "#/components/schemas/PixelFormat",
            "description": "The format frames will arrive in."
          },
          "size_image": {
            "description": "The driver's maximum frame size in bytes.",
            "format": "uint32",
            "minimum": 0,
            "type": "integer"
          },
          "width": {
            "description": "Frame width.",
            "format": "uint32",
            "minimum": 0,
            "type": "integer"
          }
        },
        "required": [
          "pixel_format",
          "width",
          "height",
          "bytes_per_line",
          "size_image",
          "interval"
        ],
        "type": "object"
      },
      "NodeKind": {
        "description": "What a device node is for, decided by `device_caps` [PF:7].",
        "oneOf": [
          {
            "description": "`V4L2_CAP_VIDEO_CAPTURE` — frames come from here.",
            "properties": {
              "kind": {
                "const": "video_capture",
                "type": "string"
              }
            },
            "required": [
              "kind"
            ],
            "type": "object"
          },
          {
            "description": "`V4L2_CAP_META_CAPTURE` — recorded, never streamed.",
            "properties": {
              "kind": {
                "const": "meta_capture",
                "type": "string"
              }
            },
            "required": [
              "kind"
            ],
            "type": "object"
          },
          {
            "description": "`V4L2_CAP_VIDEO_OUTPUT` — an output node on the same interface.",
            "properties": {
              "kind": {
                "const": "video_output",
                "type": "string"
              }
            },
            "required": [
              "kind"
            ],
            "type": "object"
          },
          {
            "description": "Something else. Represented, not discarded.",
            "properties": {
              "device_caps": {
                "description": "The `device_caps` word that produced this classification.",
                "format": "uint32",
                "minimum": 0,
                "type": "integer"
              },
              "kind": {
                "const": "other",
                "type": "string"
              }
            },
            "required": [
              "kind",
              "device_caps"
            ],
            "type": "object"
          }
        ]
      },
      "Occupation": {
        "description": "What the process that refused is itself doing with the camera's node.\n\n`Error::Busy`'s `holders` answers *who has it* by walking `/proc`, and there is one\nholder it deliberately never names: **this process**. A daemon that refuses its own\ncaller knows precisely what it is doing and will not hand over its own pid, because a\npid in a refusal is an invitation to signal it (`daemon::record`'s header). What was\nmissing is the other half of that decision — *what for* — without which the refusal\nread as \"somebody unidentified has it\" and sent an unattended caller looking for a\nprocess to kill (docs/11 **M19**, note **N217**).\n\n**Every in-process producer in the workspace has an alternative here**, and that is\nthe property rather than a tidiness. The moment one of them goes through\n`Error::busy` instead, *\"held by an unidentified process\"* is back — about the\nprogram the caller is talking to — and the guide's row telling a reader that an absent\n`this_process` means another program is false with it. Three producers shipped that\nway inside this repair and note **N221** is the entry for them.\n\nEvery alternative says what ends it, because that is what makes `Busy`'s *retry*\ndisposition true: the caller is told what to wait for, not merely to wait. None of them\nnames a verb to poll with, and that is measured rather than terse — the surface this\nrefusal reaches most often has none to offer (note **N220**).",
        "oneOf": [
          {
            "const": "recording",
            "description": "A recording this process is running for a caller.\n\nBounded by the take's own duration (`limits::MAX_RECORDING_MS` at the outside), so\nretrying is the action that succeeds.",
            "type": "string"
          },
          {
            "const": "starting_recording",
            "description": "A `record_start` this process is still negotiating.\n\nThe slot is claimed and the stream is not up yet. It resolves either way in the\ntime one `VIDIOC_STREAMON` takes, which is why it is told apart from\n`Occupation::Recording`: what a caller waits for is different, and so is how\nlong.",
            "type": "string"
          },
          {
            "const": "running_commands",
            "description": "This process's own commands for the camera, queued to the depth it allows.\n\n`engine::actor` gives each camera one thread and a bounded inbox\n(`limits::CAMERA_COMMAND_QUEUE_DEPTH`), so a full one is this process's work in the\nway and never a device that is unavailable (E3). It is the alternative a caller can\ndo something about beyond waiting: D12's `wait` spends\n`limits::CAMERA_ENQUEUE_WAIT_MS` on room in exactly this queue, so it helps here\nand with no other alternative.",
            "type": "string"
          },
          {
            "const": "streaming",
            "description": "A stream this process already has open on the node.\n\nOne streamer per node is the kernel's rule and this build's, so a second\n`start_stream` on a handle that is already streaming is refused before `S_FMT`\nwould say `EBUSY` a moment later and tear down buffers the caller is still\ndequeuing from (note **N191**). It ends when the stream that is up ends.",
            "type": "string"
          },
          {
            "const": "streaming_preview",
            "description": "A preview this process is already serving to every viewer it will.\n\nThe node is streaming and what refuses is not the node: it is a count,\n`limits::PREVIEW_MAX_VIEWERS_PER_CAMERA`. Its own alternative because it is the one\na second browser tab meets, and because what ends it is a viewer leaving rather\nthan a stream stopping.",
            "type": "string"
          }
        ]
      },
      "PhotoDelivery": {
        "description": "Where a photo's bytes ended up — `Sink`, answered.\n\nThe two variants pair with the two sinks, and each says the thing its caller cannot\notherwise learn: a path answer reports how much was written, and a bytes answer reports how\nmuch is on its way. **The bytes themselves are not in this document**: `webcam-handler-cli`\nstreams them to standard output, and D10's base64-in-JSON encoding lives with the wire\nsurface that needs it — `webcam-handler-api`'s `photo::Base64Bytes`, beside the report\nrather than inside this enum. Carrying an unused encoding here would be a dependency nobody\nreads, and it would put base64 in every session file and `--json` document too.",
        "oneOf": [
          {
            "description": "The bytes were handed back to the caller.",
            "properties": {
              "byte_count": {
                "description": "How many there are.",
                "format": "uint64",
                "minimum": 0,
                "type": "integer"
              },
              "format": {
                "$ref": "#/components/schemas/PhotoFormat",
                "description": "The encoding they are in."
              },
              "kind": {
                "const": "bytes",
                "type": "string"
              }
            },
            "required": [
              "kind",
              "format",
              "byte_count"
            ],
            "type": "object"
          },
          {
            "description": "The bytes were written to a file.",
            "properties": {
              "byte_count": {
                "description": "How many bytes it holds.",
                "format": "uint64",
                "minimum": 0,
                "type": "integer"
              },
              "kind": {
                "const": "path",
                "type": "string"
              },
              "path": {
                "description": "Where.",
                "type": "string"
              }
            },
            "required": [
              "kind",
              "path",
              "byte_count"
            ],
            "type": "object"
          }
        ]
      },
      "PhotoFormat": {
        "description": "The encoding a photo lands in.",
        "oneOf": [
          {
            "const": "jpeg",
            "description": "JPEG. Verbatim camera bytes when the stream is already MJPG (E6), else encoded.",
            "type": "string"
          },
          {
            "const": "png",
            "description": "PNG. Always encoded, always lossless.",
            "type": "string"
          },
          {
            "const": "ppm",
            "description": "Binary PPM/PGM — the \"give me pixels\" escape hatch for tooling.",
            "type": "string"
          }
        ]
      },
      "PhotoRendering": {
        "description": "What the photo pipeline did to produce a photo's bytes (design D6/E6).\n\nReported alongside every photo because \"the camera's own bitstream\" and \"a faithful\nre-encode\" are different products, and a calibration sample that was re-encoded is\npartly ranking our codec rather than the lens.",
        "oneOf": [
          {
            "description": "The camera's own bitstream, byte for byte (E6).",
            "properties": {
              "kind": {
                "const": "verbatim",
                "type": "string"
              },
              "source": {
                "$ref": "#/components/schemas/PixelFormat",
                "description": "The format the camera delivered, which is the format on disk."
              }
            },
            "required": [
              "kind",
              "source"
            ],
            "type": "object"
          },
          {
            "description": "A compressed frame was decoded and encoded again.",
            "properties": {
              "kind": {
                "const": "decoded_and_encoded",
                "type": "string"
              },
              "source": {
                "$ref": "#/components/schemas/PixelFormat",
                "description": "What the camera delivered."
              },
              "target": {
                "$ref": "#/components/schemas/PhotoFormat",
                "description": "What was written."
              }
            },
            "required": [
              "kind",
              "source",
              "target"
            ],
            "type": "object"
          },
          {
            "description": "A raw frame was converted to pixels and encoded.",
            "properties": {
              "kind": {
                "const": "converted_and_encoded",
                "type": "string"
              },
              "source": {
                "$ref": "#/components/schemas/PixelFormat",
                "description": "What the camera delivered."
              },
              "target": {
                "$ref": "#/components/schemas/PhotoFormat",
                "description": "What was written."
              }
            },
            "required": [
              "kind",
              "source",
              "target"
            ],
            "type": "object"
          }
        ]
      },
      "PhotoReport": {
        "description": "What `photo` answers (design D6).\n\nSelf-describing on purpose: a saved `--json` document says which camera, when, what the\ndevice actually agreed to stream, and whether the bytes are the camera's own — the four\nthings a calibration sample or a bug report needs and cannot reconstruct later.",
        "properties": {
          "camera": {
            "$ref": "#/components/schemas/CameraId",
            "description": "Which camera took it."
          },
          "delivery": {
            "$ref": "#/components/schemas/PhotoDelivery",
            "description": "Where the bytes went."
          },
          "frames_settled": {
            "description": "How many frames were discarded before this one [PF:11].",
            "format": "uint32",
            "minimum": 0,
            "type": "integer"
          },
          "height": {
            "description": "The height of the same.",
            "format": "uint32",
            "minimum": 0,
            "type": "integer"
          },
          "negotiated": {
            "$ref": "#/components/schemas/NegotiatedStream",
            "description": "What the device agreed to, including every way that differs from the request (D5)."
          },
          "rendering": {
            "$ref": "#/components/schemas/PhotoRendering",
            "description": "Which of D6's three paths produced the bytes."
          },
          "taken_at": {
            "$ref": "#/components/schemas/Stamp",
            "description": "When."
          },
          "transform": {
            "$ref": "#/components/schemas/TransformApplication",
            "description": "Where the orientation request went."
          },
          "width": {
            "description": "The width of the image the bytes encode, after any pixel-domain transform.",
            "format": "uint32",
            "minimum": 0,
            "type": "integer"
          }
        },
        "required": [
          "camera",
          "taken_at",
          "negotiated",
          "rendering",
          "transform",
          "width",
          "height",
          "frames_settled",
          "delivery"
        ],
        "type": "object"
      },
      "PhotoRequest": {
        "description": "Everything one photo needs (design D5, D6, D10).\n\nAssembled by the caller so `webcam-handler-cli photo`, the daemon's `photo` method and a\ncalibration sample all ask for a photo the same way — the sweep at P3 varies the control\nvalues between shots and nothing else.",
        "properties": {
          "settle": {
            "$ref": "#/components/schemas/SettlePolicy",
            "default": {
              "deadline_ms": 5000,
              "spec": {
                "frames": 10,
                "kind": "skip_frames"
              }
            },
            "description": "How long to let the sensor settle first [PF:11]."
          },
          "sink": {
            "$ref": "#/components/schemas/Sink",
            "description": "Where the bytes go."
          },
          "stream": {
            "$ref": "#/components/schemas/StreamRequest",
            "default": {
              "buffer_count": 4
            },
            "description": "What to ask the device's format negotiation for."
          },
          "transform": {
            "$ref": "#/components/schemas/Transform",
            "default": "none",
            "description": "The orientation, applied to pixels or recorded in EXIF depending on the sink (E6)."
          },
          "wait": {
            "default": false,
            "description": "Whether a request that finds the camera's command queue full waits for its turn.\n\nD12's flag: *\"a second capture request queues or is refused with `Busy` per its\n`wait` flag\"*. `false` — the default, and the shape every client sent before this\nfield existed — is `Error::Busy` the moment\n`limits::CAMERA_COMMAND_QUEUE_DEPTH` commands are already waiting. `true`\nwaits for room, bounded by `limits::CAMERA_ENQUEUE_WAIT_MS`, and takes the\n*same* refusal when that budget is spent: the flag changes when the answer arrives,\nnever what it is.\n\n`#[serde(default)]` like its three siblings, so a request written before this field\nexisted still parses and still means what it meant. `false` rather than `true` for\nthe same reason: it is the behaviour every caller has already met, and a default that\nsilently turned a prompt refusal into ten seconds of latency would be a change to\nrequests nobody rewrote.\n\nIt is on the *request* rather than beside it because the T4 executor surface takes\none `&PhotoRequest` and the T5 method takes one assembled request (design §2.10), so\na value here reaches every caller without a second parameter on three signatures.\nThe flag is meaningful only where something else can be holding the camera's one\nthread, which today is the daemon; note **N42** records why it has no command-line\nspelling yet.",
            "type": "boolean"
          }
        },
        "required": [
          "sink"
        ],
        "type": "object"
      },
      "PhotoResponse": {
        "description": "What `wch_photo` answers: the report every caller gets, and the bytes a `ReturnBytes`\nsink asked for (design D6, D10).\n\nThe bytes ride *beside* the report rather than inside\n`PhotoDelivery::Bytes` because `PhotoDelivery` is a schema type that a session file\nand a `--json` document also carry, and putting an encoding there would put base64 in\nall three. One JSON-RPC response is one document, so the two travel together here.\n\nThe whole `PhotoReport` survives the wire, untrimmed: `negotiated`, `rendering`,\n`transform` and `frames_settled` are how a caller learns that the device did not agree\nto what was asked, and a wire answer that dropped them would be a layer collapsing\n`{requested, applied}` into one value (rubric A10).\n\n**Size.** A 4K MJPG frame is roughly 8 MB, which is about 10.7 MB of base64 — past\njsonrpsee's default 10 MB response ceiling. Configuring that ceiling is the daemon's\njob (P4b) and `Sink::ServerPath` is the sink that does not pay the encoding at all;\nthis is recorded here so the limit is met in a design note rather than in the field.",
        "properties": {
          "bytes": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/Base64Bytes"
              },
              {
                "type": "null"
              }
            ],
            "description": "The bytes, for a `Sink::ReturnBytes` request; absent for `Sink::ServerPath`.\n\n`Option` rather than an empty vector, because \"the caller asked for a file\" and\n\"the caller asked for bytes and got none\" are different answers, and\n`PhotoResponse::bytes_match_the_delivery` is the predicate that tells them apart,\ncalled at both ends of the socket since P4f (see that method's own note)."
          },
          "report": {
            "$ref": "#/components/schemas/PhotoReport",
            "description": "What was taken, where it went, and what was done to it."
          }
        },
        "required": [
          "report"
        ],
        "type": "object"
      },
      "PixelFormat": {
        "description": "A pixel format, as the four-character FourCC the driver enumerated: `MJPG`, `YUYV`, `GREY`, `NV12`, `JPEG`, or one this build has never heard of, which is carried rather than rejected. A byte that is not an ASCII graphic character is escaped as `\\xNN` with two lowercase hex digits, and a literal backslash as `\\\\`, so a FourCC that is not printable crosses the wire exactly and decodes back to the same four bytes.",
        "maxLength": 16,
        "minLength": 4,
        "type": "string"
      },
      "ProbeSkip": {
        "description": "One control the empirical probe declined to toggle, and why.\n\nA named pair rather than a tuple for the reason\n`control::ControlWrite` is one — it crosses the T5 wire inside\n`report::DiscoveryReport` (D10), where an array of two strings is a shape a\nconsumer has to be told about out of band.\n\nThe `reason` is free text on purpose. A closed vocabulary here would turn a reason\nnobody anticipated into the nearest one that was, and the probe's list of what it\npassed over is exactly the place where \"we did not look\" must not be readable as \"we\nlooked and found nothing\" — §5's motor rule is one reason and a device that refused\nthe toggle is another, and both are discovered rather than designed.",
        "properties": {
          "control": {
            "$ref": "#/components/schemas/ControlSlug",
            "description": "Which control."
          },
          "reason": {
            "description": "Why, in the probe's own words.",
            "type": "string"
          }
        },
        "required": [
          "control",
          "reason"
        ],
        "type": "object"
      },
      "ProfileInvariant": {
        "description": "The part of a profile that should not change unless the device or the kernel does.",
        "properties": {
          "controls": {
            "description": "The full control set, with `current` cleared and the volatile flag bits masked\nout — see `invariant_control`.",
            "items": {
              "$ref": "#/components/schemas/ControlDesc"
            },
            "type": "array"
          },
          "formats": {
            "description": "Formats, with sizes and intervals nested [PF:9].",
            "items": {
              "$ref": "#/components/schemas/FormatInfo"
            },
            "type": "array"
          },
          "info": {
            "$ref": "#/components/schemas/CameraInfo",
            "description": "Identity, nodes, grouping."
          },
          "measured_pairs": {
            "description": "Automation pairs discovered by probing this device [PF:3].",
            "items": {
              "$ref": "#/components/schemas/AutomationPair"
            },
            "type": "array"
          }
        },
        "required": [
          "info",
          "formats",
          "controls"
        ],
        "type": "object"
      },
      "ProfileProvenance": {
        "description": "Where a profile came from. Never compared — a re-capture has a new timestamp by\ndefinition.",
        "properties": {
          "backend": {
            "$ref": "#/components/schemas/BackendKind",
            "description": "Which backend produced it. A profile captured from the fake backend would be\ncircular corpus, so this field is what makes that visible."
          },
          "captured_at": {
            "$ref": "#/components/schemas/Stamp",
            "description": "When it was captured."
          },
          "capturer": {
            "description": "Who or what ran the capture.",
            "type": "string"
          },
          "kernel": {
            "description": "`uname -r` of the capturing host.",
            "type": "string"
          },
          "tool_version": {
            "description": "The tool version that captured it.",
            "type": "string"
          }
        },
        "required": [
          "captured_at",
          "kernel",
          "tool_version",
          "capturer",
          "backend"
        ],
        "type": "object"
      },
      "ProfileState": {
        "description": "The part that changes with use.",
        "properties": {
          "flags": {
            "additionalProperties": {
              "format": "uint32",
              "minimum": 0,
              "type": "integer"
            },
            "default": {},
            "description": "Each control's raw flag word at capture time. The INACTIVE bit here tracks which\nautomation was on when the profile was taken [PF:3].",
            "type": "object"
          },
          "values": {
            "additionalProperties": {
              "$ref": "#/components/schemas/ControlValue"
            },
            "default": {},
            "description": "Each control's value at capture time — including the ones outside their declared\nrange [PF:4], which is exactly why this is recorded.",
            "type": "object"
          }
        },
        "type": "object"
      },
      "ProgressEvent": {
        "description": "One progress event, addressed to a session and stamped.\n\nThe session id is on every event rather than established once at subscription time,\nbecause P4e's subscription is per *client* and a client may watch a daemon running\nmore than one session. A stream whose events had to be correlated by arrival order\nwould be a stream that could not be multiplexed.",
        "oneOf": [
          {
            "description": "A sweep began, and this is how big it is.",
            "properties": {
              "adjustments": {
                "description": "Every way the planner's values differ from what `plan` literally asked for\n(note **N145**).\n\nD3's `{requested, applied}` doctrine, one layer up, and it rides on *this* event\nbecause this is the one a subscriber reads before any sample exists. Empty means\nthe spec was executed as written, and the encoding says that by omitting the\nfield: `skip_serializing_if` makes \"no adjustments\" and \"no such key\" one\ndocument, exactly as `NegotiatedStream::adjustments` does. What is *not* left to\ninference is the durable half — `session::SessionEvent::SweepStarted` carries the\nsame list, so a reader who was not subscribed still learns what the planner did.",
                "items": {
                  "$ref": "#/components/schemas/SweepAdjustment"
                },
                "type": "array"
              },
              "control": {
                "$ref": "#/components/schemas/ControlSlug",
                "description": "Which control."
              },
              "plan": {
                "$ref": "#/components/schemas/SweepSpec",
                "description": "The plan it is executing."
              },
              "precision": {
                "default": 0,
                "description": "The smallest gap between the values it will visit — the stride the planner\narrived at (note **N145**).\n\nThe number a caller compares against the `--precision` they typed, and the one a\nrefinement pass divides down. A sweep whose stride was widened forty-fold to fit a\ncap announced a sample count and nothing else until this field existed.",
                "format": "int64",
                "type": "integer"
              },
              "progress": {
                "const": "sweep_started",
                "type": "string"
              },
              "total": {
                "description": "How many samples it will take.",
                "format": "uint32",
                "minimum": 0,
                "type": "integer"
              }
            },
            "required": [
              "progress",
              "control",
              "plan",
              "total"
            ],
            "type": "object"
          },
          {
            "description": "The camera has been moved to the next value; the sensor is settling.\n\nEmitted before the capture rather than after it because the capture is the slow\npart: a bar that only advances on `CalibrationProgress::SampleTaken` sits still\nfor the seconds that actually pass. It carries `{requested, applied}` for the same\nreason every other layer does — the driver may have taken something else [PF:6],\nand a viewer watching a sweep should see that when it happens, not when the\nsession file is read afterwards.",
            "properties": {
              "applied": {
                "description": "The value the device actually holds.",
                "format": "int64",
                "type": "integer"
              },
              "control": {
                "$ref": "#/components/schemas/ControlSlug",
                "description": "Which control."
              },
              "index": {
                "description": "Which sample this is, counting from one.",
                "format": "uint32",
                "minimum": 0,
                "type": "integer"
              },
              "progress": {
                "const": "value_set",
                "type": "string"
              },
              "requested": {
                "description": "The value the sweep asked for.",
                "format": "int64",
                "type": "integer"
              },
              "total": {
                "description": "How many the sweep will take.",
                "format": "uint32",
                "minimum": 0,
                "type": "integer"
              },
              "warnings": {
                "description": "Anything notable about the write.",
                "items": {
                  "$ref": "#/components/schemas/WriteWarning"
                },
                "type": "array"
              }
            },
            "required": [
              "progress",
              "control",
              "index",
              "total",
              "requested",
              "applied"
            ],
            "type": "object"
          },
          {
            "description": "A photo was taken and scored.",
            "properties": {
              "applied": {
                "description": "The value the device held when the shutter fell [PF:6].",
                "format": "int64",
                "type": "integer"
              },
              "control": {
                "$ref": "#/components/schemas/ControlSlug",
                "description": "Which control."
              },
              "index": {
                "description": "Which sample this is, counting from one.",
                "format": "uint32",
                "minimum": 0,
                "type": "integer"
              },
              "metrics": {
                "additionalProperties": {
                  "format": "double",
                  "type": "number"
                },
                "description": "What the metrics made of it. Metrics *rank*; they do not decide (D8).",
                "type": "object"
              },
              "photo": {
                "description": "The photo, relative to the session directory (D9).",
                "type": "string"
              },
              "progress": {
                "const": "sample_taken",
                "type": "string"
              },
              "requested": {
                "description": "The value the sweep asked for — which is what the photo is named after.",
                "format": "int64",
                "type": "integer"
              },
              "total": {
                "description": "How many the sweep will take.",
                "format": "uint32",
                "minimum": 0,
                "type": "integer"
              }
            },
            "required": [
              "progress",
              "control",
              "index",
              "total",
              "requested",
              "applied",
              "photo"
            ],
            "type": "object"
          },
          {
            "description": "Every planned sample was taken.\n\nNote what it does **not** say: nothing was selected. A sweep produces a ranking and\nstops there, and the `Selected` fact belongs to whoever chose (D8).",
            "properties": {
              "control": {
                "$ref": "#/components/schemas/ControlSlug",
                "description": "Which control."
              },
              "progress": {
                "const": "sweep_finished",
                "type": "string"
              },
              "samples": {
                "description": "How many samples it took.",
                "format": "uint32",
                "minimum": 0,
                "type": "integer"
              }
            },
            "required": [
              "progress",
              "control",
              "samples"
            ],
            "type": "object"
          },
          {
            "description": "The sweep stopped before its plan did.\n\nThe samples already recorded stand — they happened — so `taken` is the count on\ndisk and the control is left mid-sweep rather than forced into a terminal state.",
            "properties": {
              "control": {
                "$ref": "#/components/schemas/ControlSlug",
                "description": "Which control."
              },
              "detail": {
                "description": "The refusal, rendered.",
                "type": "string"
              },
              "failure": {
                "$ref": "#/components/schemas/ErrorKind",
                "description": "The refusal's discriminant, so a consumer can branch without parsing prose."
              },
              "progress": {
                "const": "sweep_interrupted",
                "type": "string"
              },
              "taken": {
                "description": "How many samples were recorded before it stopped.",
                "format": "uint32",
                "minimum": 0,
                "type": "integer"
              },
              "total": {
                "description": "How many the plan held.",
                "format": "uint32",
                "minimum": 0,
                "type": "integer"
              }
            },
            "required": [
              "progress",
              "control",
              "taken",
              "total",
              "failure",
              "detail"
            ],
            "type": "object"
          }
        ],
        "properties": {
          "at": {
            "$ref": "#/components/schemas/Stamp",
            "description": "When it happened."
          },
          "session": {
            "description": "Which session.",
            "type": "string"
          }
        },
        "required": [
          "session",
          "at"
        ],
        "type": "object"
      },
      "Provenance": {
        "description": "Where a claim about this device came from.",
        "oneOf": [
          {
            "const": "declared",
            "description": "Transcribed from documentation. Nominated, not legislated.",
            "type": "string"
          },
          {
            "const": "measured",
            "description": "Observed on this device, by probe. Beats `Declared` on conflict (E1).",
            "type": "string"
          }
        ]
      },
      "RecordReport": {
        "description": "What one recording turned out to be (design D7, D10).\n\n`capture::PhotoReport`'s counterpart. Every field is a measurement of what\nhappened rather than a restatement of the request — `negotiated` is D5's \"requested is not\napplied\" for a stream that ran for seconds rather than for one frame, `format` is the\ncontainer the pairing chose, and `summary` is what the container itself counted.",
        "properties": {
          "camera": {
            "$ref": "#/components/schemas/CameraId",
            "description": "The camera that was recorded."
          },
          "ended": {
            "$ref": "#/components/schemas/RecordingEnd",
            "description": "Why it stopped."
          },
          "format": {
            "$ref": "#/components/schemas/VideoFormat",
            "description": "The container it is in.\n\nReported rather than assumed, because a request that named no extension let the\nnegotiated stream decide — so this is the answer to a question the caller may have\nleft open (see `RecordRequest::container`)."
          },
          "negotiated": {
            "$ref": "#/components/schemas/NegotiatedStream",
            "description": "What the device agreed to deliver, with every difference from the request (D5)."
          },
          "path": {
            "description": "The file that was written.",
            "type": "string"
          },
          "started_at": {
            "$ref": "#/components/schemas/Stamp",
            "description": "When the recording started, on the wall clock.\n\nThe caller's `Stamp`, as `PhotoReport::taken_at` is: the engine reads no clock\n(design §2.10), and the monotonic one this report's `RecordReport::wall_clock_ms`\ncomes from is a different question — conflating them is how an NTP step becomes a\nduration."
          },
          "stats": {
            "$ref": "#/components/schemas/StreamStats",
            "description": "How the take's frames were *delivered* (design D16).\n\nBeside `Self::summary` rather than inside it, because they answer two questions\nabout one take: the summary is what the **file** turned out to be, and this is what\nthe **link** turned out to be. A consumer proving a camera-forwarding path reads this\none — `frames_dropped` here and `RecordingSummary::dropped_frames` are two readings\nof one accumulator, so they cannot disagree, and everything else here (the gap runs,\nthe interval distribution, the skew against this report's own wall clock) exists\nnowhere else in the document.\n\n**Required on the wire, and that is the whole of its shape** (note **N291**). A\n`#[serde(default)]` here would make \"this daemon never measured delivery\" and \"this\nlink delivered nothing\" the same document, because the default `StreamStats` is\nbyte-identical to the answer a `RecordingEnd::DeviceQuiet` take produces — the\nconversion AGENTS rule 7 forbids, arriving through a serde attribute rather than\nthrough a `match`. An `Option` would be the other wrong answer: no producer in this\ntree can construct the `None`, so it would be an arm every consumer must branch on and\nnothing can ever reach. An answer that arrives without this field is a version skew,\nand a version skew is refused by name."
          },
          "summary": {
            "$ref": "#/components/schemas/RecordingSummary",
            "description": "What the container counted."
          },
          "wall_clock_ms": {
            "description": "How long the recording took on the engine's monotonic clock, in milliseconds.\n\n**Beside `RecordingSummary::span_us` rather than instead of it, because they are\ntwo measurements and the difference between them is a finding.** `span_us` is the last\nwritten frame's timestamp minus the first's, on the *driver's* clock and in\nmicroseconds; this is the engine's own elapsed time across the whole take, in\nmilliseconds. `webcam-handler-imaging` reads no clock of its own — its\n`RecordingCaps::max_span` doc says wall-clock time is the caller's question — and this\nengine is that caller.\n\nThree things make the two disagree, and a caller can tell them apart from the rest of\nthis document: a **driver clock that ran slow or fast** (the span is short or long\nagainst a wall clock that is not), **frames dropped before they reached us**\n(`RecordingSummary::dropped_frames` is non-zero), and **time spent in our own loop**\n— the wall clock includes the header write, the settle-free start, the close-time\nindex and every turn that brought no frame, none of which are inside the span. The\nwall clock is therefore always the larger of the two for an honest take, and a span\nthat *exceeds* it is a driver clock this host does not share.\n\n**The file's own declared duration is a third number and it is not the span.** A\nconstant-rate container declares `frames_written × declared_interval_us`, while the\ninterval is the mean of `frames_written - 1` gaps — so a healthy take's file legitimately\nclaims **one frame period more** video than this wall clock, and a comparison written the\nobvious way goes red on a camera that is working (note **N120**; measured at 1102 ms\ndeclared against 1016 ms of wall clock on a 8 fps take). The bound docs/7 P6d asks for is\ntherefore two-sided: at most one frame period above, and `MAX_WALL_CLOCK_OVERHANG_MS`\nbelow, with `crates/backends/v4l2/tests/hardware.rs` carrying the measurement.\n\nMilliseconds rather than microseconds because that is the resolution the engine's\nclock actually has (`engine::settle::Millis`), and a `wall_clock_us` filled in by\nmultiplying by a thousand would be three digits of precision nobody measured.\n\nThis pair is docs/7 **P6d**'s criterion — the declared-vs-wall-clock duration bound,\nmeasured on a real capture — which is why both live in the one document a `--json`\nconsumer already has.",
            "format": "uint64",
            "minimum": 0,
            "type": "integer"
          }
        },
        "required": [
          "camera",
          "started_at",
          "path",
          "format",
          "negotiated",
          "summary",
          "stats",
          "wall_clock_ms",
          "ended"
        ],
        "type": "object"
      },
      "RecordRequest": {
        "description": "Everything one recording needs (design D7, D10).\n\n`capture::PhotoRequest`'s counterpart, and deliberately smaller than it. There is\nno `settle` field: the frames immediately after `STREAMON` are dark until AE converges\n[PF:11], and a *photo* discards them because one dark frame is the whole answer — a\nrecording's are visible in the file beside everything that follows, and discarding them\nwould move the recording's start away from where the caller put it. An agent filming a\ntransition asked for the moment it asked for. Note **N111** records the decision.\n\nThere is no `format` field either, and that is `RecordRequest::container`'s paragraph:\nthe sink path's extension names the container, exactly as it names a photo's encoding.",
        "properties": {
          "duration_ms": {
            "description": "How long to record, in milliseconds. `None` is\n`limits::DEFAULT_RECORDING_MS`.\n\nRead through `RecordRequest::budget_ms`, which is where the default and the cap\nlive; a caller that read this field directly would be reading a request rather than a\ndecision.",
            "format": "uint64",
            "minimum": 0,
            "type": [
              "integer",
              "null"
            ]
          },
          "sink": {
            "$ref": "#/components/schemas/Sink",
            "description": "Where the file goes.\n\nA `Sink::ServerPath` and nothing else — see `RecordRequest::server_path` for the\nrefusal and note **N110** for why this verb narrows D10's two-variant DTO rather than\nchanging it."
          },
          "stream": {
            "$ref": "#/components/schemas/StreamRequest",
            "default": {
              "buffer_count": 4
            },
            "description": "What to ask the device's format negotiation for.\n\nThe negotiated answer decides the container when the sink path named none, so this\nfield and `RecordRequest::container` are the two halves of one question — which is\nwhy `VideoFormat::resolve` takes both and refuses rather than picking a winner."
          },
          "wait": {
            "default": false,
            "description": "Whether a request that finds the camera's command queue full waits for its turn.\n\nD12's flag, in the same shape and for the same reasons\n`capture::PhotoRequest::wait` carries it — that field's doc is the argument\nand this one is not a second copy of it. What is worth saying twice is which *call* it\nbounds: a recording is a chain of turns (note **N111**), and only the **first** of them\nnegotiates a stream on a camera somebody else may be holding. So this flag decides how\n`record_start` meets a busy camera and nothing else — the turns that follow are issued\nby a driver that already owns the take, and a recording that had to re-queue per frame\nwould be a recording bounded by other clients rather than by its own duration.\n\n`#[serde(default)]` for `capture::PhotoRequest::wait`'s reason: `false` is the\nbehaviour every caller written before this field existed already meets, and a default\nthat turned a prompt `Error::Busy` into ten seconds of latency would change requests\nnobody rewrote.",
            "type": "boolean"
          }
        },
        "required": [
          "sink"
        ],
        "type": "object"
      },
      "RecordStatus": {
        "description": "What one camera's recording is doing (design D10; docs/7 P6c).\n\n**The answer to `record_status`, and D10's whole progress mechanism**: *\"progress by\npolling `record_status` — no recording subscription in v1\"*. A recording runs for seconds\nor minutes on a camera the caller does not hold, so the only thing a caller can do between\n`record_start` and `record_stop` is ask; this is the shape of the asking.\n\nIt is a **struct with an optional take** rather than a three-armed enum, and the shape is\nthe claim: a camera has at most one take, so \"is there one\" and \"what is it doing\" are one\nquestion with one answer and `RecordStatus::is_running` is the one predicate every\nconsumer branches on. A client's poll loop, `webcam-handler-cli`'s renderer and the\ndaemon's own refusals all read that one function, which is what stops three copies of \"has\nit finished?\" from disagreeing about a take whose driver has stopped but whose container is\nstill closing.",
        "properties": {
          "camera": {
            "$ref": "#/components/schemas/CameraId",
            "description": "The camera this is about, resolved (D1) rather than echoed back."
          },
          "take": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/TakeStatus"
              },
              {
                "type": "null"
              }
            ],
            "description": "The take this camera holds, or `None` when it holds none.\n\n`None` is a camera that has never recorded **and** a camera whose take has been\ncollected by `record_stop`. Those are deliberately one answer rather than two: what a\ncaller can do about either is identical — start a recording — and a vocabulary that\ndistinguished them would be this daemon remembering something about a camera after the\ncaller asked for it to be forgotten."
          }
        },
        "required": [
          "camera"
        ],
        "type": "object"
      },
      "RecordingEnd": {
        "description": "Why a recording stopped.\n\n`RecordingSummary::cap_reached` answers a narrower question — *which bound the\ncontainer refused a frame at* — and it is `None` for every recording that ended for\none of the other four reasons below. An agent reading only that field cannot tell a\ntake that ran its whole duration from one whose camera went silent after two frames,\nand those are different findings about the device under test: the first is a\nrecording, the second is a defect. So the ending is carried as its own closed\nvocabulary, walked by\n`engine::record`'s `every_ending_in_the_vocabulary_is_one_a_recording_can_reach` —\nbecause an ending nothing can produce is a word in a report rather than an answer.",
        "oneOf": [
          {
            "const": "duration",
            "description": "The wall-clock budget the request asked for was spent.\n\nThe ordinary ending, and the one `RecordRequest::budget_ms` names. Measured on\nthe engine's own monotonic clock rather than on the driver's frame timestamps —\nsee `RecordReport::wall_clock_ms` for why a recording is bounded by both.",
            "type": "string"
          },
          {
            "const": "cap",
            "description": "One of the bounds `engine::record::caps` assembles out of\n`limits` refused a frame.\n\n`RecordingSummary::cap_reached` names which one; it is `Some` exactly when the\nending is this.",
            "type": "string"
          },
          {
            "const": "device_quiet",
            "description": "The device stopped delivering.\n\n`limits::RECORDING_MAX_EMPTY_TURNS` consecutive turns brought no frame,\nwhich is the bound that covers the case the duration cannot: a driver answering\npromptly and delivering nothing, on a clock that is not moving. AGENTS rule 7 is\nwhy it is an *ending* rather than an error — the recording it produced is real\nand its frames are the device's own; what is being reported is that there were no\nmore of them.",
            "type": "string"
          },
          {
            "const": "stopped",
            "description": "The caller ended it.\n\nWhat `record_stop` produces (docs/7 P6c): a recording driven one turn at a time\nstops when its caller says so, and nothing about the device or the bounds is\ninvolved.",
            "type": "string"
          },
          {
            "const": "device_failed",
            "description": "The device refused mid-take, and the caller is holding that refusal.\n\nThe container is still closed — a recording that left a half-written file because\nthe camera was unplugged would fail docs/7 P6b's \"every fault leaves a parseable\nfile\" — so this ending exists to say that the file is finished and the take is\nnot. `engine::record::run` produces it and then answers with the *device's* own\nerror rather than with the report, because a `DeviceGone` that arrived as a\nsuccessful recording would be exactly the conversion AGENTS rule 7 forbids.",
            "type": "string"
          }
        ]
      },
      "RecordingSummary": {
        "description": "What a finished recording turned out to be.\n\nEvery field is a *measurement* of the file that was written, not a restatement of what\nwas asked for — which is the point of returning it at all. `declared_interval_us` is the\nnumber now in the file's own rate field(s), and `interval_source` says whether it is a\nfinding or a placeholder.\n\nNamed for the recording rather than for AVI since P6b: the fields are the same fields\nP6a's `AviSummary` had, and every one of them is a claim a Y4M take can make too.",
        "properties": {
          "bytes_written": {
            "description": "The size of the finished file, every trailer the container writes at close included.",
            "format": "uint64",
            "minimum": 0,
            "type": "integer"
          },
          "cap_reached": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/CapReached"
              },
              {
                "type": "null"
              }
            ],
            "description": "The bound that ended the recording, if one did."
          },
          "declared_interval_us": {
            "description": "The frame interval the finished file declares.\n\nFor AVI that is `avih.dwMicroSecPerFrame` and `strh.dwScale`/`dwRate`, which\n`imaging::avi::write::AviWriter::finish` rewrites together so a reader finding them\ndisagreeing has caught a defect. For Y4M it is the header's `F` ratio, whose denominator\nis zero-padded to a fixed width so that `imaging::y4m::Y4mWriter::finish` can rewrite it\nin place without moving a byte after it.",
            "format": "uint32",
            "minimum": 0,
            "type": "integer"
          },
          "dropped_frames": {
            "description": "Frames the driver's `sequence` numbers say never arrived.\n\nA `u64` because the gaps accumulate: each one may be as large as a `u32`, and a\nrecording may hold as many gaps as it holds frames. Reporting it is what stops a\ndropped frame from reading as a slow transition (Expected usage item 10).",
            "format": "uint64",
            "minimum": 0,
            "type": "integer"
          },
          "frames_written": {
            "description": "How many frames the file holds.",
            "format": "uint32",
            "minimum": 0,
            "type": "integer"
          },
          "interval_source": {
            "$ref": "#/components/schemas/IntervalSource",
            "description": "Whether that interval was measured, negotiated or provisional.\n\nThe same three answers in both containers since P6d; see the module doc for what used to\nmake Y4M the exception and what ended it."
          },
          "span_us": {
            "description": "The last written frame's timestamp minus the first's, when that is a duration.\n\n`None` when fewer than two frames were written, and when the driver's clock ran\nbackwards across the take — a negative span is not a duration, and reporting it as\nzero would claim a measurement nobody made.\n\nCarried by **both** containers: `span_us / (frames_written - 1)` is the mean, computed by\nwhoever wants it out of two numbers that were observed. That is why the Y4M header's\nun-rewritable rate cost the caller nothing for the two phases it lasted, and it is still\nthe number to read — a header declares `frames_written × declared_interval_us`, one frame\nperiod more than this, because a constant-rate container shows its last frame for an\ninterval no gap between timestamps can contain (note **N120**).",
            "format": "uint64",
            "minimum": 0,
            "type": [
              "integer",
              "null"
            ]
          }
        },
        "required": [
          "frames_written",
          "bytes_written",
          "declared_interval_us",
          "interval_source",
          "dropped_frames"
        ],
        "type": "object"
      },
      "RestoreOutcome": {
        "description": "One control's restore outcome.",
        "oneOf": [
          {
            "description": "Written, with the usual `{requested, applied}` pair (D3 applies inside restore\ntoo — a restore can be clamped like any other write).",
            "properties": {
              "applied": {
                "$ref": "#/components/schemas/Applied",
                "description": "What was written and what stuck."
              },
              "outcome": {
                "const": "restored",
                "type": "string"
              }
            },
            "required": [
              "outcome",
              "applied"
            ],
            "type": "object"
          },
          {
            "description": "Already holding the recorded value; nothing was written.",
            "properties": {
              "control": {
                "$ref": "#/components/schemas/ControlSlug",
                "description": "Which control."
              },
              "outcome": {
                "const": "already_correct",
                "type": "string"
              }
            },
            "required": [
              "outcome",
              "control"
            ],
            "type": "object"
          },
          {
            "description": "The automation that owned this control when the snapshot was taken owns it again,\nso its value is that automation's to choose — exactly as it was.\n\n**A success**, and the distinction is not pedantry. Restoring an automation control\nto \"on\" re-engages the manual control it governs, so on any device whose INACTIVE\nflag follows the automation's value this is the *ordinary* outcome for every\nguarded write's restore. Measured on the seed hardware the day\n`controls --discover-pairs` first ran: the probe put the camera back perfectly and\nthen reported two controls it \"could not put back\", because the vocabulary had no\nway to say \"its owner is back\". See note N9.",
            "properties": {
              "automation": {
                "anyOf": [
                  {
                    "$ref": "#/components/schemas/ControlSlug"
                  },
                  {
                    "type": "null"
                  }
                ],
                "description": "The automation control that holds it, when the pair set names one."
              },
              "control": {
                "$ref": "#/components/schemas/ControlSlug",
                "description": "Which control."
              },
              "outcome": {
                "const": "owned_by_automation",
                "type": "string"
              }
            },
            "required": [
              "outcome",
              "control"
            ],
            "type": "object"
          },
          {
            "description": "Could not be put back, and why.",
            "properties": {
              "control": {
                "$ref": "#/components/schemas/ControlSlug",
                "description": "Which control."
              },
              "outcome": {
                "const": "unrestorable",
                "type": "string"
              },
              "reason": {
                "$ref": "#/components/schemas/UnrestorableReason",
                "description": "The reason."
              }
            },
            "required": [
              "outcome",
              "control",
              "reason"
            ],
            "type": "object"
          }
        ]
      },
      "RestoreReport": {
        "description": "What a whole restore did — to the camera, and to the session document.",
        "properties": {
          "freed": {
            "description": "The controls a dead sweep had stranded, given back by this call (note **N139**).\n\n**A restore repairs the session as well as the camera, and this is the half that has\nnothing to do with the snapshot** (note **N150**). A process killed between\n`begin_sweep` and its first sample leaves a control in `Sweeping { done: 0 }`, which\nevery shipped verb refuses and no snapshot describes; `calibrate restore` puts it back\nto `Untouched` and names it here. Empty is the ordinary answer and means nothing was\nstranded — not that nothing was looked at.\n\nIt is on the report rather than only in `log.ndjson` because `calibrate restore\n--json` answers *this* document and exits 0. A run that changed a control's status on\ndisk and answered `{\"outcomes\":[]}` would be telling an unattended caller that nothing\nhappened (rubric A8).",
            "items": {
              "$ref": "#/components/schemas/ControlSlug"
            },
            "type": "array"
          },
          "outcomes": {
            "description": "One entry per snapshotted control, in the order they were attempted.",
            "items": {
              "$ref": "#/components/schemas/RestoreOutcome"
            },
            "type": "array"
          }
        },
        "required": [
          "outcomes"
        ],
        "type": "object"
      },
      "Sample": {
        "description": "One sample: a value, what the device actually took, the photo, and the scores.",
        "properties": {
          "applied": {
            "description": "The value the device actually holds — D3 applies inside sweeps too [PF:6], and a\nsample labeled with a value the camera never held would poison every comparison\nbuilt on it.",
            "format": "int64",
            "type": "integer"
          },
          "captured_at": {
            "$ref": "#/components/schemas/Stamp",
            "description": "When it was taken."
          },
          "metrics": {
            "additionalProperties": {
              "format": "double",
              "type": "number"
            },
            "description": "The metric scores.",
            "type": "object"
          },
          "photo": {
            "description": "The photo, as a path relative to the session directory — so a session directory\nrelocates as a unit.",
            "type": "string"
          },
          "requested": {
            "description": "The value the sweep asked for.",
            "format": "int64",
            "type": "integer"
          },
          "warnings": {
            "description": "Anything notable about the write.",
            "items": {
              "$ref": "#/components/schemas/WriteWarning"
            },
            "type": "array"
          }
        },
        "required": [
          "requested",
          "applied",
          "photo",
          "captured_at"
        ],
        "type": "object"
      },
      "SampleCap": {
        "description": "Which cap trimmed a sweep.\n\nTwo numbers, because a sweep is bounded for two unrelated reasons and a caller told\nonly \"it was capped\" cannot tell which lever they have. `ALL` is generated from this\ndefinition, so a third cap joins every walk that reads it rather than acquiring a\nspelling nobody looked at (rubric rule 6).\n\nEvery sentence below writes its number out. These are published into\n`schemas/webcam-handler-schema.json`, which D10 commits so a consumer needs no Rust\ntoolchain — and a description that is only the name of a constant tells such a reader\nnothing at all (note **N148**).",
        "oneOf": [
          {
            "const": "total",
            "description": "The whole-sweep cap: 256 samples, `limits::MAX_SWEEP_SAMPLES`.\n\nIt bounds camera time rather than wear — at PF:9's two seconds per photo a\n256-sample sweep is already eight and a half minutes — so it applies to every\ncontrol. The stride is widened rather than the range truncated, so a capped plan\nstill spans the whole range at a coarser precision.",
            "type": "string"
          },
          {
            "const": "motion",
            "description": "The motion cap: 32 samples, `limits::MAX_MOTION_SWEEP_SAMPLES`.\n\nThis control moves motors and motors wear (design §5), so a pan or tilt sweep is\nheld to an eighth of the general cap. Nothing turns this off; a caller who wants\nparticular positions names them with explicit values.",
            "type": "string"
          }
        ]
      },
      "Selection": {
        "description": "What `calibrate select` was told to do (design D8).\n\nLives here rather than in the shared command surface because both surfaces need it and\nonly one of them may name the other: `webcam-handler-cli-core` is behind the T6 purity\nwall and can never depend on `webcam-handler-api`, so a selection the wire carries and\na selection a command line parses have to be one type or two that drift.\n\nStruct variants rather than newtypes because this crate's enums are internally tagged\nand serde cannot internally tag a newtype variant wrapping a scalar.",
        "oneOf": [
          {
            "description": "Rank the samples by this metric and take the best.",
            "properties": {
              "kind": {
                "const": "by_metric",
                "type": "string"
              },
              "metric": {
                "$ref": "#/components/schemas/MetricName",
                "description": "Which metric."
              }
            },
            "required": [
              "kind",
              "metric"
            ],
            "type": "object"
          },
          {
            "description": "A value somebody chose, and who.",
            "properties": {
              "chosen_by": {
                "$ref": "#/components/schemas/ChosenBy",
                "description": "Who chose it. A `ChosenBy` rather than a `Selector`, so a caller cannot\nrecord \"a metric chose 42\" without any metric having ranked anything."
              },
              "kind": {
                "const": "by_value",
                "type": "string"
              },
              "value": {
                "description": "The value, as a sample's *applied* value — the one the camera actually held\n[PF:6]. Selecting a requested value would name a value no photo was taken at.",
                "format": "int64",
                "type": "integer"
              }
            },
            "required": [
              "kind",
              "value",
              "chosen_by"
            ],
            "type": "object"
          }
        ]
      },
      "Selector": {
        "description": "Who chose a calibrated value.",
        "oneOf": [
          {
            "description": "A built-in metric ranked the samples.",
            "properties": {
              "kind": {
                "const": "metric",
                "type": "string"
              },
              "name": {
                "$ref": "#/components/schemas/MetricName",
                "description": "Which metric."
              }
            },
            "required": [
              "kind",
              "name"
            ],
            "type": "object"
          },
          {
            "description": "An agent reviewed the sample photos and picked one.",
            "properties": {
              "kind": {
                "const": "agent",
                "type": "string"
              }
            },
            "required": [
              "kind"
            ],
            "type": "object"
          },
          {
            "description": "A human did.",
            "properties": {
              "kind": {
                "const": "human",
                "type": "string"
              }
            },
            "required": [
              "kind"
            ],
            "type": "object"
          }
        ]
      },
      "Session": {
        "description": "A calibration session.",
        "properties": {
          "controls": {
            "additionalProperties": {
              "$ref": "#/components/schemas/ControlSession"
            },
            "default": {},
            "description": "Per-control state.",
            "type": "object"
          },
          "created_at": {
            "$ref": "#/components/schemas/Stamp",
            "description": "When it started."
          },
          "criteria": {
            "description": "Ordered criteria for judging a sample. Recorded because the *selector* needs\nthem, whether that selector is a human, an agent, or a metric.",
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "fingerprint": {
            "$ref": "#/components/schemas/CameraFingerprint",
            "description": "Which camera this was recorded against."
          },
          "goal": {
            "description": "What the session is trying to achieve, in the operator's words.",
            "type": "string"
          },
          "id": {
            "description": "The session's identity — a UUIDv7, so directory listings sort chronologically.",
            "type": "string"
          },
          "notes": {
            "description": "Free-text notes. v1 does not pretend to model that focus and exposure interact;\nit lets the operator write it down.",
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "pairs": {
            "description": "The automation pairs this session guards its writes with (design D3).\n\nPersisted rather than re-derived, and that is the whole point of the field: a\nprocess picking a crashed session back up has to put the camera away in the same\norder the sweep took it apart (D4's automation-before-manual), and re-running the\ndiscovery probe to find that out would move the camera *during* the recovery. The\ndeclared table merged with what a probe measured on this device, measured winning\n(E1) — so what is stored here is the answer, not the ingredients.",
            "items": {
              "$ref": "#/components/schemas/AutomationPair"
            },
            "type": "array"
          },
          "pre_snapshot": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/Snapshot"
              },
              {
                "type": "null"
              }
            ],
            "description": "The control state as found, persisted **before** the first write so a crashed\nsweep is recoverable (design §6, gate G3)."
          },
          "queue": {
            "description": "The control queue, in the order the operator wants them calibrated. Reorderable\nbetween sweeps.",
            "items": {
              "$ref": "#/components/schemas/ControlSlug"
            },
            "type": "array"
          },
          "schema_version": {
            "description": "The document version. Present from day one; a foreign version is a typed refusal\n(design D9), never a best-effort parse.",
            "format": "uint32",
            "minimum": 0,
            "type": "integer"
          },
          "task": {
            "description": "The task, as the operator described it.",
            "type": "string"
          },
          "task_slug": {
            "description": "The task through the slug transform — the directory name.",
            "type": "string"
          },
          "tool_version": {
            "description": "The tool version that wrote it.",
            "type": "string"
          },
          "updated_at": {
            "$ref": "#/components/schemas/Stamp",
            "description": "When it last changed."
          }
        },
        "required": [
          "schema_version",
          "id",
          "fingerprint",
          "task",
          "task_slug",
          "goal",
          "created_at",
          "updated_at",
          "tool_version"
        ],
        "type": "object"
      },
      "SessionList": {
        "description": "Every session the store holds, newest first — what `calibrate list` answers.",
        "properties": {
          "sessions": {
            "default": [],
            "description": "The sessions, newest first by UUIDv7 byte order. Always emitted, empty or not — an\nempty store answers with an empty list, not with a missing key.",
            "items": {
              "$ref": "#/components/schemas/SessionListing"
            },
            "type": "array"
          }
        },
        "type": "object"
      },
      "SessionListing": {
        "description": "One session in a listing, as the directory tree alone describes it.\n\n**Nothing here is parsed out of a session document**, and that is the property rather\nthan a shortcut: D9 chose UUIDv7 so a listing sorts chronologically without opening a\nfile, so listing costs no parse, cannot fail on the one session whose document is\ncorrupt, and still shows a session written by a build this one cannot read. Reading a\nsession is `SessionStatus`'s job, and it is the one that refuses a foreign version.",
        "properties": {
          "camera": {
            "description": "The camera, as the fingerprint slug that names its directory.",
            "type": "string"
          },
          "id": {
            "description": "The session's identity — the directory's name.",
            "type": "string"
          },
          "path": {
            "description": "Where it lives.",
            "type": "string"
          },
          "task_slug": {
            "description": "The task, as the slug that names its directory.",
            "type": "string"
          }
        },
        "required": [
          "id",
          "camera",
          "task_slug",
          "path"
        ],
        "type": "object"
      },
      "SessionRef": {
        "description": "Which session a verb is about (design D8, D9).\n\nThe two forms are not interchangeable and the type keeps them apart: a task names the\nsession for *this* camera, and a UUID names one session wherever it lives — including\nunder another camera, which is the case `calibrate apply`'s fingerprint check exists\nfor.\n\nHere rather than in the command surface for the reason `Selection` is: the wire and\nthe command line name the same session, and `webcam-handler-cli-core` cannot see\n`webcam-handler-api` (T6). Struct variants, and the `id` is a string in the emitted\nschema, matching what `Session::id` already does.",
        "oneOf": [
          {
            "description": "The newest session recorded for this camera and task.",
            "properties": {
              "kind": {
                "const": "task",
                "type": "string"
              },
              "task": {
                "description": "The task, in the words `calibrate start` recorded.",
                "type": "string"
              }
            },
            "required": [
              "kind",
              "task"
            ],
            "type": "object"
          },
          {
            "description": "The session with this id, whichever camera it belongs to.",
            "properties": {
              "id": {
                "description": "The session's UUIDv7.",
                "type": "string"
              },
              "kind": {
                "const": "id",
                "type": "string"
              }
            },
            "required": [
              "kind",
              "id"
            ],
            "type": "object"
          }
        ]
      },
      "SessionStatus": {
        "description": "A session and its history — what `calibrate status` answers.\n\nTwo documents rather than one, and both of them, because they answer different\nquestions and neither can be read off the other. `Session` is *where the session\nstands*: which controls are calibrated, at what value, chosen by whom. The log is\n*what happened to get there*, and it holds the facts the document by design does not —\nwhich automation controls were switched off, and, when a sweep stopped early, the\nrefusal that stopped it (`SessionEvent::SweepInterrupted`).",
        "properties": {
          "log": {
            "default": [],
            "description": "Its `log.ndjson`, oldest first, with a torn last line dropped (design D9).\n\nAlways emitted, empty or not: this is an *answer*, and a consumer counting the\nhistory should meet a list with nothing in it rather than a missing key.",
            "items": {
              "$ref": "#/components/schemas/LogEntry"
            },
            "type": "array"
          },
          "session": {
            "$ref": "#/components/schemas/Session",
            "description": "The session document."
          }
        },
        "required": [
          "session"
        ],
        "type": "object"
      },
      "SettlePolicy": {
        "description": "A settle policy: the spec plus the deadline that bounds it.\n\nBoth forms are bounded, because \"skip 10 frames\" on a camera delivering none is a\nhang, and a hang is the failure mode E3 warns about wearing a disguise.",
        "properties": {
          "deadline_ms": {
            "default": 5000,
            "description": "How long the whole settle may take, in milliseconds. At most 10000\n(`limits::MAX_SETTLE_DEADLINE_MS`).\n\nA deadline above that cap is refused rather than clamped: one camera is one actor\nthread, so a settle is time nobody else gets, and silently shortening a number a\ncaller chose would answer a question they did not ask. The number is written out\nhere as well as named, because this sentence is published into\n`schemas/webcam-handler-schema.json` for consumers who have no Rust toolchain to\nresolve a constant with (D10), and `schemars` carries the same bound as this field's\n`maximum` so a validator refuses what the tool refuses.",
            "format": "uint64",
            "maximum": 10000,
            "minimum": 0,
            "type": "integer"
          },
          "spec": {
            "$ref": "#/components/schemas/SettleSpec",
            "default": {
              "frames": 10,
              "kind": "skip_frames"
            },
            "description": "What counts as settled."
          }
        },
        "type": "object"
      },
      "SettleSpec": {
        "description": "How long to wait for the sensor to settle before taking a photo [PF:11].",
        "oneOf": [
          {
            "description": "Discard this many frames first.",
            "properties": {
              "frames": {
                "description": "How many.",
                "format": "uint32",
                "minimum": 0,
                "type": "integer"
              },
              "kind": {
                "const": "skip_frames",
                "type": "string"
              }
            },
            "required": [
              "kind",
              "frames"
            ],
            "type": "object"
          },
          {
            "description": "Keep discarding frames for this long.",
            "properties": {
              "kind": {
                "const": "settle_for",
                "type": "string"
              },
              "millis": {
                "description": "How long, in milliseconds.",
                "format": "uint64",
                "minimum": 0,
                "type": "integer"
              }
            },
            "required": [
              "kind",
              "millis"
            ],
            "type": "object"
          }
        ]
      },
      "Sink": {
        "description": "Where binary results go (design D10).\n\nTwo variants, because a daemon and an in-process CLI need different answers and\npretending otherwise is how `-o out.jpg` ends up meaning the server's cwd. Clients\nresolve relative paths against their own cwd *before* sending.",
        "oneOf": [
          {
            "description": "Send the bytes back in the response.",
            "properties": {
              "format": {
                "$ref": "#/components/schemas/PhotoFormat",
                "description": "The encoding to produce."
              },
              "kind": {
                "const": "return_bytes",
                "type": "string"
              }
            },
            "required": [
              "kind",
              "format"
            ],
            "type": "object"
          },
          {
            "description": "Write them to an absolute path on whichever host runs the engine.",
            "properties": {
              "kind": {
                "const": "server_path",
                "type": "string"
              },
              "path": {
                "description": "The absolute destination.",
                "type": "string"
              }
            },
            "required": [
              "kind",
              "path"
            ],
            "type": "object"
          }
        ]
      },
      "SizeRefusal": {
        "description": "The size half of a `Error::FormatUnsupported`: the frame size a caller named, and\nevery size the device can actually deliver.\n\n**A refusal has to say which knob to turn.** `FormatUnsupported` carried one slot for\n\"what was asked for\" and it held a `PixelFormat`, so a request refused for its *size*\nhad to report `requested: None` and a list of formats — which rendered as *\"format\n(unspecified) is unavailable; MJPG, YUYV would be accepted\"* and told an unattended\ncaller that the half of its request that was answerable was the problem. Retrying with\none of the formats named produces the identical refusal, so the guide's *\"fix the\nrequest\"* disposition becomes a loop: note **N129**'s misdirection class, at the same\nvariant, one phase later (note **N138**).\n\nSo the size that was refused and the sizes that would be accepted travel together, and\nthe presence of this payload is what says *which* half of the request could not be met.\nSee `Error::size_unsupported` for the two constructors that keep the two causes\nmutually exclusive.",
        "properties": {
          "available": {
            "description": "Every size the device can deliver, across **every** format it enumerates — because\nthe resolver's candidate set is device-wide, so \"no mode fits\" is a fact about the\ndevice rather than about whichever format a ranking happened to pick.\n\nA `FrameSize` rather than a `(width, height)` pair, so a **stepwise** entry\narrives as the range it is: collapsing it to its maximum corner here would be the\nsame falsehood `FrameSize::largest_within` exists to avoid, committed in the\nmessage that is supposed to repair the request.\n\nEntries whose shape this build cannot read (D2's `FrameSize::Unknown`) are left\nout: this list is what *would be accepted*, and a size we cannot interpret is not\nsomething a caller can ask for. The device's own enumeration still carries them —\n`info` is where a caller sees that the driver said something we did not understand.",
            "items": {
              "$ref": "#/components/schemas/FrameSize"
            },
            "type": "array"
          },
          "requested_height": {
            "description": "The height the caller named.",
            "format": "uint32",
            "minimum": 0,
            "type": "integer"
          },
          "requested_width": {
            "description": "The width the caller named.",
            "format": "uint32",
            "minimum": 0,
            "type": "integer"
          }
        },
        "required": [
          "requested_width",
          "requested_height",
          "available"
        ],
        "type": "object"
      },
      "Snapshot": {
        "description": "Every writable control's value at one instant.",
        "properties": {
          "camera": {
            "$ref": "#/components/schemas/CameraFingerprint",
            "description": "Which camera it came from — restoring a snapshot onto a different camera is a\n`Error::FingerprintMismatch`, not a surprise."
          },
          "declined": {
            "description": "The writable controls this snapshot could **not** record, because the device\ndeclined to read them — a `current` absent from a descriptor that predicted one.\n\nA snapshot's whole promise is that the camera can be put back, and a control with\nno recorded value is one it cannot promise for. Leaving such a control out of\n`entries` and saying nothing would make `is_complete` answer `true` for a camera\nthat was changed and not changed back — outcomes are one per entry, so a control\nthat never became an entry produces no outcome to be incomplete about (note\n**N195**).\n\nEmpty on every device this project has measured, and the ordinary answer. Restore\nturns each name here into an `unrestorable` outcome whose reason is\n`never_recorded`, which is what makes the incompleteness reach a report, an exit\ncode and a human line.",
            "items": {
              "$ref": "#/components/schemas/ControlSlug"
            },
            "type": "array"
          },
          "entries": {
            "description": "The recorded values.",
            "items": {
              "$ref": "#/components/schemas/SnapshotEntry"
            },
            "type": "array"
          },
          "taken_at": {
            "$ref": "#/components/schemas/Stamp",
            "description": "When it was taken."
          }
        },
        "required": [
          "taken_at",
          "camera",
          "entries"
        ],
        "type": "object"
      },
      "SnapshotEntry": {
        "description": "One control's recorded value.",
        "properties": {
          "control": {
            "$ref": "#/components/schemas/ControlSlug",
            "description": "Which control."
          },
          "id": {
            "$ref": "#/components/schemas/ControlId",
            "description": "Its kernel id, so restore does not depend on the slug transform being stable\nacross versions."
          },
          "role": {
            "$ref": "#/components/schemas/ControlRole",
            "description": "Its role in restore ordering, decided from the pairing set when the snapshot was\ntaken."
          },
          "value": {
            "$ref": "#/components/schemas/ControlValue",
            "description": "The value at snapshot time."
          },
          "was_inactive": {
            "description": "Whether the device reported it INACTIVE at snapshot time [PF:3] — such a control\nis restored on the second pass.",
            "type": "boolean"
          },
          "was_volatile": {
            "description": "Whether the device reported it VOLATILE. A volatile value cannot be meaningfully\nrestored, and restore says so rather than pretending it did.",
            "type": "boolean"
          }
        },
        "required": [
          "control",
          "id",
          "value",
          "role",
          "was_inactive",
          "was_volatile"
        ],
        "type": "object"
      },
      "Stamp": {
        "description": "An RFC 3339 timestamp, e.g. 2026-08-08T10:11:12Z",
        "type": "string"
      },
      "StreamRequest": {
        "description": "What a caller wants from a stream. Every field is optional: \"just give me something\"\nis a legitimate request, and the answer is always reported back.",
        "properties": {
          "buffer_count": {
            "default": 4,
            "description": "How many buffers to queue.",
            "format": "uint32",
            "minimum": 0,
            "type": "integer"
          },
          "height": {
            "description": "Preferred height in pixels.",
            "format": "uint32",
            "minimum": 0,
            "type": [
              "integer",
              "null"
            ]
          },
          "interval": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/FrameInterval"
              },
              {
                "type": "null"
              }
            ],
            "description": "Preferred frame interval.\n\nThe vocabulary is shared with the *answer*, which is what makes the `unstated` kind\nreadable here at all — and as a request it means what its name says: the caller\nstated none. Ask for it through `StreamRequest::stated_interval` rather than\nmatching this field, so the two spellings of \"nothing was asked for\" cannot become\ntwo behaviours (note **N199**)."
          },
          "pixel_format": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/PixelFormat"
              },
              {
                "type": "null"
              }
            ],
            "description": "Preferred pixel format."
          },
          "width": {
            "description": "Preferred width in pixels.",
            "format": "uint32",
            "minimum": 0,
            "type": [
              "integer",
              "null"
            ]
          }
        },
        "type": "object"
      },
      "StreamStats": {
        "description": "How a stream *delivered*, as distinct from what it delivered (design D16; FR-W4).\n\nUSB-over-IP's characteristic failures — added latency, isochronous bandwidth collapse,\ndropped frames — are visible only in two fields every frame already carries:\n`capture::Frame::sequence`, whose gaps mean dropped frames, and\n`capture::Frame::timestamp_us`, the driver's own clock. Thermal throttling and a\ncontended hub look the same way on an ordinary rig. This is what an accumulator over\nthose two numbers can say about a take, and **nothing here is a judgement**: the stats\nrank and report exactly as D8's metrics do, and deciding whether a stream was \"healthy\"\nbelongs to the consumer whose tolerance it is.\n\nEvery quantity is an integer in microseconds. `imaging::stream_stats::Accumulator`\ncomputes it; the recording path fills `Self::wall_clock_skew_us`, which is the one field\nno pure core could produce because the engine is the only layer that reads a clock.\n\n# The frame contract these numbers are read out of\n\nStated here, and here only, because this is the surface a `--json` consumer holds: the two\nframe fields are not wire types — a `capture::Frame` never leaves the process —\nso their semantics would otherwise live in rustdoc that no committed artifact carries and\nno gate can see (note **N290**). Every backend owes a caller these three sentences. Two of\nthem are claims a stream either honours or breaks, and `testkit::battery::FrameLedger` is\nthe one place they are asserted — pushed into by the conformance battery's stream arm and\nby both real-device stream arms, so the claim rides whichever backend is in front of it.\nThe third is a *reading* rather than a claim: the counters below are where it is answered,\nand nothing in this tree refuses a stream for it (note **N298**).\n\n- **A gap means dropped frames.** `capture::Frame::sequence` advances by one for\n  each frame the driver delivered, so a sequence that jumps from *s* to *s + n* is\n  *n - 1* frames the driver had and this link did not — the arithmetic written out\n  because a consumer implementing it off this sentence must land on the same number the\n  tool reports (note **N298**). Those frames are `Self::frames_dropped`, summed over\n  every jump; the jumps themselves are `Self::gap_events`, counted as runs.\n- **A reset is not a gap.** A sequence number that repeats or goes backwards is a driver\n  restarting its counter, not the four billion frames a wrapping subtraction would invent;\n  it is `Self::sequence_resets` and it joins no drop count (AGENTS rule 6).\n- **Both numbers are per stream, and the clock is the driver's own.**\n  `capture::Frame::timestamp_us` is that clock in microseconds, not this host's.\n  A frame that arrives no later than the one before it contributes no interval and is\n  counted as `Self::clock_reversals` — a finding about the link rather than a broken\n  backend, which is why it is a number here and not a refusal anywhere (D16's last\n  bullet: deciding what a reading means belongs to the consumer). A `STREAMON` may\n  legitimately restart both fields, so no claim here reaches across one.",
        "properties": {
          "clock_reversals": {
            "description": "Frames whose timestamp was not later than the one before it.\n\nA negative interval is not a duration, so it joins no statistic below; the count is\nhere because a clock that ran backwards is a finding about the host, not noise.",
            "format": "uint32",
            "minimum": 0,
            "type": "integer"
          },
          "frames_delivered": {
            "description": "How many frames this accumulator was given.\n\n**Which side of the cap that is, stated rather than left to be discovered** (note\n**N292**): for a take it is the frames the *container took*, because both muxers push\nfrom inside their commit path and a frame a cap refused is refused here too — so a\ntake that ended on `RecordingSummary::cap_reached` reached this process with one\nframe more than this counts, and the summary's own `cap_reached` is what says one did.\nA consumer aggregating a live stream in process pushes every frame it receives and\ngets the literal reading. The difference is that one frame at the end of a capped take\nand nothing at all otherwise, and a forwarding path judged on this number needs to\nknow which reading it is holding.",
            "format": "uint64",
            "minimum": 0,
            "type": "integer"
          },
          "frames_dropped": {
            "description": "How many the driver's sequence numbers say never did.\n\nSummed forward gaps, each one counted as the frames it hides: a jump from *s* to\n*s + n* contributes *n - 1*, so a link that lost nothing contributes nothing. A\nrepeated or backwards sequence number is not a gap — it is a driver doing something\nelse, counted as `Self::sequence_resets` rather than as the four billion frames a\nwrapping subtraction would invent.",
            "format": "uint64",
            "minimum": 0,
            "type": "integer"
          },
          "gap_events": {
            "description": "How many *runs* of dropped frames there were.\n\nBeside `Self::frames_dropped` because one gap of sixty and sixty gaps of one are the\nsame count and different failures: the first is a stall, the second is a link losing\none frame in every two.",
            "format": "uint32",
            "minimum": 0,
            "type": "integer"
          },
          "intervals": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/IntervalStats"
              },
              {
                "type": "null"
              }
            ],
            "description": "What the intervals between consecutive frames looked like, when there were any.\n\n`None` for a take of fewer than two usable frames — which spans no interval at all,\nand reporting zeros there would claim a measurement nobody made."
          },
          "sequence_resets": {
            "description": "Frames whose sequence number did not advance.\n\nA `u32` sequence wraps after four and a half years at 30 fps, so in practice this is a\ndriver restarting its counter — recorded rather than discarded (AGENTS rule 6), and\ndeliberately not folded into the drop count.",
            "format": "uint32",
            "minimum": 0,
            "type": "integer"
          },
          "wall_clock_skew_us": {
            "description": "The driver's span against the caller's own clock, when a caller filled it in.\n\n`None` from a pure accumulator, which sees no wall clock by construction. The\nrecording path fills it — the take's own start and stop stamps against the span the\ndriver's timestamps describe — and a consumer aggregating a live stream in process\nfills it from its own stamps or leaves it absent. Public precisely so it can.",
            "format": "int64",
            "type": [
              "integer",
              "null"
            ]
          }
        },
        "required": [
          "frames_delivered",
          "frames_dropped",
          "gap_events",
          "sequence_resets",
          "clock_reversals"
        ],
        "type": "object"
      },
      "SweepAdjustment": {
        "description": "One way the values a sweep will actually visit differ from what the spec literally asked\nfor.\n\nThe same doctrine as D3's `{requested, applied}`, one layer up: the caller is told what\nhappened to their request instead of diffing two lists to find out.\n\n**It is here rather than in `webcam-handler-engine::sweep` since note N145**, and the move\nis the finding rather than tidying. The planner has always produced these and both\ncomposition roots dropped the whole sweep outcome on the floor, so four adjustment kinds\nand a precision reached no caller for three phases — rubric A8's \"a typed declaration\nnothing reads\", read only by a hardware test. They reach a caller now because they ride on\nthe `sweep_started` progress event *and* are written into the session document beside the\ncontrol they are about; a vocabulary that crosses those seams is the schema's, like every\nother one.\n\nWhy it matters to the consumer AGENTS names: a `--precision 1` sweep of a 10 000-wide\nrange plans 251 samples at a stride of 40, and without this the agent is told \"251\nsamples\" and has no way to learn that the precision it asked for is not the precision it\ngot. Every comparison it then draws from those photos is drawn at a resolution it believes\nis forty times finer than it is.",
        "oneOf": [
          {
            "description": "A requested value sat outside the control's range.",
            "properties": {
              "adjustment": {
                "const": "clamped",
                "type": "string"
              },
              "planned": {
                "description": "What the plan holds.",
                "format": "int64",
                "type": "integer"
              },
              "requested": {
                "description": "What the spec named.",
                "format": "int64",
                "type": "integer"
              }
            },
            "required": [
              "adjustment",
              "requested",
              "planned"
            ],
            "type": "object"
          },
          {
            "description": "A requested value was not a whole number of steps above the control's minimum.",
            "properties": {
              "adjustment": {
                "const": "step_aligned",
                "type": "string"
              },
              "planned": {
                "description": "What the plan holds.",
                "format": "int64",
                "type": "integer"
              },
              "requested": {
                "description": "What the spec named.",
                "format": "int64",
                "type": "integer"
              }
            },
            "required": [
              "adjustment",
              "requested",
              "planned"
            ],
            "type": "object"
          },
          {
            "description": "Values collapsed onto each other after clamping and alignment.",
            "properties": {
              "adjustment": {
                "const": "deduplicated",
                "type": "string"
              },
              "dropped": {
                "description": "How many were dropped.",
                "format": "uint",
                "minimum": 0,
                "type": "integer"
              }
            },
            "required": [
              "adjustment",
              "dropped"
            ],
            "type": "object"
          },
          {
            "description": "A cap trimmed the sweep, and the stride was widened so the plan still spans the whole\nrange rather than stopping part way in.",
            "properties": {
              "adjustment": {
                "const": "capped",
                "type": "string"
              },
              "cap": {
                "$ref": "#/components/schemas/SampleCap",
                "description": "Which cap it was."
              },
              "limit": {
                "description": "The cap that fired.",
                "format": "uint32",
                "minimum": 0,
                "type": "integer"
              },
              "planned": {
                "description": "How many the plan holds.",
                "format": "uint32",
                "minimum": 0,
                "type": "integer"
              },
              "requested": {
                "description": "How many samples the spec would have taken.",
                "format": "uint64",
                "minimum": 0,
                "type": "integer"
              }
            },
            "required": [
              "adjustment",
              "requested",
              "planned",
              "limit",
              "cap"
            ],
            "type": "object"
          }
        ]
      },
      "SweepRequest": {
        "description": "Everything one sweep needs that is not the session, the camera, or the clock.\n\nA DTO rather than an engine struct because three callers need the same request and two of\nthem cannot see the engine: `webcam-handler-cli` parses one from a command line through the\nshared command surface (T4), P4's `calibrate_sweep` takes one off the wire (D10), and\n`webcam-handler-engine::calibrate::run` executes it. The engine re-exports this type rather\nthan defining a second one — a request that had to be translated at each seam is a request\nthree places can disagree about.",
        "properties": {
          "allow_motion": {
            "default": false,
            "description": "Whether a motorized control may be swept at all (design §5: never implicit).",
            "type": "boolean"
          },
          "control": {
            "$ref": "#/components/schemas/ControlSlug",
            "description": "Which control to sweep."
          },
          "photo_format": {
            "$ref": "#/components/schemas/PhotoFormat",
            "default": "jpeg",
            "description": "Which encoding the sample photos are written in."
          },
          "plan": {
            "$ref": "#/components/schemas/SweepSpec",
            "description": "How to derive the values."
          },
          "settle": {
            "$ref": "#/components/schemas/SettlePolicy",
            "default": {
              "deadline_ms": 5000,
              "spec": {
                "frames": 10,
                "kind": "skip_frames"
              }
            },
            "description": "How long to let the sensor settle before each shot [PF:11]."
          },
          "stream": {
            "$ref": "#/components/schemas/StreamRequest",
            "default": {
              "buffer_count": 4
            },
            "description": "What to ask the device's format negotiation for, once per sample."
          }
        },
        "required": [
          "control",
          "plan"
        ],
        "type": "object"
      },
      "SweepSpec": {
        "description": "How to derive the values a sweep visits.",
        "oneOf": [
          {
            "description": "Every step from min to max, up to the 256 samples a sweep may take\n(`limits::MAX_SWEEP_SAMPLES`, checked against this sentence below).\n\nThe number is written out for note **N148**'s reason: this description is published\ninto `schemas/webcam-handler-schema.json`, where a reader has no Rust toolchain to\nresolve a constant with, and \"capped by a name you cannot look up\" tells them a cap\nexists and nothing about what it is.",
            "properties": {
              "kind": {
                "const": "all",
                "type": "string"
              }
            },
            "required": [
              "kind"
            ],
            "type": "object"
          },
          {
            "description": "Every `step`-th value, aligned to the control's own step.",
            "properties": {
              "kind": {
                "const": "uniform",
                "type": "string"
              },
              "step": {
                "description": "The requested spacing.",
                "format": "int64",
                "type": "integer"
              }
            },
            "required": [
              "kind",
              "step"
            ],
            "type": "object"
          },
          {
            "description": "Logarithmically spaced, for controls like exposure time whose useful range\nspans orders of magnitude.",
            "properties": {
              "kind": {
                "const": "log",
                "type": "string"
              },
              "points": {
                "description": "How many samples.",
                "format": "uint32",
                "minimum": 0,
                "type": "integer"
              }
            },
            "required": [
              "kind",
              "points"
            ],
            "type": "object"
          },
          {
            "description": "Exactly these values.",
            "properties": {
              "kind": {
                "const": "explicit",
                "type": "string"
              },
              "values": {
                "description": "The values, in order.",
                "items": {
                  "format": "int64",
                  "type": "integer"
                },
                "type": "array"
              }
            },
            "required": [
              "kind",
              "values"
            ],
            "type": "object"
          }
        ]
      },
      "TakeStatus": {
        "description": "One take, while it runs and after it ends (design D7, D10).\n\nEvery field is a *measurement*, in `RecordingSummary`'s tradition, and the two are not\nthe same document: this one exists while the container is still open, so it carries what\ncan be known mid-take and nothing that cannot. `frames_written` is here and\n`bytes_written` is not — the container's byte count includes trailers written at close\n(`imaging::avi::write::AviWriter::finish` writes the whole `idx1` there), so a running\ntake's file size is not the recording's size and reporting it as one would be a number\nthat changes meaning when the take ends.\n\nThe finished take's full accounting is `RecordReport`, which `record_stop` answers with.",
        "properties": {
          "budget_ms": {
            "description": "How long it may run, from `RecordRequest::budget_ms`.\n\nBeside `TakeStatus::elapsed_ms` so a caller can draw a bar without holding the\nrequest it sent — AGENTS' primary consumer has no hands, and a progress mechanism that\nneeds the client to remember what it asked for is one a restarted client cannot use.",
            "format": "uint64",
            "minimum": 0,
            "type": "integer"
          },
          "elapsed_ms": {
            "description": "How long it has run, on the daemon's monotonic clock.\n\nMonotonic rather than `Stamp::now() - started_at`, because those are two clocks and\nsubtracting one from the other is how an NTP step becomes a duration\n(`RecordReport::wall_clock_ms` argues the same pair).",
            "format": "uint64",
            "minimum": 0,
            "type": "integer"
          },
          "ended": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/RecordingEnd"
              },
              {
                "type": "null"
              }
            ],
            "description": "Why it stopped — `None` while it is still running."
          },
          "failed": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/ErrorKind"
              },
              {
                "type": "null"
              }
            ],
            "description": "The D13 discriminant of the refusal that ended it, when one did.\n\nA **kind and not the error**, for `schema::progress::CalibrationProgress`'s reason one\nstream along: a status document is read by a poller that wants to know whether to stop\npolling, and the error itself belongs to the caller that asked for the take — which is\n`record_stop`, and which answers with the device's own `Error` unchanged. Putting the\nwhole error here as well would give one refusal two homes and let a client act on the\ncopy that is easier to reach rather than on the one the verb returned.\n\n**It does not follow `TakeStatus::ended`, and the two are separate fields because of\nthat rather than in spite of it.** A take the *device* refused ends\n`RecordingEnd::DeviceFailed` and carries the device's kind here. A take that ran its\nduration and whose **container could not be closed** ends\n`RecordingEnd::Duration` — because that is what happened, and rewriting the ending\nbecause a flush refused would lose the one fact the vocabulary carries — and carries\nthe disk's kind here. An ending is a fact about the recording and a refusal is a fact\nabout the whatever refused; AGENTS rule 7 is the line between them, and folding one\ninto the other is the collapse it forbids.\n\nWhat is always true is the other direction: `failed` is `Some` only for a take that has\n**ended**, because nothing fills it in until `TakeStatus::ended` does."
          },
          "format": {
            "$ref": "#/components/schemas/VideoFormat",
            "description": "The container, decided by `VideoFormat::resolve` when the take started.\n\nReported from the first status onwards because a request that named no extension left\nit to the negotiated stream, and an agent that has not enumerated the camera learns\nhere which file it is about to have (see `RecordRequest::container`)."
          },
          "frames_written": {
            "description": "How many frames the container has accepted so far.\n\nFrames *written*, not frames delivered: a frame a cap refused is not in the file, and a\ncount that included it would tell an agent its recording holds a frame it can never\nread. It ends equal to `RecordingSummary::frames_written`, which is a cross-check a\ntest can make rather than a coincidence.",
            "format": "uint32",
            "minimum": 0,
            "type": "integer"
          },
          "negotiated": {
            "$ref": "#/components/schemas/NegotiatedStream",
            "description": "What the device agreed to deliver, with every difference from the request (D5)."
          },
          "path": {
            "description": "The file it is being written to.",
            "type": "string"
          },
          "started_at": {
            "$ref": "#/components/schemas/Stamp",
            "description": "When it started, on the wall clock."
          }
        },
        "required": [
          "path",
          "format",
          "negotiated",
          "started_at",
          "budget_ms",
          "elapsed_ms",
          "frames_written"
        ],
        "type": "object"
      },
      "TerminationReport": {
        "description": "What `terminate_holder` answers when it did signal something (design §5, D10, D13).\n\nKilling a process that holds the camera names its target, so the answer names the\ntarget back. Every field is here because signalling is a *request* and the doctrine\nthat requested is not applied (E4) does not stop at the device: the process may ignore\n`SIGTERM`, may take longer than the re-check, or may have exited between the diagnosis\nand the signal.\n\n`Error::HolderGone` is the refusal for a pid that was not holding the node\nwhen we looked. This is the answer for every case where a signal was actually sent —\nincluding the one where it changed nothing, which `still_held` says out loud rather\nthan leaving the caller to re-run `info` and guess.",
        "properties": {
          "camera": {
            "$ref": "#/components/schemas/CameraId",
            "description": "The camera whose node the pid was holding."
          },
          "holder": {
            "$ref": "#/components/schemas/Holder",
            "description": "Who was signalled, as diagnosed from `/proc/*/fd` *before* the signal — the same\n`Holder` a `Error::Busy` refusal carries, so both verbs name a process\nthe same way."
          },
          "signal": {
            "$ref": "#/components/schemas/TerminationSignal",
            "description": "What was sent."
          },
          "still_held": {
            "description": "Whether the node was still held when we looked again, after a bounded wait.\n\n`true` is not an error and must not be rendered as one: it is the honest answer\nfor a process that ignored `SIGTERM`, and E3's \"availability is not capability\"\nmeans the caller has to be able to tell \"signalled, device now free\" from\n\"signalled, still held\" without guessing.",
            "type": "boolean"
          }
        },
        "required": [
          "camera",
          "holder",
          "signal",
          "still_held"
        ],
        "type": "object"
      },
      "TerminationSignal": {
        "description": "The signals this tool will send to a process holding a camera.\n\nClosed, and with one member, because \"which signal\" is a decision the product\nmakes rather than one a caller supplies: design §5 makes killing a holder \"an\nexplicit command naming its target, never a fallback\", and a wire field that\naccepted an arbitrary signal number would make `terminate_holder` a general-purpose\n`kill` with a camera argument.",
        "oneOf": [
          {
            "const": "term",
            "description": "`SIGTERM`. Asks; does not compel.",
            "type": "string"
          }
        ]
      },
      "Transform": {
        "description": "An orientation transform (the skill's flip and rotate).\n\nOn the verbatim-JPEG sink these become an EXIF Orientation tag — zero re-encode,\nbyte fidelity preserved (E6). On PNG and re-encode sinks they are applied to\npixels.\n\nThe serde spelling and `Transform::as_str` are the same strings on purpose: a\n`--transform` argument and a `\"transform\"` field in a JSON document must name the\nsame thing the same way, and the CLI reaches this vocabulary rather than deriving\na second one of its own.",
        "oneOf": [
          {
            "const": "none",
            "description": "Leave the image as the camera framed it.",
            "type": "string"
          },
          {
            "const": "hflip",
            "description": "Mirror horizontally.",
            "type": "string"
          },
          {
            "const": "vflip",
            "description": "Mirror vertically.",
            "type": "string"
          },
          {
            "const": "rot90",
            "description": "Rotate a quarter turn clockwise.",
            "type": "string"
          },
          {
            "const": "rot180",
            "description": "Rotate a half turn.",
            "type": "string"
          },
          {
            "const": "rot270",
            "description": "Rotate a quarter turn counter-clockwise.",
            "type": "string"
          }
        ]
      },
      "TransformApplication": {
        "description": "Where a requested orientation ended up.\n\n\"The photo is rotated\" and \"the photo says it is rotated\" are different facts, and a\nviewer that ignores EXIF distinguishes them — so the answer records which happened\nrather than leaving the caller to infer it from the sink.",
        "oneOf": [
          {
            "description": "Nothing was asked for.",
            "properties": {
              "kind": {
                "const": "identity",
                "type": "string"
              }
            },
            "required": [
              "kind"
            ],
            "type": "object"
          },
          {
            "description": "The pixels were rotated or mirrored before encoding.",
            "properties": {
              "kind": {
                "const": "pixels",
                "type": "string"
              }
            },
            "required": [
              "kind"
            ],
            "type": "object"
          },
          {
            "description": "The pixels were left alone and the orientation rides in EXIF, so the bitstream\nstays verbatim (E6).",
            "properties": {
              "kind": {
                "const": "exif_orientation",
                "type": "string"
              },
              "orientation": {
                "description": "The tag value `Transform::exif_orientation` produced.",
                "format": "uint16",
                "maximum": 65535,
                "minimum": 0,
                "type": "integer"
              }
            },
            "required": [
              "kind",
              "orientation"
            ],
            "type": "object"
          }
        ]
      },
      "UnrestorableReason": {
        "description": "Why one control could not be put back.",
        "oneOf": [
          {
            "description": "The control has been taken over by automation that did **not** hold it when the\nsnapshot was taken.\n\nA real change we could not undo, and distinct from\n`RestoreOutcome::OwnedByAutomation`, which is the same *flag* meaning the\nopposite thing: there, the owner is the one that was there before.",
            "properties": {
              "automation": {
                "anyOf": [
                  {
                    "$ref": "#/components/schemas/ControlSlug"
                  },
                  {
                    "type": "null"
                  }
                ],
                "description": "The automation partner, when one is known."
              },
              "kind": {
                "const": "still_inactive",
                "type": "string"
              }
            },
            "required": [
              "kind"
            ],
            "type": "object"
          },
          {
            "description": "The control reports VOLATILE: its value is the device's to choose.",
            "properties": {
              "kind": {
                "const": "volatile",
                "type": "string"
              }
            },
            "required": [
              "kind"
            ],
            "type": "object"
          },
          {
            "description": "The control has gone read-only, or vanished, since the snapshot.",
            "properties": {
              "kind": {
                "const": "no_longer_writable",
                "type": "string"
              }
            },
            "required": [
              "kind"
            ],
            "type": "object"
          },
          {
            "description": "The snapshot has no value for it: the device declined to read this control when\nthe snapshot was taken, so there is nothing to write back.\n\nThe one reason here that is decided before any write is attempted, and the reason\nit exists at all: an unrecorded control used to be an absence rather than an\noutcome, and an absence cannot make a report incomplete (note **N195**).",
            "properties": {
              "kind": {
                "const": "never_recorded",
                "type": "string"
              }
            },
            "required": [
              "kind"
            ],
            "type": "object"
          },
          {
            "description": "The write failed. The error rides along so the caller can act on it.",
            "properties": {
              "error": {
                "$ref": "#/components/schemas/Error",
                "description": "What went wrong."
              },
              "kind": {
                "const": "write_failed",
                "type": "string"
              }
            },
            "required": [
              "kind",
              "error"
            ],
            "type": "object"
          }
        ]
      },
      "Unverifiable": {
        "description": "Why a write could not be read back.\n\nBoth members are read off the descriptor the device supplied, so neither is a\nguess about the device's intentions.",
        "oneOf": [
          {
            "const": "type_has_no_value",
            "description": "The control's type has no value: `V4L2_CTRL_TYPE_BUTTON` is a trigger, and\nwriting it *is* the effect.",
            "type": "string"
          },
          {
            "const": "write_only",
            "description": "The device flags the control `WRITE_ONLY`, which is the device saying that\nreading it would not mean anything.",
            "type": "string"
          },
          {
            "const": "device_declined_to_read",
            "description": "The write was accepted and the read-back that should have followed it was\nrefused by the device.\n\nDistinct from the two above because it is not a property of the descriptor:\nnothing said this control was unreadable, and then it was. Several drivers\nanswer `EINVAL` to `G_EXT_CTRLS` for controls they happily enumerate, so this\nis a real outcome rather than a defensive branch — and it is the one case\nwhere a caller might sensibly re-read later.",
            "type": "string"
          }
        ]
      },
      "UsbId": {
        "description": "A USB vendor:product pair.",
        "properties": {
          "product": {
            "description": "Product id.",
            "format": "uint16",
            "maximum": 65535,
            "minimum": 0,
            "type": "integer"
          },
          "vendor": {
            "description": "Vendor id.",
            "format": "uint16",
            "maximum": 65535,
            "minimum": 0,
            "type": "integer"
          }
        },
        "required": [
          "vendor",
          "product"
        ],
        "type": "object"
      },
      "VideoFormat": {
        "description": "The container a recording lands in (design D7).\n\nTwo, and they are not alternatives a caller picks on taste: each one carries a\ndisjoint set of pixel formats, so the container is decided by what the device\nnegotiated. See `VideoFormat::carries` for the law and `VideoFormat::resolve` for\nthe decision every caller makes with it.\n\nThe serde spelling, `VideoFormat::as_str` and `VideoFormat::extension` are the\nsame three strings for the same reason `PhotoFormat`'s are: a `--format y4m`\nargument, a `\"format\":\"y4m\"` field and a `.y4m` filename must name one thing.",
        "oneOf": [
          {
            "const": "avi",
            "description": "RIFF/AVI carrying MJPEG frames verbatim — D7's L0 container, and the product\n(E6): the camera's own bitstream reaches the file untouched.",
            "type": "string"
          },
          {
            "const": "y4m",
            "description": "YUV4MPEG2 carrying raw planes — D7's raw fallback, \"enormous but exact\".",
            "type": "string"
          }
        ]
      },
      "WriteReport": {
        "description": "What `set` answers (design D3/E4).\n\nA list rather than a single `Applied`, because a guarded write is more than one\nwrite: switching an automation partner off is a change to the camera the caller is\nentitled to see, and hiding it would make `--guarded` a verb whose side effects are\nundocumented at the moment they happen.",
        "properties": {
          "camera": {
            "$ref": "#/components/schemas/CameraId",
            "description": "Which camera took them."
          },
          "disabled_automation": {
            "description": "The automation controls that were switched off to make the rest stick.\n\nDerived from the plan rather than from the writes, so it stays a statement about\nintent: a caller restoring the camera afterwards needs the list even when one of\nthe switch-offs was itself adjusted.",
            "items": {
              "$ref": "#/components/schemas/ControlSlug"
            },
            "type": "array"
          },
          "writes": {
            "description": "Every write the plan made, in the order it made them — the automation switch-offs\nincluded, each with its own `{requested, applied}` pair.",
            "items": {
              "$ref": "#/components/schemas/Applied"
            },
            "type": "array"
          }
        },
        "required": [
          "camera",
          "writes"
        ],
        "type": "object"
      },
      "WriteWarning": {
        "description": "A warning that rides a successful write.\n\nNot an error: the driver accepted the write and reported success. But the caller\nasked for one thing and got another, and D13 keeps warnings out of the error registry\nprecisely so a warning is never mistaken for a failure.",
        "oneOf": [
          {
            "description": "The driver clamped the value into range and said \"success\" [PF:6].",
            "properties": {
              "applied": {
                "description": "What the device now holds.",
                "format": "int64",
                "type": "integer"
              },
              "kind": {
                "const": "clamped",
                "type": "string"
              },
              "range": {
                "$ref": "#/components/schemas/ControlRange",
                "description": "The range it was clamped into, as declared at write time."
              },
              "requested": {
                "description": "What we asked for.",
                "format": "int64",
                "type": "integer"
              }
            },
            "required": [
              "kind",
              "requested",
              "applied",
              "range"
            ],
            "type": "object"
          },
          {
            "description": "The driver aligned the value to the control's step.",
            "properties": {
              "applied": {
                "description": "What the device now holds.",
                "format": "int64",
                "type": "integer"
              },
              "kind": {
                "const": "step_aligned",
                "type": "string"
              },
              "requested": {
                "description": "What we asked for.",
                "format": "int64",
                "type": "integer"
              },
              "step": {
                "description": "The step it was aligned to.",
                "format": "int64",
                "type": "integer"
              }
            },
            "required": [
              "kind",
              "requested",
              "applied",
              "step"
            ],
            "type": "object"
          },
          {
            "description": "The value came back different for a reason we cannot attribute. Still reported:\nan unexplained difference is the most interesting kind.",
            "properties": {
              "applied": {
                "$ref": "#/components/schemas/ControlValue",
                "description": "What the device now holds."
              },
              "kind": {
                "const": "adjusted",
                "type": "string"
              },
              "requested": {
                "$ref": "#/components/schemas/ControlValue",
                "description": "What we asked for."
              }
            },
            "required": [
              "kind",
              "requested",
              "applied"
            ],
            "type": "object"
          },
          {
            "description": "The write was accepted and there was nothing to read back, so\n`Applied::applied` is what was *asked for* rather than what was observed.\n\nD3 says every write reads back. Some controls cannot: a `BUTTON` is a trigger with\nno value, and a `WRITE_ONLY` control's value is the device's to keep. Returning\n`{requested, applied}` with the two equal and no warning would make those writes\nindistinguishable from verified ones, which is the collapse E4 exists to prevent —\nso the gap is carried as data instead.",
            "properties": {
              "because": {
                "$ref": "#/components/schemas/Unverifiable",
                "description": "What makes the value unreadable."
              },
              "kind": {
                "const": "unverified",
                "type": "string"
              }
            },
            "required": [
              "kind",
              "because"
            ],
            "type": "object"
          }
        ]
      }
    }
  },
  "info": {
    "description": "The webcam-handler daemon's JSON-RPC surface (design D10, T5), generated from the Rust trait and committed as documentation — never read back, never a second source of truth. The daemon listens on a Unix domain socket (D11), so this document describes methods rather than a URL. Errors are the closed D13 registry under `components/errors`: `code` from a closed numeric range, `message` the error's own rendering, `data` the typed error itself. The daemon also carries server-initiated streams, which OpenRPC 1.3.2 has no shape for: they are described under the `x-subscriptions` extension, reached over a WebSocket upgrade on the same socket, and each one's `item` is the payload of one notification.",
    "license": {
      "name": "MIT OR Apache-2.0"
    },
    "title": "webcam-handler",
    "version": "0.1.0"
  },
  "methods": [
    {
      "description": "Every camera, plus anything worth saying about what is missing (D1).\n\n# Errors\n\nWhatever the backend says. An empty list is not an error — D1's \"an empty\nenumeration is diagnosed, not shrugged at\" is answered by the hints in the list,\nnot by a refusal.",
      "errors": [
        {
          "$ref": "#/components/errors/device_gone"
        },
        {
          "$ref": "#/components/errors/busy"
        },
        {
          "$ref": "#/components/errors/permission_denied"
        },
        {
          "$ref": "#/components/errors/camera_unknown"
        },
        {
          "$ref": "#/components/errors/camera_ambiguous"
        },
        {
          "$ref": "#/components/errors/control_unknown"
        },
        {
          "$ref": "#/components/errors/control_read_only"
        },
        {
          "$ref": "#/components/errors/control_inactive"
        },
        {
          "$ref": "#/components/errors/format_unsupported"
        },
        {
          "$ref": "#/components/errors/settle_timeout"
        },
        {
          "$ref": "#/components/errors/fingerprint_mismatch"
        },
        {
          "$ref": "#/components/errors/session_conflict"
        },
        {
          "$ref": "#/components/errors/illegal_transition"
        },
        {
          "$ref": "#/components/errors/schema_version_foreign"
        },
        {
          "$ref": "#/components/errors/store_locked"
        },
        {
          "$ref": "#/components/errors/holder_gone"
        },
        {
          "$ref": "#/components/errors/device_io"
        },
        {
          "$ref": "#/components/errors/storage_io"
        }
      ],
      "name": "wch_list",
      "paramStructure": "by-name",
      "params": [],
      "result": {
        "name": "CameraList",
        "required": true,
        "schema": {
          "$ref": "#/components/schemas/CameraList"
        }
      },
      "summary": "Every camera, plus anything worth saying about what is missing (D1)."
    },
    {
      "description": "One camera's identity and format tree.\n\n# Errors\n\n`camera_unknown` or `camera_ambiguous` for a\nselector that does not resolve; otherwise whatever the backend says.",
      "errors": [
        {
          "$ref": "#/components/errors/device_gone"
        },
        {
          "$ref": "#/components/errors/busy"
        },
        {
          "$ref": "#/components/errors/permission_denied"
        },
        {
          "$ref": "#/components/errors/camera_unknown"
        },
        {
          "$ref": "#/components/errors/camera_ambiguous"
        },
        {
          "$ref": "#/components/errors/control_unknown"
        },
        {
          "$ref": "#/components/errors/control_read_only"
        },
        {
          "$ref": "#/components/errors/control_inactive"
        },
        {
          "$ref": "#/components/errors/format_unsupported"
        },
        {
          "$ref": "#/components/errors/settle_timeout"
        },
        {
          "$ref": "#/components/errors/fingerprint_mismatch"
        },
        {
          "$ref": "#/components/errors/session_conflict"
        },
        {
          "$ref": "#/components/errors/illegal_transition"
        },
        {
          "$ref": "#/components/errors/schema_version_foreign"
        },
        {
          "$ref": "#/components/errors/store_locked"
        },
        {
          "$ref": "#/components/errors/holder_gone"
        },
        {
          "$ref": "#/components/errors/device_io"
        },
        {
          "$ref": "#/components/errors/storage_io"
        }
      ],
      "name": "wch_info",
      "paramStructure": "by-name",
      "params": [
        {
          "name": "camera",
          "required": true,
          "schema": {
            "$ref": "#/components/schemas/CameraSelector"
          }
        }
      ],
      "result": {
        "name": "CameraDetail",
        "required": true,
        "schema": {
          "$ref": "#/components/schemas/CameraDetail"
        }
      },
      "summary": "One camera's identity and format tree."
    },
    {
      "description": "One camera's control set, and the auto/manual pairs in effect for it.\n\nRead-only: the pairs it reports are the declared table filtered to this device\n(D3's first layer). Measuring pairs on the device is `wch_discover_pairs`,\nwhich is a different method because it writes.\n\n# Errors\n\nAs `wch_info`.",
      "errors": [
        {
          "$ref": "#/components/errors/device_gone"
        },
        {
          "$ref": "#/components/errors/busy"
        },
        {
          "$ref": "#/components/errors/permission_denied"
        },
        {
          "$ref": "#/components/errors/camera_unknown"
        },
        {
          "$ref": "#/components/errors/camera_ambiguous"
        },
        {
          "$ref": "#/components/errors/control_unknown"
        },
        {
          "$ref": "#/components/errors/control_read_only"
        },
        {
          "$ref": "#/components/errors/control_inactive"
        },
        {
          "$ref": "#/components/errors/format_unsupported"
        },
        {
          "$ref": "#/components/errors/settle_timeout"
        },
        {
          "$ref": "#/components/errors/fingerprint_mismatch"
        },
        {
          "$ref": "#/components/errors/session_conflict"
        },
        {
          "$ref": "#/components/errors/illegal_transition"
        },
        {
          "$ref": "#/components/errors/schema_version_foreign"
        },
        {
          "$ref": "#/components/errors/store_locked"
        },
        {
          "$ref": "#/components/errors/holder_gone"
        },
        {
          "$ref": "#/components/errors/device_io"
        },
        {
          "$ref": "#/components/errors/storage_io"
        }
      ],
      "name": "wch_controls",
      "paramStructure": "by-name",
      "params": [
        {
          "name": "camera",
          "required": true,
          "schema": {
            "$ref": "#/components/schemas/CameraSelector"
          }
        }
      ],
      "result": {
        "name": "ControlReport",
        "required": true,
        "schema": {
          "$ref": "#/components/schemas/ControlReport"
        }
      },
      "summary": "One camera's control set, and the auto/manual pairs in effect for it."
    },
    {
      "description": "Measure this camera's auto/manual pairs by asking it (D3's second layer).\n\n**This writes to the camera.** The probe toggles automation-shaped controls, reads\nthe control set back, and restores what it touched. It is its own method rather\nthan a flag on `wch_controls` because the daemon has to route, permission and\ncount it as the write it is — the plan routes the read verbs at P4b and this one\nwith the mutating half at P4c, which is only expressible if they are two methods.\nThe shared command surface keeps its `controls --discover-pairs` flag; a verb with\na flag and an operation with a name are different units, and the engine still has\nexactly one probe.\n\nThe answer is more than a `ControlReport` for the same reason: what the probe\ndeclined to touch, and what it could not put back, are facts about the run that\n`webcam-handler-cli` prints on standard error. A caller that could not see them\nwould be running a write with its restoration report withheld.\n\n# Errors\n\nAs `wch_info`, plus whatever the camera said when the probe could not begin —\na probe that cannot record where the camera started must not start.",
      "errors": [
        {
          "$ref": "#/components/errors/device_gone"
        },
        {
          "$ref": "#/components/errors/busy"
        },
        {
          "$ref": "#/components/errors/permission_denied"
        },
        {
          "$ref": "#/components/errors/camera_unknown"
        },
        {
          "$ref": "#/components/errors/camera_ambiguous"
        },
        {
          "$ref": "#/components/errors/control_unknown"
        },
        {
          "$ref": "#/components/errors/control_read_only"
        },
        {
          "$ref": "#/components/errors/control_inactive"
        },
        {
          "$ref": "#/components/errors/format_unsupported"
        },
        {
          "$ref": "#/components/errors/settle_timeout"
        },
        {
          "$ref": "#/components/errors/fingerprint_mismatch"
        },
        {
          "$ref": "#/components/errors/session_conflict"
        },
        {
          "$ref": "#/components/errors/illegal_transition"
        },
        {
          "$ref": "#/components/errors/schema_version_foreign"
        },
        {
          "$ref": "#/components/errors/store_locked"
        },
        {
          "$ref": "#/components/errors/holder_gone"
        },
        {
          "$ref": "#/components/errors/device_io"
        },
        {
          "$ref": "#/components/errors/storage_io"
        }
      ],
      "name": "wch_discover_pairs",
      "paramStructure": "by-name",
      "params": [
        {
          "name": "camera",
          "required": true,
          "schema": {
            "$ref": "#/components/schemas/CameraSelector"
          }
        }
      ],
      "result": {
        "name": "DiscoveryReport",
        "required": true,
        "schema": {
          "$ref": "#/components/schemas/DiscoveryReport"
        }
      },
      "summary": "Measure this camera's auto/manual pairs by asking it (D3's second layer)."
    },
    {
      "description": "One control's descriptor and current value.\n\nThe whole descriptor rather than the bare value: a value with no range, no flags\nand no menu is not renderable, and an agent reading this needs the same context a\nhuman reading the table does.\n\n# Errors\n\nAs `wch_info`, plus `control_unknown` naming the closest\nslugs this camera does have.",
      "errors": [
        {
          "$ref": "#/components/errors/device_gone"
        },
        {
          "$ref": "#/components/errors/busy"
        },
        {
          "$ref": "#/components/errors/permission_denied"
        },
        {
          "$ref": "#/components/errors/camera_unknown"
        },
        {
          "$ref": "#/components/errors/camera_ambiguous"
        },
        {
          "$ref": "#/components/errors/control_unknown"
        },
        {
          "$ref": "#/components/errors/control_read_only"
        },
        {
          "$ref": "#/components/errors/control_inactive"
        },
        {
          "$ref": "#/components/errors/format_unsupported"
        },
        {
          "$ref": "#/components/errors/settle_timeout"
        },
        {
          "$ref": "#/components/errors/fingerprint_mismatch"
        },
        {
          "$ref": "#/components/errors/session_conflict"
        },
        {
          "$ref": "#/components/errors/illegal_transition"
        },
        {
          "$ref": "#/components/errors/schema_version_foreign"
        },
        {
          "$ref": "#/components/errors/store_locked"
        },
        {
          "$ref": "#/components/errors/holder_gone"
        },
        {
          "$ref": "#/components/errors/device_io"
        },
        {
          "$ref": "#/components/errors/storage_io"
        }
      ],
      "name": "wch_get",
      "paramStructure": "by-name",
      "params": [
        {
          "name": "camera",
          "required": true,
          "schema": {
            "$ref": "#/components/schemas/CameraSelector"
          }
        },
        {
          "name": "control",
          "required": true,
          "schema": {
            "$ref": "#/components/schemas/ControlSlug"
          }
        }
      ],
      "result": {
        "name": "ControlDesc",
        "required": true,
        "schema": {
          "$ref": "#/components/schemas/ControlDesc"
        }
      },
      "summary": "One control's descriptor and current value."
    },
    {
      "description": "Write controls, switching automation off first unless `guarded` is false (D3).\n\nThe answer carries every write the plan made — the automation switch-offs\nincluded — each with its own `{requested, applied}` pair, because requested is not\napplied (E4) and a clamp is a warning on the result rather than an error code\n(D13: a warning with an error code is a success nobody can distinguish from a\nfailure).\n\n# Errors\n\nAs `wch_info`, plus the planner's refusals —\n`control_read_only`, `control_inactive` naming the\nautomation to disable — and the device's.",
      "errors": [
        {
          "$ref": "#/components/errors/device_gone"
        },
        {
          "$ref": "#/components/errors/busy"
        },
        {
          "$ref": "#/components/errors/permission_denied"
        },
        {
          "$ref": "#/components/errors/camera_unknown"
        },
        {
          "$ref": "#/components/errors/camera_ambiguous"
        },
        {
          "$ref": "#/components/errors/control_unknown"
        },
        {
          "$ref": "#/components/errors/control_read_only"
        },
        {
          "$ref": "#/components/errors/control_inactive"
        },
        {
          "$ref": "#/components/errors/format_unsupported"
        },
        {
          "$ref": "#/components/errors/settle_timeout"
        },
        {
          "$ref": "#/components/errors/fingerprint_mismatch"
        },
        {
          "$ref": "#/components/errors/session_conflict"
        },
        {
          "$ref": "#/components/errors/illegal_transition"
        },
        {
          "$ref": "#/components/errors/schema_version_foreign"
        },
        {
          "$ref": "#/components/errors/store_locked"
        },
        {
          "$ref": "#/components/errors/holder_gone"
        },
        {
          "$ref": "#/components/errors/device_io"
        },
        {
          "$ref": "#/components/errors/storage_io"
        }
      ],
      "name": "wch_set",
      "paramStructure": "by-name",
      "params": [
        {
          "name": "camera",
          "required": true,
          "schema": {
            "$ref": "#/components/schemas/CameraSelector"
          }
        },
        {
          "name": "writes",
          "required": true,
          "schema": {
            "items": {
              "$ref": "#/components/schemas/ControlWrite"
            },
            "type": "array"
          }
        },
        {
          "name": "guarded",
          "required": true,
          "schema": {
            "type": "boolean"
          }
        }
      ],
      "result": {
        "name": "WriteReport",
        "required": true,
        "schema": {
          "$ref": "#/components/schemas/WriteReport"
        }
      },
      "summary": "Write controls, switching automation off first unless `guarded` is false (D3)."
    },
    {
      "description": "Every writable control's current value (D4).\n\n# Errors\n\nAs `wch_info`.",
      "errors": [
        {
          "$ref": "#/components/errors/device_gone"
        },
        {
          "$ref": "#/components/errors/busy"
        },
        {
          "$ref": "#/components/errors/permission_denied"
        },
        {
          "$ref": "#/components/errors/camera_unknown"
        },
        {
          "$ref": "#/components/errors/camera_ambiguous"
        },
        {
          "$ref": "#/components/errors/control_unknown"
        },
        {
          "$ref": "#/components/errors/control_read_only"
        },
        {
          "$ref": "#/components/errors/control_inactive"
        },
        {
          "$ref": "#/components/errors/format_unsupported"
        },
        {
          "$ref": "#/components/errors/settle_timeout"
        },
        {
          "$ref": "#/components/errors/fingerprint_mismatch"
        },
        {
          "$ref": "#/components/errors/session_conflict"
        },
        {
          "$ref": "#/components/errors/illegal_transition"
        },
        {
          "$ref": "#/components/errors/schema_version_foreign"
        },
        {
          "$ref": "#/components/errors/store_locked"
        },
        {
          "$ref": "#/components/errors/holder_gone"
        },
        {
          "$ref": "#/components/errors/device_io"
        },
        {
          "$ref": "#/components/errors/storage_io"
        }
      ],
      "name": "wch_snapshot",
      "paramStructure": "by-name",
      "params": [
        {
          "name": "camera",
          "required": true,
          "schema": {
            "$ref": "#/components/schemas/CameraSelector"
          }
        }
      ],
      "result": {
        "name": "Snapshot",
        "required": true,
        "schema": {
          "$ref": "#/components/schemas/Snapshot"
        }
      },
      "summary": "Every writable control's current value (D4)."
    },
    {
      "description": "Put a snapshot back, automation before manual (D4).\n\nTakes the snapshot document itself, not a path: a client reads its own file and\nsends the value, so `webcam-handler-client restore` reads the caller's filesystem\nrather than the daemon's.\n\n# Errors\n\nAs `wch_info`, plus `fingerprint_mismatch` when the snapshot\ncame from a different camera. A control that could not be put back is in the\n*report*, not an error — including the one that is a success, a control whose\nautomation partner owns it now as it did then (note N9).",
      "errors": [
        {
          "$ref": "#/components/errors/device_gone"
        },
        {
          "$ref": "#/components/errors/busy"
        },
        {
          "$ref": "#/components/errors/permission_denied"
        },
        {
          "$ref": "#/components/errors/camera_unknown"
        },
        {
          "$ref": "#/components/errors/camera_ambiguous"
        },
        {
          "$ref": "#/components/errors/control_unknown"
        },
        {
          "$ref": "#/components/errors/control_read_only"
        },
        {
          "$ref": "#/components/errors/control_inactive"
        },
        {
          "$ref": "#/components/errors/format_unsupported"
        },
        {
          "$ref": "#/components/errors/settle_timeout"
        },
        {
          "$ref": "#/components/errors/fingerprint_mismatch"
        },
        {
          "$ref": "#/components/errors/session_conflict"
        },
        {
          "$ref": "#/components/errors/illegal_transition"
        },
        {
          "$ref": "#/components/errors/schema_version_foreign"
        },
        {
          "$ref": "#/components/errors/store_locked"
        },
        {
          "$ref": "#/components/errors/holder_gone"
        },
        {
          "$ref": "#/components/errors/device_io"
        },
        {
          "$ref": "#/components/errors/storage_io"
        }
      ],
      "name": "wch_restore",
      "paramStructure": "by-name",
      "params": [
        {
          "name": "camera",
          "required": true,
          "schema": {
            "$ref": "#/components/schemas/CameraSelector"
          }
        },
        {
          "name": "snapshot",
          "required": true,
          "schema": {
            "$ref": "#/components/schemas/Snapshot"
          }
        }
      ],
      "result": {
        "name": "RestoreReport",
        "required": true,
        "schema": {
          "$ref": "#/components/schemas/RestoreReport"
        }
      },
      "summary": "Put a snapshot back, automation before manual (D4)."
    },
    {
      "description": "Take one photo (D5, D6, D10).\n\nTakes an assembled `PhotoRequest`, never the flags one is built from: the sink\ncarries either \"hand the bytes back\" or an **absolute** server path, and D10 puts\nthe relative-path resolution on the caller's side, before the request is sent. A\nrelative `Sink::ServerPath` arriving here is a request the server must refuse\nrather than resolve against its own working directory, which under systemd is `/`.\nThat rule is not restated here as prose to be remembered: it is\n`schema::capture::Sink::is_addressable`, beside the variants it constrains, and\nthe routing that landed at P4c calls it — before it resolves a camera, so a sink\nthis server cannot address costs nobody a descriptor. Its sibling\n`schema::capture::Sink::writable_format` is asked at the same moment and is the\nreason a path named `.webp` is a refusal rather than JPEG bytes in a file whose\nname says otherwise.\n\nA `ServerPath` sink is a write primitive: any client that can call this can write a\nfile anywhere the daemon's uid can. That is deliberate and it is exactly what D11's\nauthentication model covers — filesystem permissions on a 0700 socket directory —\nand it is why the opt-in TCP listener is token-gated with no flag that removes the\ntoken off loopback.\n\n# Errors\n\nAs `wch_info`, plus `settle_timeout` [PF:11],\n`format_unsupported` when the camera offers nothing that was asked\nfor, `storage_io` from the sink, and\n`illegal_transition` for a request this server will not honour at\nall — a `ServerPath` that is not absolute, or one whose extension names an\nencoding this build does not write, naming the three it does; or a\n`settle.deadline_ms` past 10000 (`schema::limits::MAX_SETTLE_DEADLINE_MS`), which\nis refused rather than clamped for `wch_record_start`'s reason one bound along: one\ncamera is one thread (D12), so a settle is time nobody else on this daemon gets,\nand an agent whose settle was quietly shortened cannot tell that from a sensor\nthat converged. All of them are answered before a camera is resolved and before\nthe destination is opened, so a request this build was never going to honour\ncosts nobody a descriptor and leaves no file where none was (note **N147**).\nDeliberately not `FormatUnsupported`: the camera declined nothing, and `.webp` is\nnot its fault (E3, note N46). Deliberately not `SettleTimeout` either: nothing\nwaited.",
      "errors": [
        {
          "$ref": "#/components/errors/device_gone"
        },
        {
          "$ref": "#/components/errors/busy"
        },
        {
          "$ref": "#/components/errors/permission_denied"
        },
        {
          "$ref": "#/components/errors/camera_unknown"
        },
        {
          "$ref": "#/components/errors/camera_ambiguous"
        },
        {
          "$ref": "#/components/errors/control_unknown"
        },
        {
          "$ref": "#/components/errors/control_read_only"
        },
        {
          "$ref": "#/components/errors/control_inactive"
        },
        {
          "$ref": "#/components/errors/format_unsupported"
        },
        {
          "$ref": "#/components/errors/settle_timeout"
        },
        {
          "$ref": "#/components/errors/fingerprint_mismatch"
        },
        {
          "$ref": "#/components/errors/session_conflict"
        },
        {
          "$ref": "#/components/errors/illegal_transition"
        },
        {
          "$ref": "#/components/errors/schema_version_foreign"
        },
        {
          "$ref": "#/components/errors/store_locked"
        },
        {
          "$ref": "#/components/errors/holder_gone"
        },
        {
          "$ref": "#/components/errors/device_io"
        },
        {
          "$ref": "#/components/errors/storage_io"
        }
      ],
      "name": "wch_photo",
      "paramStructure": "by-name",
      "params": [
        {
          "name": "camera",
          "required": true,
          "schema": {
            "$ref": "#/components/schemas/CameraSelector"
          }
        },
        {
          "name": "request",
          "required": true,
          "schema": {
            "$ref": "#/components/schemas/PhotoRequest"
          }
        }
      ],
      "result": {
        "name": "PhotoResponse",
        "required": true,
        "schema": {
          "$ref": "#/components/schemas/PhotoResponse"
        }
      },
      "summary": "Take one photo (D5, D6, D10)."
    },
    {
      "description": "Start recording `camera` to a file on the host this daemon runs on (D7, D10).\n\n**The first of three, and the only one that takes a request.** A recording is not\none call, and cannot be: this trait's surface is served by a daemon that gives\neach open camera one thread taking one command at a time, so a take written as a\nsingle method would make `wch_record_stop` undeliverable — the stop would queue\nbehind the recording it exists to stop, and only the take's own duration could ever\nend it (note **N111**). So this method starts a take and returns as soon as the\ncontainer's header is on disk, and the take runs on a driver of the daemon's own.\n\nThe answer is the take's first `schema::video::RecordStatus`, which is what makes\nthe sequence usable by a caller that has not enumerated the camera: it names the\ncontainer the negotiation chose and the file the bytes are going to, both of which a\nrequest that left the extension off (see\n`schema::video::RecordRequest::container`) deliberately did not decide.\n\n**A recording is not returned as bytes.** `schema::video::RecordRequest::sink`\ntakes one of D10's two sink variants and refuses the other: a take at its own cap is\nthirty-two times `schema::limits::RPC_MAX_RESPONSE_BYTES` before base64 grows it\nby a third, so `ReturnBytes` names an answer this build cannot send (note **N110**).\nThe path is absolute for `wch_photo`'s reason — this daemon's working directory\nunder systemd is `/` — and `cli_core::Command::record_request` resolves a relative\n`-o` against the *caller's* cwd before the request is sent (D10).\n\n# Errors\n\nAs `wch_info`, plus:\n`illegal_transition` for a request this build will not honour at all\n— a `ReturnBytes` sink, a relative path, an extension naming a container this build\ndoes not write, or a duration past `schema::limits::MAX_RECORDING_MS`, which is\nrefused rather than clamped because an agent whose take was quietly shortened cannot\ntell that from a camera that stopped (note **N46** widened the variant, note\n**N110**).\n`busy` when this camera is **already recording** — which means\n*retry*, and is the honest advice: the take that is running is bounded by its own\nduration. It is also what a camera whose one thread is held by something else\nanswers, unless the request set `wait` (D12).\n`format_unsupported` carrying a `container` payload when the file the\npath names cannot carry what the device negotiated — D7's \"non-MJPG `record` requests\nget Y4M or `FormatUnsupported`\" arriving at a caller who typed `.avi` over a GREY\ncamera. The remedy it names is the **extension**, because the format was chosen by\nD5's ranking rather than typed: `container.carried_by` lists every file this build\nwrites that would have taken those frames, and it names no pixel format, since a\nformat this build records is not a format this camera has (note **N211**).\n`storage_io` from the destination, and the device's own answer for\nanything it refused.",
      "errors": [
        {
          "$ref": "#/components/errors/device_gone"
        },
        {
          "$ref": "#/components/errors/busy"
        },
        {
          "$ref": "#/components/errors/permission_denied"
        },
        {
          "$ref": "#/components/errors/camera_unknown"
        },
        {
          "$ref": "#/components/errors/camera_ambiguous"
        },
        {
          "$ref": "#/components/errors/control_unknown"
        },
        {
          "$ref": "#/components/errors/control_read_only"
        },
        {
          "$ref": "#/components/errors/control_inactive"
        },
        {
          "$ref": "#/components/errors/format_unsupported"
        },
        {
          "$ref": "#/components/errors/settle_timeout"
        },
        {
          "$ref": "#/components/errors/fingerprint_mismatch"
        },
        {
          "$ref": "#/components/errors/session_conflict"
        },
        {
          "$ref": "#/components/errors/illegal_transition"
        },
        {
          "$ref": "#/components/errors/schema_version_foreign"
        },
        {
          "$ref": "#/components/errors/store_locked"
        },
        {
          "$ref": "#/components/errors/holder_gone"
        },
        {
          "$ref": "#/components/errors/device_io"
        },
        {
          "$ref": "#/components/errors/storage_io"
        }
      ],
      "name": "wch_record_start",
      "paramStructure": "by-name",
      "params": [
        {
          "name": "camera",
          "required": true,
          "schema": {
            "$ref": "#/components/schemas/CameraSelector"
          }
        },
        {
          "name": "request",
          "required": true,
          "schema": {
            "$ref": "#/components/schemas/RecordRequest"
          }
        }
      ],
      "result": {
        "name": "RecordStatus",
        "required": true,
        "schema": {
          "$ref": "#/components/schemas/RecordStatus"
        }
      },
      "summary": "Start recording `camera` to a file on the host this daemon runs on (D7, D10)."
    },
    {
      "description": "What `camera`'s recording is doing, or what its last one turned out to be.\n\n**D10's whole progress mechanism**: *\"progress by polling `wch_record_status` — no\nrecording subscription in v1\"*. A client polls this between `wch_record_start` and\n`wch_record_stop` and stops when `schema::video::RecordStatus::is_running` answers\n`false`; `schema::limits::CLIENT_RECORD_POLL_MS` is what\n`webcam-handler-client` polls at.\n\nA camera holds **at most one** take, and this answers about that one: the take that\nis running, or the one that has ended and not been collected by `wch_record_stop`.\nA camera holding neither answers with no take at all, which is the same answer for a\ncamera that has never recorded and for one whose take has been collected — those are\none fact rather than two, because what a caller can do about either is start a\nrecording, and remembering the difference would be this daemon keeping something\nabout a camera after the caller asked for it to be handed over.\n\n**It is a read.** It opens no camera, takes no lock, and never changes what a later\n`wch_record_stop` will answer — so a poll loop that ran twice as fast would cost the\ntake nothing. What it does cost is a live enumeration per call (E2: a camera id is\nresolved against the machine every time), which is why the poll interval is a\nbounded constant rather than as fast as a client can ask.\n\n# Errors\n\nAs `wch_info`. A camera with no recording is **not** an error — that is the answer,\nand a refusal there would make an agent unable to tell \"nothing is recording\" from\n\"this camera is gone\" (AGENTS rule 7).",
      "errors": [
        {
          "$ref": "#/components/errors/device_gone"
        },
        {
          "$ref": "#/components/errors/busy"
        },
        {
          "$ref": "#/components/errors/permission_denied"
        },
        {
          "$ref": "#/components/errors/camera_unknown"
        },
        {
          "$ref": "#/components/errors/camera_ambiguous"
        },
        {
          "$ref": "#/components/errors/control_unknown"
        },
        {
          "$ref": "#/components/errors/control_read_only"
        },
        {
          "$ref": "#/components/errors/control_inactive"
        },
        {
          "$ref": "#/components/errors/format_unsupported"
        },
        {
          "$ref": "#/components/errors/settle_timeout"
        },
        {
          "$ref": "#/components/errors/fingerprint_mismatch"
        },
        {
          "$ref": "#/components/errors/session_conflict"
        },
        {
          "$ref": "#/components/errors/illegal_transition"
        },
        {
          "$ref": "#/components/errors/schema_version_foreign"
        },
        {
          "$ref": "#/components/errors/store_locked"
        },
        {
          "$ref": "#/components/errors/holder_gone"
        },
        {
          "$ref": "#/components/errors/device_io"
        },
        {
          "$ref": "#/components/errors/storage_io"
        }
      ],
      "name": "wch_record_status",
      "paramStructure": "by-name",
      "params": [
        {
          "name": "camera",
          "required": true,
          "schema": {
            "$ref": "#/components/schemas/CameraSelector"
          }
        }
      ],
      "result": {
        "name": "RecordStatus",
        "required": true,
        "schema": {
          "$ref": "#/components/schemas/RecordStatus"
        }
      },
      "summary": "What `camera`'s recording is doing, or what its last one turned out to be."
    },
    {
      "description": "End `camera`'s recording and hand over what it turned out to be (D7, D10).\n\n**Stop *and collect*, which is one verb because they are one question.** A take that\nis running is asked to stop, its container is closed, and the finished\n`schema::video::RecordReport` is the answer; a take that has already reached its\nown bound — the ordinary case, since most takes end on their duration while the\ncaller is still polling — is simply handed over. Either way the camera's slot is\nemptied, so the sequence `wch_record_start` → poll → `wch_record_stop` is total for\nevery ending a take can have, which is what AGENTS' handless primary consumer needs\nof a verb that spans three calls.\n\nThe stop is delivered within one `schema::limits::FRAME_DEADLINE_MS`, because the\ndriver issues one command per frame and is between commands for most of every turn\n(note **N111** — that is the whole reason a recording is a chain of turns).\n\n**A take that failed is answered with the device's own refusal, not with a report.**\nThe container is still closed first, so docs/7 P6b's \"every fault leaves a parseable\nfile\" holds across the wire as it does in process; what the caller gets is the\n`DeviceGone` or the `StorageIo` that ended it, because a refusal arriving as a\nsuccessful recording is the conversion AGENTS rule 7 forbids.\n\n# Errors\n\nAs `wch_info`, plus `illegal_transition` when this camera holds no\ntake at all — deliberately not a bland \"nothing happened\", because an unattended\ncaller has to be able to tell \"my recording is over\" from \"my `wch_record_start`\nnever took\", and those two are the same call's two answers.\n`busy` when a `wch_record_start` for this camera is still\nnegotiating, which means retry for the same reason it does there. Otherwise whatever\nended the take: the device's answer, or `storage_io` naming the\nfile.",
      "errors": [
        {
          "$ref": "#/components/errors/device_gone"
        },
        {
          "$ref": "#/components/errors/busy"
        },
        {
          "$ref": "#/components/errors/permission_denied"
        },
        {
          "$ref": "#/components/errors/camera_unknown"
        },
        {
          "$ref": "#/components/errors/camera_ambiguous"
        },
        {
          "$ref": "#/components/errors/control_unknown"
        },
        {
          "$ref": "#/components/errors/control_read_only"
        },
        {
          "$ref": "#/components/errors/control_inactive"
        },
        {
          "$ref": "#/components/errors/format_unsupported"
        },
        {
          "$ref": "#/components/errors/settle_timeout"
        },
        {
          "$ref": "#/components/errors/fingerprint_mismatch"
        },
        {
          "$ref": "#/components/errors/session_conflict"
        },
        {
          "$ref": "#/components/errors/illegal_transition"
        },
        {
          "$ref": "#/components/errors/schema_version_foreign"
        },
        {
          "$ref": "#/components/errors/store_locked"
        },
        {
          "$ref": "#/components/errors/holder_gone"
        },
        {
          "$ref": "#/components/errors/device_io"
        },
        {
          "$ref": "#/components/errors/storage_io"
        }
      ],
      "name": "wch_record_stop",
      "paramStructure": "by-name",
      "params": [
        {
          "name": "camera",
          "required": true,
          "schema": {
            "$ref": "#/components/schemas/CameraSelector"
          }
        }
      ],
      "result": {
        "name": "RecordReport",
        "required": true,
        "schema": {
          "$ref": "#/components/schemas/RecordReport"
        }
      },
      "summary": "End `camera`'s recording and hand over what it turned out to be (D7, D10)."
    },
    {
      "description": "One camera's full device profile (T3).\n\n`capturer` is provenance: a profile records who took it, because a transcription\nand a probe are different claims about a device (E1, E2).\n\n`discover_pairs` runs D3's empirical probe before the reading and folds what it\nmeasured into `invariant.measured_pairs`. It **writes to the camera** — every\nautomation-shaped control is toggled through its positions and put back — which is\nwhy it is a parameter a caller supplies rather than something this method decides.\nA profile is a corpus entry, and whether the device was perturbed to produce one is\na fact the document's own reader needs, so it belongs to the call (note **N239**).\n`false` leaves the pair set empty, which means *this capture measured none* and\nnever *this device has none*. The probe's other two answers — what it declined, and\nwhat the restore achieved — are `wch_discover_pairs`', and a caller that needs them\nasks that verb.\n\n# Errors\n\nAs `wch_info`, plus, when the probe was asked for, its refusal to begin: a camera\nwhose state cannot be recorded is not probed, so nothing is written to it.",
      "errors": [
        {
          "$ref": "#/components/errors/device_gone"
        },
        {
          "$ref": "#/components/errors/busy"
        },
        {
          "$ref": "#/components/errors/permission_denied"
        },
        {
          "$ref": "#/components/errors/camera_unknown"
        },
        {
          "$ref": "#/components/errors/camera_ambiguous"
        },
        {
          "$ref": "#/components/errors/control_unknown"
        },
        {
          "$ref": "#/components/errors/control_read_only"
        },
        {
          "$ref": "#/components/errors/control_inactive"
        },
        {
          "$ref": "#/components/errors/format_unsupported"
        },
        {
          "$ref": "#/components/errors/settle_timeout"
        },
        {
          "$ref": "#/components/errors/fingerprint_mismatch"
        },
        {
          "$ref": "#/components/errors/session_conflict"
        },
        {
          "$ref": "#/components/errors/illegal_transition"
        },
        {
          "$ref": "#/components/errors/schema_version_foreign"
        },
        {
          "$ref": "#/components/errors/store_locked"
        },
        {
          "$ref": "#/components/errors/holder_gone"
        },
        {
          "$ref": "#/components/errors/device_io"
        },
        {
          "$ref": "#/components/errors/storage_io"
        }
      ],
      "name": "wch_profile_capture",
      "paramStructure": "by-name",
      "params": [
        {
          "name": "camera",
          "required": true,
          "schema": {
            "$ref": "#/components/schemas/CameraSelector"
          }
        },
        {
          "name": "capturer",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "discover_pairs",
          "required": true,
          "schema": {
            "type": "boolean"
          }
        }
      ],
      "result": {
        "name": "DeviceProfile",
        "required": true,
        "schema": {
          "$ref": "#/components/schemas/DeviceProfile"
        }
      },
      "summary": "One camera's full device profile (T3)."
    },
    {
      "description": "Signal the process holding a camera's node (design §5).\n\nNames both the camera and the pid, and is never a fallback behaviour of anything\nelse: nothing in this surface kills a process to get a device free. The answer says\nwhat was sent and whether the node was still held afterwards, because signalling is\na request and a process may ignore it — E4's doctrine does not stop at the device.\n\n# Errors\n\nAs `wch_info`, plus `holder_gone` when the pid no longer\nholds the device, which is a refusal rather than a no-op: signalling a pid that has\nbeen recycled would kill somebody else's process.",
      "errors": [
        {
          "$ref": "#/components/errors/device_gone"
        },
        {
          "$ref": "#/components/errors/busy"
        },
        {
          "$ref": "#/components/errors/permission_denied"
        },
        {
          "$ref": "#/components/errors/camera_unknown"
        },
        {
          "$ref": "#/components/errors/camera_ambiguous"
        },
        {
          "$ref": "#/components/errors/control_unknown"
        },
        {
          "$ref": "#/components/errors/control_read_only"
        },
        {
          "$ref": "#/components/errors/control_inactive"
        },
        {
          "$ref": "#/components/errors/format_unsupported"
        },
        {
          "$ref": "#/components/errors/settle_timeout"
        },
        {
          "$ref": "#/components/errors/fingerprint_mismatch"
        },
        {
          "$ref": "#/components/errors/session_conflict"
        },
        {
          "$ref": "#/components/errors/illegal_transition"
        },
        {
          "$ref": "#/components/errors/schema_version_foreign"
        },
        {
          "$ref": "#/components/errors/store_locked"
        },
        {
          "$ref": "#/components/errors/holder_gone"
        },
        {
          "$ref": "#/components/errors/device_io"
        },
        {
          "$ref": "#/components/errors/storage_io"
        }
      ],
      "name": "wch_terminate_holder",
      "paramStructure": "by-name",
      "params": [
        {
          "name": "camera",
          "required": true,
          "schema": {
            "$ref": "#/components/schemas/CameraSelector"
          }
        },
        {
          "name": "pid",
          "required": true,
          "schema": {
            "format": "int32",
            "type": "integer"
          }
        }
      ],
      "result": {
        "name": "TerminationReport",
        "required": true,
        "schema": {
          "$ref": "#/components/schemas/TerminationReport"
        }
      },
      "summary": "Signal the process holding a camera's node (design §5)."
    },
    {
      "description": "Open a calibration session for a camera and a task (D8).\n\n# Errors\n\nAs `wch_info`, plus `session_conflict` when the slot already\nholds an open session (note N14), and whatever the store refuses with —\n`store_locked` when another process owns the state directory (D9).",
      "errors": [
        {
          "$ref": "#/components/errors/device_gone"
        },
        {
          "$ref": "#/components/errors/busy"
        },
        {
          "$ref": "#/components/errors/permission_denied"
        },
        {
          "$ref": "#/components/errors/camera_unknown"
        },
        {
          "$ref": "#/components/errors/camera_ambiguous"
        },
        {
          "$ref": "#/components/errors/control_unknown"
        },
        {
          "$ref": "#/components/errors/control_read_only"
        },
        {
          "$ref": "#/components/errors/control_inactive"
        },
        {
          "$ref": "#/components/errors/format_unsupported"
        },
        {
          "$ref": "#/components/errors/settle_timeout"
        },
        {
          "$ref": "#/components/errors/fingerprint_mismatch"
        },
        {
          "$ref": "#/components/errors/session_conflict"
        },
        {
          "$ref": "#/components/errors/illegal_transition"
        },
        {
          "$ref": "#/components/errors/schema_version_foreign"
        },
        {
          "$ref": "#/components/errors/store_locked"
        },
        {
          "$ref": "#/components/errors/holder_gone"
        },
        {
          "$ref": "#/components/errors/device_io"
        },
        {
          "$ref": "#/components/errors/storage_io"
        }
      ],
      "name": "wch_calibrate_start",
      "paramStructure": "by-name",
      "params": [
        {
          "name": "camera",
          "required": true,
          "schema": {
            "$ref": "#/components/schemas/CameraSelector"
          }
        },
        {
          "name": "task",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "goal",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "criteria",
          "required": true,
          "schema": {
            "items": {
              "type": "string"
            },
            "type": "array"
          }
        }
      ],
      "result": {
        "name": "Session",
        "required": true,
        "schema": {
          "$ref": "#/components/schemas/Session"
        }
      },
      "summary": "Open a calibration session for a camera and a task (D8)."
    },
    {
      "description": "Queue controls for calibration, or reorder the queue.\n\n`controls` empty means every control the camera has. `order` treats the named\ncontrols as the queue's new order rather than as additions.\n\n# Errors\n\nAs `wch_calibrate_start`, plus `control_unknown` for a slug\nthis camera does not have and `illegal_transition` when `order` is\nnot a permutation of the queue.",
      "errors": [
        {
          "$ref": "#/components/errors/device_gone"
        },
        {
          "$ref": "#/components/errors/busy"
        },
        {
          "$ref": "#/components/errors/permission_denied"
        },
        {
          "$ref": "#/components/errors/camera_unknown"
        },
        {
          "$ref": "#/components/errors/camera_ambiguous"
        },
        {
          "$ref": "#/components/errors/control_unknown"
        },
        {
          "$ref": "#/components/errors/control_read_only"
        },
        {
          "$ref": "#/components/errors/control_inactive"
        },
        {
          "$ref": "#/components/errors/format_unsupported"
        },
        {
          "$ref": "#/components/errors/settle_timeout"
        },
        {
          "$ref": "#/components/errors/fingerprint_mismatch"
        },
        {
          "$ref": "#/components/errors/session_conflict"
        },
        {
          "$ref": "#/components/errors/illegal_transition"
        },
        {
          "$ref": "#/components/errors/schema_version_foreign"
        },
        {
          "$ref": "#/components/errors/store_locked"
        },
        {
          "$ref": "#/components/errors/holder_gone"
        },
        {
          "$ref": "#/components/errors/device_io"
        },
        {
          "$ref": "#/components/errors/storage_io"
        }
      ],
      "name": "wch_calibrate_plan",
      "paramStructure": "by-name",
      "params": [
        {
          "name": "camera",
          "required": true,
          "schema": {
            "$ref": "#/components/schemas/CameraSelector"
          }
        },
        {
          "name": "session",
          "required": true,
          "schema": {
            "$ref": "#/components/schemas/SessionRef"
          }
        },
        {
          "name": "controls",
          "required": true,
          "schema": {
            "items": {
              "$ref": "#/components/schemas/ControlSlug"
            },
            "type": "array"
          }
        },
        {
          "name": "order",
          "required": true,
          "schema": {
            "type": "boolean"
          }
        }
      ],
      "result": {
        "name": "Session",
        "required": true,
        "schema": {
          "$ref": "#/components/schemas/Session"
        }
      },
      "summary": "Queue controls for calibration, or reorder the queue."
    },
    {
      "description": "Sweep one control and record a sample per value (D8).\n\nRequest and response, with no progress parameter: the live events are\n`schema::progress::ProgressEvent`s and P4e puts them on their own subscription\nrather than threading a watcher through a call. Until then a client's progress bar\nsimply does not move, which is honest; a boolean or a subscription id in this\nrequest would be a wire field with no producer and a second progress vocabulary\nforever.\n\nThis is the one method whose latency is unbounded by design — a sweep is minutes of\ncamera time — so a client's request timeout has to be raised or disabled at\nconnection setup rather than left at jsonrpsee's 60-second default.\n\n# Errors\n\nAs `wch_calibrate_start`, plus the planner's refusals (a control with no\nordered range; a motorized control without `allow_motion`, because §5 says a plan\nthat would move motors says so first) and whatever the camera said at the sample\nthat stopped it. `illegal_transition` also for a\n`settle.deadline_ms` past 10000 (`schema::limits::MAX_SETTLE_DEADLINE_MS`), which\nis answered before the camera is resolved — a sweep that reached its first sample\nwould have armed a pre-sweep snapshot, switched an automation partner off and\nmoved a motor before finding out this build was never going to honour the request\n(note **N147**).",
      "errors": [
        {
          "$ref": "#/components/errors/device_gone"
        },
        {
          "$ref": "#/components/errors/busy"
        },
        {
          "$ref": "#/components/errors/permission_denied"
        },
        {
          "$ref": "#/components/errors/camera_unknown"
        },
        {
          "$ref": "#/components/errors/camera_ambiguous"
        },
        {
          "$ref": "#/components/errors/control_unknown"
        },
        {
          "$ref": "#/components/errors/control_read_only"
        },
        {
          "$ref": "#/components/errors/control_inactive"
        },
        {
          "$ref": "#/components/errors/format_unsupported"
        },
        {
          "$ref": "#/components/errors/settle_timeout"
        },
        {
          "$ref": "#/components/errors/fingerprint_mismatch"
        },
        {
          "$ref": "#/components/errors/session_conflict"
        },
        {
          "$ref": "#/components/errors/illegal_transition"
        },
        {
          "$ref": "#/components/errors/schema_version_foreign"
        },
        {
          "$ref": "#/components/errors/store_locked"
        },
        {
          "$ref": "#/components/errors/holder_gone"
        },
        {
          "$ref": "#/components/errors/device_io"
        },
        {
          "$ref": "#/components/errors/storage_io"
        }
      ],
      "name": "wch_calibrate_sweep",
      "paramStructure": "by-name",
      "params": [
        {
          "name": "camera",
          "required": true,
          "schema": {
            "$ref": "#/components/schemas/CameraSelector"
          }
        },
        {
          "name": "session",
          "required": true,
          "schema": {
            "$ref": "#/components/schemas/SessionRef"
          }
        },
        {
          "name": "request",
          "required": true,
          "schema": {
            "$ref": "#/components/schemas/SweepRequest"
          }
        }
      ],
      "result": {
        "name": "Session",
        "required": true,
        "schema": {
          "$ref": "#/components/schemas/Session"
        }
      },
      "summary": "Sweep one control and record a sample per value (D8)."
    },
    {
      "description": "A session's document and its history.\n\n# Errors\n\nAs `wch_info`, plus `schema_version_foreign` for a session\nanother build wrote (D9) — a foreign document is a typed refusal, never a\nbest-effort parse.",
      "errors": [
        {
          "$ref": "#/components/errors/device_gone"
        },
        {
          "$ref": "#/components/errors/busy"
        },
        {
          "$ref": "#/components/errors/permission_denied"
        },
        {
          "$ref": "#/components/errors/camera_unknown"
        },
        {
          "$ref": "#/components/errors/camera_ambiguous"
        },
        {
          "$ref": "#/components/errors/control_unknown"
        },
        {
          "$ref": "#/components/errors/control_read_only"
        },
        {
          "$ref": "#/components/errors/control_inactive"
        },
        {
          "$ref": "#/components/errors/format_unsupported"
        },
        {
          "$ref": "#/components/errors/settle_timeout"
        },
        {
          "$ref": "#/components/errors/fingerprint_mismatch"
        },
        {
          "$ref": "#/components/errors/session_conflict"
        },
        {
          "$ref": "#/components/errors/illegal_transition"
        },
        {
          "$ref": "#/components/errors/schema_version_foreign"
        },
        {
          "$ref": "#/components/errors/store_locked"
        },
        {
          "$ref": "#/components/errors/holder_gone"
        },
        {
          "$ref": "#/components/errors/device_io"
        },
        {
          "$ref": "#/components/errors/storage_io"
        }
      ],
      "name": "wch_calibrate_status",
      "paramStructure": "by-name",
      "params": [
        {
          "name": "camera",
          "required": true,
          "schema": {
            "$ref": "#/components/schemas/CameraSelector"
          }
        },
        {
          "name": "session",
          "required": true,
          "schema": {
            "$ref": "#/components/schemas/SessionRef"
          }
        }
      ],
      "result": {
        "name": "SessionStatus",
        "required": true,
        "schema": {
          "$ref": "#/components/schemas/SessionStatus"
        }
      },
      "summary": "A session's document and its history."
    },
    {
      "description": "Record a control's chosen value and who chose it (D8).\n\n# Errors\n\nAs `wch_calibrate_start`, plus the D8 machine's refusals: a control that\nnever swept, a value no sample holds, a metric that cannot rank.",
      "errors": [
        {
          "$ref": "#/components/errors/device_gone"
        },
        {
          "$ref": "#/components/errors/busy"
        },
        {
          "$ref": "#/components/errors/permission_denied"
        },
        {
          "$ref": "#/components/errors/camera_unknown"
        },
        {
          "$ref": "#/components/errors/camera_ambiguous"
        },
        {
          "$ref": "#/components/errors/control_unknown"
        },
        {
          "$ref": "#/components/errors/control_read_only"
        },
        {
          "$ref": "#/components/errors/control_inactive"
        },
        {
          "$ref": "#/components/errors/format_unsupported"
        },
        {
          "$ref": "#/components/errors/settle_timeout"
        },
        {
          "$ref": "#/components/errors/fingerprint_mismatch"
        },
        {
          "$ref": "#/components/errors/session_conflict"
        },
        {
          "$ref": "#/components/errors/illegal_transition"
        },
        {
          "$ref": "#/components/errors/schema_version_foreign"
        },
        {
          "$ref": "#/components/errors/store_locked"
        },
        {
          "$ref": "#/components/errors/holder_gone"
        },
        {
          "$ref": "#/components/errors/device_io"
        },
        {
          "$ref": "#/components/errors/storage_io"
        }
      ],
      "name": "wch_calibrate_select",
      "paramStructure": "by-name",
      "params": [
        {
          "name": "camera",
          "required": true,
          "schema": {
            "$ref": "#/components/schemas/CameraSelector"
          }
        },
        {
          "name": "session",
          "required": true,
          "schema": {
            "$ref": "#/components/schemas/SessionRef"
          }
        },
        {
          "name": "control",
          "required": true,
          "schema": {
            "$ref": "#/components/schemas/ControlSlug"
          }
        },
        {
          "name": "selection",
          "required": true,
          "schema": {
            "$ref": "#/components/schemas/Selection"
          }
        }
      ],
      "result": {
        "name": "Session",
        "required": true,
        "schema": {
          "$ref": "#/components/schemas/Session"
        }
      },
      "summary": "Record a control's chosen value and who chose it (D8)."
    },
    {
      "description": "Write a session's calibrated values back to a camera, automation first (D4's\nordering, D8's gate).\n\n# Errors\n\nAs `wch_calibrate_start`, plus `fingerprint_mismatch` naming\nthe fields that differ when the camera is not the one the session was recorded\nagainst, and `illegal_transition` when the session still has\nuncalibrated work and `partial` is false.",
      "errors": [
        {
          "$ref": "#/components/errors/device_gone"
        },
        {
          "$ref": "#/components/errors/busy"
        },
        {
          "$ref": "#/components/errors/permission_denied"
        },
        {
          "$ref": "#/components/errors/camera_unknown"
        },
        {
          "$ref": "#/components/errors/camera_ambiguous"
        },
        {
          "$ref": "#/components/errors/control_unknown"
        },
        {
          "$ref": "#/components/errors/control_read_only"
        },
        {
          "$ref": "#/components/errors/control_inactive"
        },
        {
          "$ref": "#/components/errors/format_unsupported"
        },
        {
          "$ref": "#/components/errors/settle_timeout"
        },
        {
          "$ref": "#/components/errors/fingerprint_mismatch"
        },
        {
          "$ref": "#/components/errors/session_conflict"
        },
        {
          "$ref": "#/components/errors/illegal_transition"
        },
        {
          "$ref": "#/components/errors/schema_version_foreign"
        },
        {
          "$ref": "#/components/errors/store_locked"
        },
        {
          "$ref": "#/components/errors/holder_gone"
        },
        {
          "$ref": "#/components/errors/device_io"
        },
        {
          "$ref": "#/components/errors/storage_io"
        }
      ],
      "name": "wch_calibrate_apply",
      "paramStructure": "by-name",
      "params": [
        {
          "name": "camera",
          "required": true,
          "schema": {
            "$ref": "#/components/schemas/CameraSelector"
          }
        },
        {
          "name": "session",
          "required": true,
          "schema": {
            "$ref": "#/components/schemas/SessionRef"
          }
        },
        {
          "name": "partial",
          "required": true,
          "schema": {
            "type": "boolean"
          }
        }
      ],
      "result": {
        "name": "WriteReport",
        "required": true,
        "schema": {
          "$ref": "#/components/schemas/WriteReport"
        }
      },
      "summary": "Write a session's calibrated values back to a camera, automation first (D4's ordering, D8's gate)."
    },
    {
      "description": "Put the camera back where this session found it, from its pre-sweep snapshot\n(note N23).\n\nThe eighth calibrate verb, and the one that makes D4's \"leave the camera as you\nfound it\" true for a session rather than only for a single write. Running it twice\nis not an error: the second time there is nothing left to put back, and an empty\nreport says so.\n\n# Errors\n\nAs `wch_calibrate_start`, plus `fingerprint_mismatch` when\nthe camera is not the session's.",
      "errors": [
        {
          "$ref": "#/components/errors/device_gone"
        },
        {
          "$ref": "#/components/errors/busy"
        },
        {
          "$ref": "#/components/errors/permission_denied"
        },
        {
          "$ref": "#/components/errors/camera_unknown"
        },
        {
          "$ref": "#/components/errors/camera_ambiguous"
        },
        {
          "$ref": "#/components/errors/control_unknown"
        },
        {
          "$ref": "#/components/errors/control_read_only"
        },
        {
          "$ref": "#/components/errors/control_inactive"
        },
        {
          "$ref": "#/components/errors/format_unsupported"
        },
        {
          "$ref": "#/components/errors/settle_timeout"
        },
        {
          "$ref": "#/components/errors/fingerprint_mismatch"
        },
        {
          "$ref": "#/components/errors/session_conflict"
        },
        {
          "$ref": "#/components/errors/illegal_transition"
        },
        {
          "$ref": "#/components/errors/schema_version_foreign"
        },
        {
          "$ref": "#/components/errors/store_locked"
        },
        {
          "$ref": "#/components/errors/holder_gone"
        },
        {
          "$ref": "#/components/errors/device_io"
        },
        {
          "$ref": "#/components/errors/storage_io"
        }
      ],
      "name": "wch_calibrate_restore",
      "paramStructure": "by-name",
      "params": [
        {
          "name": "camera",
          "required": true,
          "schema": {
            "$ref": "#/components/schemas/CameraSelector"
          }
        },
        {
          "name": "session",
          "required": true,
          "schema": {
            "$ref": "#/components/schemas/SessionRef"
          }
        }
      ],
      "result": {
        "name": "RestoreReport",
        "required": true,
        "schema": {
          "$ref": "#/components/schemas/RestoreReport"
        }
      },
      "summary": "Put the camera back where this session found it, from its pre-sweep snapshot (note N23)."
    },
    {
      "description": "Every session on this machine, or one camera's, newest first.\n\n`camera` is the only optional parameter on this surface: `null`, or the key left\nout altogether, both mean every session. Measured against a real `RpcModule` —\n`{\"params\":{}}` and `{\"params\":{\"camera\":null}}` are answered identically —\nbecause serde resolves a missing `Option` field through `missing_field`, which\nvisits `None`. The emitted document says the same thing by marking this parameter\n`required: false`, which is derived from its schema rather than written twice.\n\n# Errors\n\nAs `wch_info` when a camera is named; otherwise whatever the store says. A\nsession whose document this build cannot read still *lists* — listing parses\nnothing (D9).",
      "errors": [
        {
          "$ref": "#/components/errors/device_gone"
        },
        {
          "$ref": "#/components/errors/busy"
        },
        {
          "$ref": "#/components/errors/permission_denied"
        },
        {
          "$ref": "#/components/errors/camera_unknown"
        },
        {
          "$ref": "#/components/errors/camera_ambiguous"
        },
        {
          "$ref": "#/components/errors/control_unknown"
        },
        {
          "$ref": "#/components/errors/control_read_only"
        },
        {
          "$ref": "#/components/errors/control_inactive"
        },
        {
          "$ref": "#/components/errors/format_unsupported"
        },
        {
          "$ref": "#/components/errors/settle_timeout"
        },
        {
          "$ref": "#/components/errors/fingerprint_mismatch"
        },
        {
          "$ref": "#/components/errors/session_conflict"
        },
        {
          "$ref": "#/components/errors/illegal_transition"
        },
        {
          "$ref": "#/components/errors/schema_version_foreign"
        },
        {
          "$ref": "#/components/errors/store_locked"
        },
        {
          "$ref": "#/components/errors/holder_gone"
        },
        {
          "$ref": "#/components/errors/device_io"
        },
        {
          "$ref": "#/components/errors/storage_io"
        }
      ],
      "name": "wch_calibrate_list",
      "paramStructure": "by-name",
      "params": [
        {
          "name": "camera",
          "required": false,
          "schema": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/CameraSelector"
              },
              {
                "type": "null"
              }
            ]
          }
        }
      ],
      "result": {
        "name": "SessionList",
        "required": true,
        "schema": {
          "$ref": "#/components/schemas/SessionList"
        }
      },
      "summary": "Every session on this machine, or one camera's, newest first."
    }
  ],
  "openrpc": "1.3.2",
  "x-d13-error-data": {
    "$ref": "#/components/schemas/Error"
  },
  "x-subscriptions": [
    {
      "description": "Device nodes appearing and disappearing, as the kernel reports them (D10, P4d).\n\nOne `HotplugEvent` per node, never per camera: \"re-enumeration decides what\ncamera it belongs to — the event names a node, never a camera, because grouping\nis not a node property\" (`schema::backend::HotplugEvent`). A client that wants\nthe camera list calls `wch_list` afterwards, which enumerates live (E2).\n\nThe daemon's watch is a **diff of successive readings of the node tree** (note\nN53), so it inherits that shape's one honest hole: a node that leaves and\nreturns entirely between two readings is not reported at all. It opens no node,\nbecause subscribing to events is not a use of a camera (D12, N53).\n\n**A subscriber that falls too far behind is closed rather than resynced**, and\nthat is the opposite of what `wch_subscribe_calibration` does one method down.\nA `HotplugEvent` is a *delta*: a gap makes a consumer's picture of the node tree\nwrong in a way it cannot detect, and there is no variant in this closed\nvocabulary that means \"you missed some\". Ending the stream with the count is the\nonly answer that does not hand over a quiet lie — the client re-subscribes and\nre-enumerates.\n\n# Errors\n\nWhatever the backend refuses `CameraBackend::watch` with — typically\n`device_io` when the uevent socket cannot be opened, which is a\ncontainer or an LSM rather than a missing privilege [PF:21]. The watch is\nstarted on the first subscription rather than at startup, deliberately: a\ndaemon that refused to *start* because it could not watch would be answering\n`wch_list` with nothing on a host where enumeration works perfectly (E3).",
      "errors": [
        {
          "$ref": "#/components/errors/device_gone"
        },
        {
          "$ref": "#/components/errors/busy"
        },
        {
          "$ref": "#/components/errors/permission_denied"
        },
        {
          "$ref": "#/components/errors/camera_unknown"
        },
        {
          "$ref": "#/components/errors/camera_ambiguous"
        },
        {
          "$ref": "#/components/errors/control_unknown"
        },
        {
          "$ref": "#/components/errors/control_read_only"
        },
        {
          "$ref": "#/components/errors/control_inactive"
        },
        {
          "$ref": "#/components/errors/format_unsupported"
        },
        {
          "$ref": "#/components/errors/settle_timeout"
        },
        {
          "$ref": "#/components/errors/fingerprint_mismatch"
        },
        {
          "$ref": "#/components/errors/session_conflict"
        },
        {
          "$ref": "#/components/errors/illegal_transition"
        },
        {
          "$ref": "#/components/errors/schema_version_foreign"
        },
        {
          "$ref": "#/components/errors/store_locked"
        },
        {
          "$ref": "#/components/errors/holder_gone"
        },
        {
          "$ref": "#/components/errors/device_io"
        },
        {
          "$ref": "#/components/errors/storage_io"
        }
      ],
      "item": {
        "name": "HotplugEvent",
        "required": true,
        "schema": {
          "$ref": "#/components/schemas/HotplugEvent"
        }
      },
      "name": "wch_subscribe_events",
      "notification": "wch_subscribe_events",
      "summary": "Device nodes appearing and disappearing, as the kernel reports them (D10, P4d).",
      "unsubscribe": "wch_unsubscribe_events"
    },
    {
      "description": "Every calibration sweep's live progress, from every session (D8, D10, P3c).\n\n`schema::progress::ProgressEvent` verbatim and no second vocabulary: the same\nevents `webcam-handler-cli calibrate sweep` renders its progress bar from, which is\nwhat docs/7's risk register asked P3c to guarantee. Each carries its session id and\neach in-flight variant carries `index`/`total`, so a client that connects mid-sweep\ncan paint a truthful bar from the first event it sees.\n\n**Every session, filtered by the consumer.** The stream is per client, not per\nsession; `ProgressEvent::session` is what a client with one session in mind\nmatches on. A session parameter would resolve a `SessionRef` at subscribe time\nagainst a store lock this path has no business taking, and a\n`SessionRef::Task` subscription would silently follow whichever session\noccupied the slot next.\n\n**A subscriber that falls behind loses events and is told how many** — the\nopposite of `wch_subscribe_events`, for a reason in the payload rather than in\nthe transport: `index`/`total` ride on every in-flight variant, so a gap is\nself-healing and the next event repaints a correct bar. The alternative,\nclosing the stream, would end a client's view of a twenty-minute sweep because\nit was briefly slow. Nothing a subscriber does reaches the sweep: the camera is\nnever held for a progress bar's benefit (note N17).\n\nA sweep that *fails* does not fail this stream. The refusal arrives as an\nevent — `CalibrationProgress::SweepInterrupted`, carrying the D13 discriminant\n— because the sweep's own caller is the one being refused.\n\n# Errors\n\nNothing, today: the events come from this daemon's own sweeps rather than from\na device, so there is nothing here to refuse with. The method still answers a\n`codes::WireError` on rejection because that is the one shape a refusal on\nthis surface may take.",
      "errors": [
        {
          "$ref": "#/components/errors/device_gone"
        },
        {
          "$ref": "#/components/errors/busy"
        },
        {
          "$ref": "#/components/errors/permission_denied"
        },
        {
          "$ref": "#/components/errors/camera_unknown"
        },
        {
          "$ref": "#/components/errors/camera_ambiguous"
        },
        {
          "$ref": "#/components/errors/control_unknown"
        },
        {
          "$ref": "#/components/errors/control_read_only"
        },
        {
          "$ref": "#/components/errors/control_inactive"
        },
        {
          "$ref": "#/components/errors/format_unsupported"
        },
        {
          "$ref": "#/components/errors/settle_timeout"
        },
        {
          "$ref": "#/components/errors/fingerprint_mismatch"
        },
        {
          "$ref": "#/components/errors/session_conflict"
        },
        {
          "$ref": "#/components/errors/illegal_transition"
        },
        {
          "$ref": "#/components/errors/schema_version_foreign"
        },
        {
          "$ref": "#/components/errors/store_locked"
        },
        {
          "$ref": "#/components/errors/holder_gone"
        },
        {
          "$ref": "#/components/errors/device_io"
        },
        {
          "$ref": "#/components/errors/storage_io"
        }
      ],
      "item": {
        "name": "ProgressEvent",
        "required": true,
        "schema": {
          "$ref": "#/components/schemas/ProgressEvent"
        }
      },
      "name": "wch_subscribe_calibration",
      "notification": "wch_subscribe_calibration",
      "summary": "Every calibration sweep's live progress, from every session (D8, D10, P3c).",
      "unsubscribe": "wch_unsubscribe_calibration"
    }
  ]
}
