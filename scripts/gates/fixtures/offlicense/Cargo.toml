# A workspace whose graph carries a copyleft license, for the failing arm of
# `license-allowlist.sh`. Not a member of the repository workspace; never compiled.
[workspace]
resolver = "3"
members = ["consumer", "copyleft-fixture"]
