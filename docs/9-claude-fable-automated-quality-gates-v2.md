# webcam-handler — Automated quality gates (v2)

Doc 9 in the webcam-handler series, **v2 — first revision**. Supersedes docs/4 (v1, now
under `docs/historical/`). Gates for docs/8 (rubric
v2, Part D), consumed by docs/7's phase gates. Convention inherited from the predecessor
and in force from the first commit: **the repository's files are
authoritative; this document records the commissioned set, deltas, and rationale.** A
drift between this document and the repo's files is a defect in whichever one the
evidence says is wrong. This revision brings the record up to the G2-closed tree: Part 1
describes the suite as built (including where deviations N1, N3 and N5 moved it), Part
2's commissioning table strikes what landed, and the gaps register is regenerated.

**The structural rule this suite is built around** (predecessor's costliest lesson,
adopted here from day one rather than learned again): *a gate written to close a defect
is not itself tested against its own inverse, and the second arm is where it fails.*
Therefore: every multi-line CI predicate lives in `scripts/gates/*.sh`; all of them run
under one table-driven `scripts/gates/selftest.sh` that requires **both directions per
gate** — a predicate with no failing case fails the selftest, and one with only failing
cases fails it too. Compiled predicates (Rust tests standing in for a shell gate) are
deviations to record in the implementation notes where they happen, not silent
exceptions. Second structural rule, same provenance: **checks must not name locations
they can drift from** — populations are derived (from `cargo metadata`, from a `find`,
from an exhaustive match), never transcribed lists. Third structural rule, paid for
locally this time [S:N10]: **the inverse arm is driven by the thing under test, not by a
model of it** — `counted-selections.sh` shipped green-by-construction because its
failing arm used a stub returning a shape the real tool never produces; where a stub is
unavoidable (a seeded table is cheaper than a rebuilt workspace), one arm still runs the
real tool, and that arm is what notices when a tool release changes shape.

## Part 1 — The suite as built (P0 baseline + P1/P2 accretions)

Landed with the workspace scaffold and grown since; G0 required all of it green and
self-tested, and every later gate runs it again (`just ci` includes `gates`).

### `just ci` (offline, ≡ CI by construction)

- `cargo fmt --check`; `cargo clippy --workspace --all-targets -- -D warnings` with the
  banned-API set below; `cargo nextest run --workspace` with `--no-tests=fail` and
  `retries = 0`; `cargo doc` build; `cargo deny check bans licenses sources` (advisories
  live in the networked job — keeps `ci` offline); `typos`; `cargo machete`;
  `shellcheck` over `scripts/` **including `scripts/gates/`**; `--locked` everywhere.
- `ci.yml` runs `just ci` verbatim. Divergences between local and CI are recorded in this
  document when they appear; the target count is zero (the daemon needs no CI-resident
  credentials — this project's advantage over its predecessor).

### clippy configuration (lint gates)

- `disallowed-methods`: `v4l::device::Device::query_controls` (panics on unknown control
  types — PF:1; our raw layer is the home), `std::process::exit` outside `main`,
  `std::thread::sleep` **workspace-wide** (deviation N3: `clippy.toml` has no notion of
  test-vs-not, and a global ban with named `#[expect(..., reason)]` exceptions is
  auditable where a test-only ban is not; there are no production sleep sites yet).
- `disallowed-types`: none at P0; grows with evidence.
- On device/request-driven paths (enforced by module-scoped lint levels):
  `unwrap_used`, `expect_used`, `panic`, `indexing_slicing`, `as_conversions`.

**The lint policy's home is `[workspace.lints]`** (deviation N1 — one table in the root
`Cargo.toml`, every crate opting in with `[lints] workspace = true`), not the
thirteen-copy crate-root attribute block v1 wrote. The policy: deny
`unsafe_op_in_unsafe_fn`, `clippy::undocumented_unsafe_blocks`,
`clippy::missing_safety_doc`, `clippy::multiple_unsafe_ops_per_block` (one obligation
per SAFETY), `clippy::allow_attributes`, `clippy::allow_attributes_without_reason`.
Cargo's table cannot express `cfg(test)`, so the two suppression-hygiene lints bind
test code as well — strictly *stronger* than the v1 text: test suppressions also write
`#[expect(..., reason = "...")]`. One attribute cannot live in the table:
`#![forbid(unsafe_code)]` stays a crate-root attribute on **every crate except
`webcam-handler-v4l2`** (which must not have it; its `unsafe` is confined to `src/sys/`
by `unsafe-scope.sh` below), and the gate asserts each root carries it — the copies are
gate-checked, not trusted. `webcam-handler-v4l2` additionally denies
`clippy::cast_possible_truncation`, `cast_sign_loss`, `cast_possible_wrap` — the
`try_from`-not-`as` rule as a lint on the one crate that reads kernel-shaped integers.

### `scripts/gates/` — the predicate population, as built

| Predicate | Catches | Both-direction cases |
|---|---|---|
| `license-allowlist.sh` (drives `cargo deny`) | any copyleft/off-list license entering the tree; each named ban (`v4l-sys`, `v4l2-sys`, `udev`/`libudev-sys`/`tokio-udev`, `libcamera*`, `alsa*`/`cpal`, `colored`, `minimp4`, `env-libvpx-sys`/`vpx-encode`/`webm`, `ffmpeg-next`/`-sys`, `x264`/`x264-dev`/`x265`, `dssim`, `jpeg-encoder`/`turbojpeg`/`mozjpeg`, `option-ext` — N2's `directories` door; the authoritative population is `deny.toml`, each entry carrying its reason) | a scratch manifest **with a committed lockfile** carrying a banned dep must fail under `cargo deny --offline` (the selftest never resolves the network — that is how the failing arm stays inside an offline `just ci`); the shipped tree must pass |
| `feature-posture.sh` | `v4l` with non-default features (the `libv4l` LGPL door); `image` with default features (the `avif`→rav1e drag); any TLS feature (CDLA cert stores); checked from `cargo metadata`, not manifest grep | a scratch manifest flipping each posture must fail |
| `dependency-walls.sh` | T6 violations from `cargo metadata`, populations derived from §2.8's edge list: tokio/axum/hyper linked by `schema`/`imaging`/`fake`/`cli-core`/`engine` (`engine` joined at P4b — §2.1 puts the camera actor there and §2.8 gives the engine no runtime, which is the whole reason `engine::actor` names no reply channel of its own [S:N41]; membership means linkage and nothing else, so the engine still owns the state directory); **`api`'s own wall — no axum, no hyper, no tower-http, tokio allowed** (deviation N5: one `#[rpc(server, client)]` trait links tokio necessarily; re-run N5's measurement on any jsonrpsee bump — and since P4b that wall is live rather than hypothetical, because the daemon's `jsonrpsee-server` edge puts hyper in the graph [S:N38]); backends linked by anything but the two composition roots (`webcam-handler-cli`, `webcam-handler-daemon`); `client` linking a backend or the engine (the thin-client wall); **`webcam-handler-web`'s own wall, added at P5a — neither stack, so no tokio *and* no axum/hyper/tower-http**, its own row rather than a name on an existing list because the pure wall does not cover `tower-http` and the wire wall allows tokio for a reason (N5) that has nothing to say about an asset crate: what that crate may not have is a reason to serve anything, and both halves of that arrive as a linkage edge (design §2.10 — "what these files are" is its law, "how bytes become an HTTP response" is the daemon's); V4L2 types escaping the backend (grep for `v4l::`/`v4l_sys::` outside `crates/backends/v4l2/`) | a scratch edge in each direction — and for the asset wall four, because the two halves arrive by different routes and neither implies the other: a server edge (`rust-embed`'s `axum-ex` feature is one manifest line away), a runtime edge, `tower-http` alone (the part in *neither* list this crate could have been added to, which is the only thing that makes a union-spelled wall worth spelling), and the crate leaving the workspace, since a wall whose subject is no longer a member stops being able to fail and stops silently |
| `uds-permissions.sh` (P4b) | a world-usable camera daemon socket: it starts a real `webcam-handler-daemon` against scratch XDG directories, learns from the daemon's own stderr that it is serving, and asserts the directory it served from is `0700` and owned by the user who ran it, that something was actually bound there, that a second uid cannot walk in — **where the host offers one, else a named counted skip**, and only after the *parent* is proven reachable, because `mktemp -d` is itself `0700` and would otherwise answer for the daemon [S:N44] — that a `webcam-handler-daemon` handed a `0755` directory refuses to serve and leaves the mode alone, and that a `webcam-handler-daemon` handed a **symlink** where its socket directory goes refuses however private the target is — because `stat` and `create_dir_all` both follow links, so a by-path check passes while the daemon serves from wherever the name currently points [S:N39] | `$WCH_GATE_WCHD` points the predicate at daemon-shaped programs that get D11 wrong one way each (umask decided the mode; announced a socket it never bound; repaired a widened directory instead of refusing; checked the mode by path so a symlink answered for its target; never started); `pass_case` drives the shipped `webcam-handler-daemon` (rubric rule 6 [S:N10]) |
| `unsafe-scope.sh` | the token `unsafe` outside `crates/backends/v4l2/src/sys/` (rubric B10 [V]); the allowed path is derived from the tree, and the other crates' `#![forbid(unsafe_code)]` roots are asserted present. **Third claim added at P4d:** the *residual-`unsafe` register* — `sys/mod.rs`'s counted table of one row per block, and the sentence stating its totals — is reconciled against the region it describes, in both directions. Rubric B11 treats a count written in prose as a claim like any other, and P4d is what made this one load-bearing: it put a kernel-facing module (`sys::uevent`) inside the exempt region and added **no** row, because `rustix` is a safe wrapper, so "the count below is unchanged by it" became a sentence a reader had no way to check and the next edge written with `libc` would have made quietly false. Both sides are walked — the tree's tokens, split by whether they open an `unsafe impl`, against the table's own rows — so nothing here is transcribed. The summary's totals may be digits or English words up to twenty; a spelling the predicate cannot read is a failure rather than a pass, because an unreadable count is the state the claim exists to prevent, not a milder version of it | a seeded unsafe block elsewhere must fail; a missing `forbid` root must fail; the sys module must pass — and, since P4d, must pass *with its register row*, which is the shape a legitimate new block lands in; a block with no row must fail, a row naming no block must fail, a summary disagreeing with its own table must fail, a summary spelled "several" must fail, and the section losing its heading must fail |
| `kill-is-never-a-fallback.sh` (P4c) | a second way to signal a camera's holder: the signal has one home (`webcam-handler-v4l2::holders::terminate`, forwarding to the one `unsafe` block in `sys/signal.rs`) and exactly one caller outside the backend crate — the daemon's `terminate_holder`. AGENTS' "never a fallback" is an **absence**, and an integration suite can only witness the verbs it drives, so a `Busy` retry added to `wch_photo` would leave every test green [S:N48] | a seeded second caller in the engine and one in the daemon must fail; a tree where nothing calls it must fail (the verb would not signal); a home that stopped defining `terminate` or lost `sys/signal.rs` must fail; a comment that names the signal without calling it must pass |
| `token-comparison-has-one-home.sh` (P5a) | the web listener's bearer token compared anywhere but in the one place shaped to compare it safely, and the type acquiring an operator that would short-circuit on it (D11, design §2.10, AGENTS "Hardware and privacy"). **It exists because a mutant survived**: replacing `daemon::http::token::Token::verify`'s constant-time body with an equality on the exposed secret leaves every test in this workspace passing, since the answer is identical for every input and the only thing that changes is how long the daemon takes to say no — `token.rs`'s own doc says so, as a limit rather than a promise, and rule 1 has no exemption for a defect class that is hard to see. It cannot measure a clock, so it holds the shape the timing argument stands on, in four claims. **(1) The secret has one reader**: `expose_secret` outside `token.rs` appears only inside `#[cfg(test)]` — the token gate is what this is chiefly about, because a comparison written in `gate.rs` needs the secret in hand, so the accessor in that file's product code is the defect spelled out one line before the `==`. **(2) The type refuses `==`**: no `PartialEq`, `Eq`, `Hash` or `Display` for `Token` anywhere in the workspace, derived or hand-written — `str`'s `PartialEq` compares lengths and then bytes and returns at the first difference, and a `Display` is how a secret reaches a comparison or a log line without anybody typing the word `expose`. **(3) `Debug` is hand-written and names no field**, with the field names read out of the declaration rather than typed into the predicate; only half of this is the gate's, since *what* the hand-written impl prints is asserted by a test in `token.rs` that can go red, and what the gate adds is the half that test cannot see — a derive would satisfy nothing there and would still have to be caught before it printed. **(4) Something still calls the comparison**, which is `kill-is-never-a-fallback.sh`'s "the only caller went away" arm about a different absence: every claim above is true of a tree where the gate stopped checking the token. Line comments are stripped before matching, so **prose does not count** — a decision made differently from `kill-is-never-a-fallback.sh`, and deliberately, because what defends the timing claim *is* the argument beside the code and a gate that turned writing about the secret into a violation would push that argument out of the modules that need it. Test code is everything from a file's one `#[cfg(test)]` marker to the end; a file this cannot classify is a **failure and not a pass**, `unsafe-scope.sh`'s price for a count it cannot read. Populations are derived (the daemon's directory from `cargo metadata`, the files from the tree walk, `Token`'s fields from its declaration) and every name it treats as policy is asserted to exist before anything is counted, so a rename, a deletion or a move fails it rather than quietly emptying it. Its honest limit is in the gaps below | sixteen fail arms and two green ones. The arm the row is named for is `fail_case_the_gate_compares_the_secret_itself`, which seeds the surviving mutant itself — one `token.verify(candidate)` rewritten as an equality on `expose_secret` — and if any arm in the file stops going red that is the one to look at first. Then, per claim: a new module reaching for the secret in product code; a file carrying two `#[cfg(test)]` markers, so the product/test boundary has no answer; the accessor renamed, which would leave the confinement quantifying over nothing; a hand-written `PartialEq`, a derived one, and a `Display`; a derived `Debug`, a hand-written one that prints the field, and the hand-written impl going away entirely; the gate no longer calling the comparison; the comparison no longer declared; and four ways for the subject itself to vanish — the type renamed, either module moved, and the daemon leaving the workspace. The green arms are the shipped tree and `pass_case_a_test_may_hold_the_secret`, which matters for the mirror-image reason: the daemon's own suite cannot assert that an equal-length near miss is refused without building one out of the token, so every reader of the secret outside its home is a test, and a confinement gate that refused those would be a gate somebody turns off |
| `web-routes-are-gated.sh` (owner ruling, 2026-08-12) | a route the web listener serves that D11's bearer token is **not** over. It exists because a ruling took a property away: the gate used to be `Router::layer` over one router, so "every route is gated" was true by construction, and the owner then ruled that the static assets are open-source code rather than a secret and are served without authentication — leaving `route_layer` over the routes and a *list*, `daemon::http::CAMERA_BEARING_PATHS`, where a shape used to be. A camera-bearing route added later without being named on it is a live camera served to strangers and is **green in this workspace**, because a test can only ask for a path somebody named: that is `kill-is-never-a-fallback.sh`'s absence argument one transport along, and it is why this is a predicate and not a test. Four claims. **(1) The list is real**: it exists, is non-empty, and names constants rather than literals, each declared in the daemon's `http` tree — a path typed into the list as a literal is a second spelling of it, and the copy that would stop matching is the router's. **(2) Every route names one of them**: `.route(`, `.route_service(`, `.nest(` and `.nest_service(` in the workspace's product code must pass a listed constant from inside the daemon's `http` modules; a route on a literal, on a constant nobody listed, or in another crate is the defect. **(3) The ungated half is the asset fallback and nothing else**: `.fallback(`/`.fallback_service(` appears once, in the composition, naming the asset handler — the door claim 2 does not watch, since a fallback is exactly what is now served without a credential. **(4) The gate is still installed over the routes**: `gate::check` exactly once, in the composition, with exactly one `route_layer(` beside it, because every claim above is true both of a tree that stopped gating anything and of a tree that put the gate back over everything — and the owner ruled on both. Prose and test code do not count (`lib.sh`'s `gate_product_lines`, shared with `token-comparison-has-one-home.sh` so that two predicates cannot disagree about what a file's test half is), and a file whose boundary cannot be read is a failure rather than a pass. Its honest limit is in the gaps below | sixteen fail arms, four green. The arm the row is named for is `fail_case_a_new_route_module_nobody_put_on_the_list` — a route on a path of its own in the daemon's own tree with the list left alone, which is what the defect looks like in a diff — and the same shape again spelled tidily, with a `pub const` beside it. Then, per claim: a route registered in another crate; a file with two `#[cfg(test)]` markers that registers a route, so its product half has no answer; a second fallback; a fallback that stops naming the assets; `Router::layer` back in place of `route_layer`, which is the ruling undone and would gate the client's own source; the gate not installed at all; a route that installs a gate of its own; the list emptied, spelled with literals, naming a path nothing declares, and renamed away; the composition module deleted; no route registered at all; and the daemon leaving the workspace. The green arms are the shipped tree, a comment that names a route without registering one — the modules this is about argue about routing at length and a gate that forbade the argument would push it out of them — `pass_case_a_test_may_build_a_router_of_its_own`, and a declaration rustfmt wrapped onto two lines when a third path joined it — a formatting event that must never be a finding, which is note **N60**'s bill charged to a predicate whose subject is a security boundary |
| `testkit-is-dev-only.sh` | `webcam-handler-testkit` on a normal edge (it may grow tooling deps that must never ship) | non-vacuity arm: fails if nothing dev-depends on it |
| `atomic-write-home.sh` | `serde_json::to_writer`/`fs::write` targeting the state dir *or D9's session files* outside `webcam-handler-engine::store` (rubric A5: bypassing the home); and, since P3a, the home itself failing to show D9's atomic sequence or the one `fd-lock` | a scratch bypass must fail — including one that names only `log.ndjson` and never `state_dir`, which is what the P3a widening bought; a home with no `write_json_atomic`, no rename, or no lock must fail; a missing home must fail; an empty population must fail (no more named skip); the shipped tree passes, and so must a home split across `src/store/` |
| `no-frame-bytes-in-repo.sh` | committed camera frames: content-sniffs the tree; each format has one permitted home — `corpus/images/` for PNG/JPEG, `crates/imaging/fixtures/avi/` for AVI (**delta at P6a**), `crates/imaging/fixtures/y4m/` for Y4M (**delta at P6b**) — and a fixture there must carry a `generated-by` marker in its bytes or a sidecar and declare a frame extent below the cap. An AVI is walked to its `avih` for that extent and a Y4M's `W`/`H` are read off its header line, since a container has no SOF0. **Two containers arrived invisible to this gate in two consecutive phases** — a `RIFF`/`AVI ` envelope and a `YUV4MPEG2 ` header each matched no image magic number, and the Y4M case is the worse of the two because its payload *is* the picture, one 8-bit luma sample per byte — so the format list is a decision somebody makes when a container lands, not a thing that grows on its own | a scratch JPEG without provenance must fail; a provenanced synthetic must pass; four `fail_case_avi_*` and five `fail_case_y4m_*` arms cover the container half, the fifth being a Y4M parked in the AVI directory |
| `no-external-fetch-in-web.sh` | CDN/script-src URLs, `fetch(` to non-relative origins, and `<script src` with a scheme in `webcam-handler-web` assets. Anchors are deliberately *not* violations: a link the user may click is not a subresource the page loads. **Since P5a the empty case is a failure rather than a named skip** — all ten rules quantify over the files the walk finds, so a walk that finds none makes every one of them vacuously true, and until the assets existed the emptiness was the honest answer and was printed as a counted skip | one seeded violation per pattern; the P5a non-vacuity arms landed with the assets that made them assertable — an emptied asset directory and a deleted one both fail, since `gate_find` walks what is there and says nothing about what is not |
| `schema-artifacts-current.sh` | committed generated artifacts drifting from what emits them (re-runs xtask emit, diffs) — the JSON Schema bundle against the `webcam-handler-schema` types, and, since P4a, the OpenRPC document against the T5 trait. The predicate names no filename: it walks the emitted tree and the committed directory, so a new artifact joins the comparison by being written. An **empty** artifact directory is a failure, not a named skip: the "nothing is committed yet" branch stopped being reachable when the bundle landed at P2, and everything that reaches it now (a bad merge, a dropped rebase, a `.gitignore` edit) is the drift this gate exists to catch | a scratch type edit must fail; a hand-edited or uncommitted artifact must fail; a wire method renamed in `crates/api` without regenerating must fail; a `schemas/` with nothing in it must fail; clean tree passes |
| `agent-guide-current.sh` (P6e) | the generated agent usage guide drifting from the command surface it teaches: it re-runs `xtask generate` into scratch and diffs `docs/agent-guide.md`, with the guide's path read out of xtask's own `GUIDE_PATH` rather than written in the script — the trick `schema-artifacts-current.sh` uses for `ARTIFACT_DIR`. It is a **second** predicate rather than a widening of that one because that gate's population is a *directory* and the guide is a single file outside it; moving the guide into `schemas/` to be covered for free would put a Markdown manual where its reader does not look. Second claim: the deprecation pointer beside the vendored skill names the guide's real path — `vendor/v4l2-webcam-skill/` is a submodule, so the pointer lives beside it in this repository, and a forwarding address that has stopped forwarding is worse than none | a hand-edited guide must fail; an uncommitted one must fail; a generator that writes no guide must fail; **a value name renamed in `crates/cli-core` with nothing under `docs/` touched must fail** — the arm the row below actually commissions; a pointer naming another path, and no pointer at all, must each fail; clean tree passes |
| `counted-selections.sh` | gate recipes whose test filters select zero (selections compare `(package, binary)` pairs; the `grep -c`-under-`pipefail` trap — a zero count exits nonzero — is known and avoided by construction throughout `scripts/`). **Corrected once already [S:N10]**: `cargo nextest list -T json` lists the whole workspace whatever the filter says, so the count is `filter-match.status == "matches"`, never `test-count` — the original predicate could not count to zero and was green by construction from the day it shipped | a recipe naming a nonexistent test must fail; the selftest includes a **real-tool arm** (the actual nextest over a filter matching nothing), because the stubbed arm is what hid the defect |
| `msrv-sync.sh` | the one MSRV fact diverging across `Cargo.toml`s and CI | seeded divergence |
| `ignored-suites-have-recipes.sh` | every `#[ignore]`d suite named by a `just` recipe, every recipe selecting a real `(package, binary)`, and every declared `wch-suite:` prefix present in the `exclusive-device` nextest group (so a rung cannot be added or renamed without the serialization following it). Its test-group half reads only the group's own overrides and whole-prefix matches, both directions — the P2 review found it matching prefixes anywhere in a filter, including subtracted ones [S:E4] | both halves seeded |
| `corpus-floor.sh` (P1) | dead corpus: the profile directory empty, a committed profile no test reaches, or a corpus nobody *replays* (parsing is not replaying) | all three claims seeded both ways |
| `json-validates.sh` (P1) | a renderer that wraps the answer in an envelope, adds a field, or hand-builds an object — checked by running the built `webcam-handler-cli` over the fake and validating **every verb `--help` offers, subcommands included** (writes included, performed against the replayed fake) against `#/$defs/<type>` in the committed bundle; the population is scraped at both levels — `webcam-handler-cli --help`, then `webcam-handler-cli <verb> --help` for each — and a row must match a leaf verb's name exactly, so the population cannot silently shrink; its honest limit is in the gaps below | seeded violation per pattern; clean tree passes |
| `privileged-helper.sh` (with N8) | the blessed helper widening: any product crate depending on `crates/priv/`, or the blessed copy's mode wider than `0700` (a restore or `chmod -R` can widen it long after the bless — the mode *is* the security boundary, §2.13). **Two claims added at P6e** and the first was impossible until then: **(5)** no verb hands the binary's capabilities to a program its caller names — held over the source text, where a second route that never reached clap would be, while `main.rs` holds the same claim over the clap tree. Until the G6 reckoning deleted `exec` this was not a claim but an accepted risk stated in the predicate's own header (notes N8, **N125**). **(6)** every capability-carrying file under `.wch-bin/` is the blessed helper, carrying **exactly** the blessing `caps.rs` declares — a copy carrying *more* is privilege nobody argued for, and a capability-carrying file that is not the helper is note **N126**: the `wch-priv` the N90 rename orphaned, root-capable and carrying the deleted verb, which every name-keyed check looked straight past for two days | both directions; the last four arms drive the `$WCH_GATE_GETCAP` seam, because setting a capability needs the privilege this gate contains, and two of them are transcripts of this machine on 2026-08-15 rather than shapes somebody imagined |
| `systemd-units.sh` (P4e-ii) | shipped unit files that stopped agreeing with the binary. A unit file is the one artifact in this project that cannot ask a crate what a constant is — it is text under `~/.config/systemd/user/`, read by a manager that has never heard of `schema::limits` — so every value in it is a *transcription*, which is the class the derived-population rule exists for. Four claims, each re-derived from the tree rather than read and nodded at: **`Type=notify`, never `Type=forking`, and no `PIDFile=`** (there is no `fork` in the workspace, so a unit claiming otherwise would leave systemd tracking and signalling the wrong process; it is also what makes `NotifyAccess=main` sufficient and D9's lock record name the pid systemd knows); **`SocketMode=0600` and `DirectoryMode=0700`**, against systemd's *default* `DirectoryMode` of 0755 — D11 makes filesystem permissions the whole authentication model, so a socket unit that left it alone would create a directory every account on the machine can walk into; **`ListenStream=` ending at the socket the daemon looks for**, composed here from `schema::paths::APP_DIR` and `schema::limits::DAEMON_SOCKET_FILE`, so a rename in either crate is red rather than a unit that binds a socket nothing connects to; and **`TimeoutStopSec` exceeding `limits::DAEMON_SHUTDOWN_DRAIN_MS`** — the pair that can only be wrong together, because whichever is smaller is the bound that fires, and if it is systemd's the daemon is SIGKILLed mid-drain and every sentence about *how* it stops describes a path nothing takes. Its honest limit is in the gaps below: it is text over files, so it does not claim systemd would accept the units | ten failing arms, one per way to get a unit wrong: a service that claims to fork; one that writes a PID file; a socket directory anyone can walk into; a world-writable socket inode; a `ListenStream=` where no client looks; one in the abstract namespace; a stop timeout shorter than the daemon's drain; the same pair broken from the *other* side (a drain the unit's timeout cannot fit); a stop timeout in units nothing can read; and a tree with no unit files at all, so the gate cannot go green by checking nothing. Two green arms: the shipped tree, and a stop timeout written in systemd's *other* spellings (`1min`, then bare `45`), both longer than the daemon's drain, because a predicate that understood only the spelling this repository happens to use would silently stop checking the pair the day somebody wrote it another way |
| `socket-activation.sh` (P4e-ii) | a daemon that gets socket activation or the journal wrong under a **real service manager** — the two things `daemon::systemd`'s unit tests and `crates/daemon/tests/systemd.rs` cannot produce between them: a descriptor passed in through `LISTEN_FDS`, and a stderr that *is* the journal. Four claims. **The daemon serves the socket systemd bound and never binds its own**, asserted on the socket's **inode** — read once when `systemd-socket-activate` says it is listening and again once the daemon says it is serving, because a daemon that unlinked the inherited socket and bound its own leaves a different inode at the same path and every client that had connected is talking to nothing — with the path the daemon *announces* checked beside it. **An abstract-namespace address is refused outright**: no directory, no mode, no owner, reachable by anything that can spell the name, so there is nothing to check rather than something this build cannot verify. **More than one inherited descriptor is refused** rather than guessed at. **When stderr really is the journal the log goes there as structured entries**, from the journald layer installed *instead of* the stderr formatter — both would put every line in the journal twice — told apart by `_TRANSPORT`, which is `journal` for an entry the layer wrote and `stdout` for the same line rendered to a captured stderr. **On this host every arm ran for real and no skip was taken**: the activation claims were driven by `systemd-socket-activate` and the journal claim by a transient `systemd-run --user` unit. What D11 *cannot* ask of an inherited socket is recorded where the defence lives — the check is on a name, because the bind already happened in systemd from a `ListenStream=` string, so N39's substitution defence does not apply and the unit's `DirectoryMode=0700` is what closes it (N39's 2026-08-11 amendment) | `$WCH_GATE_WCHD` points the predicate at daemon-shaped programs that get one claim wrong each, two of them the real binary with one thing taken away: a daemon that adopts whatever descriptor it is handed (`from_raw_fd(3)` is two lines and asking what the descriptor *is* is twenty); one that never looks at `LISTEN_FDS` and binds its own socket over the one systemd bound; one that never serves, leaving the gate nothing to examine; and one that renders its log to a stderr that is already the journal. `pass_case` drives the shipped `webcam-handler-daemon` (rubric rule 6 [S:N10]). The second green arm is about the **skip**: `$WCH_GATE_SOCKET_ACTIVATE` pointed at a program that is not there must decline the three activation claims *named and counted* and still pass, with the journald claim still running — so the counted skip this host did not need is itself exercised green [S:N44] |
| `cli-parity.sh` (P4f) | the one command surface forking under its two roots: `webcam-handler-cli` and `webcam-handler-client` answering one verb two ways. It starts a real `webcam-handler-daemon` over the fake on a scratch pair of XDG directories, waits on the daemon's **own stderr** for the socket it binds, then walks a table whose population is `webcam-handler-cli --help` scraped at both levels with an exact row match — a leaf in no bucket fails, a row naming no leaf fails, and a bucket outside the closed four-name vocabulary fails. Six read verbs are compared byte for byte with both roots required to exit zero (two programs that both failed would otherwise agree on an empty document and be reported as parity); eleven exempt verbs are each driven through `webcam-handler-client` alone and must answer, which is how docs/9's "no local-only verbs at P4" is **tested** rather than restated; the six session-writing verbs are additionally asserted to be **refused to `webcam-handler-cli`** by D9's lifetime store lock, in the refusal's own words read out of `schema::error`; and `snapshot`'s exemption is measured to be exactly one field (`taken_at`) wide. The profile, the camera id, the control it names, the socket path and the lock advice are all derived from the tree, never transcribed. Its honest limit is in the header and in the gaps below: it catches a row that is missing, a bucket that is misspelt, and a read verb relabelled into `session` or `stamped` — but a read verb relabelled into `device` would pass, because the only checkable consequence of that exemption is the double drive it exists to avoid | the arm the row is named for edits `cli_core::render::json` in a scratch tree so the emitter forks on which binary is running, builds **real** `webcam-handler-cli` and `webcam-handler-client` from it (its own `CARGO_TARGET_DIR`, note N32) and points `$WCH_GATE_WCH`/`$WCH_GATE_WCHC` at them — a stub pair that disagreed would prove only that this predicate can spot a `diff` [S:N10]. Then, in a copy of the predicate's own table: a leaf verb whose row is deleted; a read verb relabelled into `session` (red because `webcam-handler-cli get` answers happily beside a running daemon); the same into `stamped` (red because `info` carries no stamp, so the two roots are byte-identical and there is nothing to exempt); a bucket name outside the vocabulary; and a row naming a verb the surface does not offer. Then, at the seams: a `webcam-handler-client` that declines `profile capture` — which is what a local-only verb *is*, and the arm that keeps the "none at P4" claim tested; a daemon that never serves; and a daemon that announces a socket and is not there. `pass_case` drives the shipped `webcam-handler-cli`, `webcam-handler-client` and `webcam-handler-daemon` (rubric rule 6 [S:N10]). Every arm asserts the failure **message**, not the status, because several are red under more than one branch [S:N31] |
| `agents-md-current.sh` (G4 review; the redirect half, owner ruling 2026-08-14) | the deployed `AGENTS.md` at the repository root drifting from doc 10, which is its source. The file's own opening declares the deployment and tells you what to do "when they drift" — a reconciliation procedure written into a document is the project naming an anticipated defect class, and rule 1 says a named class becomes something that can go red. It had nothing: `grep -rn 'docs/10' scripts/` returned empty. Ten commits had moved the pair since the v2 series was issued and all ten moved both copies, which is the record of a rule nobody has needed yet rather than a rule that cannot break. **Nothing is transcribed**: the source is whichever `docs/*.md` carries the sentence "Deploy at the repository root as `X`", and the deployed name is read out of that sentence, so a v3 reissue or a renumbering follows the document; `docs/historical/` is excluded because docs/5 is v1 of this same file and still carries its own instruction; and only each document's **preamble** is searched, everything above its first `##`. That last one was measured, not designed: the first version read whole files and `pass_case` went red on the shipped tree the moment *this row* was written, because the row quotes the rule it documents. A declaration about where a document deploys is a statement the document makes about itself and this series makes those in its opening block; below the first heading a document is discussing the world, and a gate that cannot tell the two apart forbids writing about itself. The comparison is byte for byte with **no allowance** — the source is written in the deployed copy's voice, the two are identical today, and a tolerance for "a header the deployed copy might carry" is a hole that a paragraph fits through next. **The same defect class one file along: the redirect (owner ruling, 2026-08-14, note **N102**).** The house practice is the standardized, tool-neutral `AGENTS.md` plus a `CLAUDE.md` at the root holding `@AGENTS.md` and nothing else, so a Claude-specific reader looking for the name it knows is pointed at the one home the rules have rather than given a second copy of them. A ruling that puts a second file at the root creates a second class with the same shape as the first: `AGENTS.md` can drift from doc 10, and `CLAUDE.md` can stop being a pointer and grow content — a paragraph somebody thought worth saying to a Claude reader in particular, a "quick summary" above the reference, a stale rule nobody deleted — with the same harm arriving through a different door, two sets of non-negotiable rules in force at once and neither reader able to tell which one it is holding. Rule 1 has no exemption for a class anticipated by a ruling rather than by a parenthesis, so the claim landed here beside the one it rhymes with. **"Nothing else" is exactly the reference and one trailing newline** — 11 bytes, asserted byte for byte, with no tolerance for a leading blank line, a trailing comment, a second line of any kind, or the *absence* of the newline. That is the byte-identity argument above made about a smaller file: it is not a bar the repository has to climb to, it is where the ruling already put it, and the tempting allowance ("let it carry a comment explaining the redirect") is the same hole with a nicer name, because a file that may carry one explanatory line may carry a paragraph contradicting `AGENTS.md`, and the reader who needs telling which of the two is authoritative is exactly the reader who cannot be. The missing newline is argued rather than assumed: nothing is *harmed* by a file whose last byte is `d`, but "the redirect is these bytes" has no seam in it while "these bytes, or those, and here is the rule for which" is a predicate that admits a set, and a set with two members is a set somebody extends. **A link is refused, and refused before the bytes are read**: `CLAUDE.md` as a link to `AGENTS.md` makes "the redirect resolves to the rules" true by construction — the unfalsifiability the deployment half's own link check exists for — and it is a second copy rather than a pointer, since a tool that does not follow links gets a path where a reference belongs and one that does gets eighteen kilobytes. It is reported first because the byte comparison would otherwise answer it with the wrong sentence, naming the size of the mistake instead of its mechanism. **Derived here too**: neither the redirect's name nor the name it points at reaches the logic as a literal — both are read out of the doc's own sentence, and the target is then required to be the name the deploy sentence carries, because a pointer at anything other than the deployed copy is a reader sent where the rules are not | sixteen fail arms and four green ones, eight of the fail arms and one of the green belonging to the redirect half. For the deployment: a root copy with a rule the doc has never heard of; the same divergence seeded from the docs side; the root copy missing; the source document gone; the deploy sentence reworded away (seeded in *both* files, so what goes red is "no source declares a deployment" and not the drift it would otherwise drag in); a second document claiming the same deployment; a deploy target that is a path rather than a name at the root; and the copy replaced by a **symlink** to its source — which changes nothing a reader would see and makes the comparison unfalsifiable, so the predicate reports it rather than passing over a population of one. For the redirect, the same eight questions asked of the second file: the redirect missing; the redirect carrying more than the reference; the redirect pointing at another file; the redirect a **link** to the deployed copy, which is the unfalsifiability arm one file along; the source no longer declaring a redirect at all; a second document declaring one; a declared redirect that is a path rather than a name at the root; and a declared reference naming something other than the deployed copy — the arm that says a pointer at the wrong name is a reader sent where the rules are not. The green arms are the shipped tree; the source document renamed to `docs/11-…-v3.md`, which is what proves the population is derived rather than the filename transcribed; another document quoting the rule in its **body**, which is why only each document's preamble is searched; and the redirect renamed in the document and in the tree together, which proves the second half is derived from the same sentence rather than transcribed beside it |
| `avi-reparse-is-independent.sh` (P6a) | the two independently derived implementations of one AVI byte layout starting to share code — a constant, a FourCC, a helper — so that the re-parse docs/7 P6a requires quietly stops being independent of the muxer it checks. **It exists because nothing could go red on the property at all**, and that is not "a test was forgotten": `crates/imaging/src/avi.rs`'s module doc makes the claim in the strongest form there is — `read.rs` and `write.rs` "share **no** code, not a constant, not a FourCC, not a helper" — and a test sees values, while this claim is about `use` statements and where files sit. The behavioural half is the `g6` row above it: `the_size_fields_agree_with_the_bytes_written_over_many_frame_length_sequences` drives thirty-two of the muxer's own recordings back through `imaging::avi::read` and checks every size field, and it is exactly as green against a reader that reads its FourCCs off the muxer's line as against one derived from the specification — greener, because two sides that share a constant agree by construction. So the day a shared `FOURCC_MOVI` or a `chunk_header()` helper is factored into a third module beside these two, every test in this workspace passes, the criterion stays green, and the property P6a asked for is gone with nothing left to notice. That is `token-comparison-has-one-home.sh`'s argument — a defect class the suite is structurally unable to witness — in another costume, and `web-routes-are-gated.sh`'s shape: a claim over the source, made where the decision is actually taken. Four claims. **(1) The pair is the whole population**: `read.rs` and `write.rs` are both under `crates/imaging/src/avi/` and are the **only** `.rs` files there, recursively — a third module is precisely where a shared layout helper lands, it would satisfy every claim below, and the diff that adds it reads like tidying, so adding one is a decision taken in this predicate with an argument rather than in a file nobody weighs. **(2) The reader's product code names no path into the writer**: `write::`, `super::write`, `crate::avi::write` and `use super::{write, …}` are one rule — the sibling's stem in a path position — and an ordinary word is not, since `write.rs`'s product code contains "already" and a predicate that read the `read` inside it would be the gate note **N60** bills for. **(3) The writer's product code names no path into the reader**, while its `#[cfg(test)]` half may, does and must: `write.rs`'s tests drive `avi::read` on purpose and that coupling *is* what P6a asked for, so a predicate forbidding it would forbid the criterion it exists to protect and is one somebody turns off. **(4) Both modules are still declared**, in `avi.rs` and in the crate root — an orphaned `.rs` file is not a compile error in Rust, so the whole subtree can leave the build in one deleted line while every claim above stays green and vacuous; `dependency-walls.sh`'s "the crate left the workspace" arm, charged to a module. Prose and test code do not count (`lib.sh`'s `gate_product_lines`, shared with the two predicates above precisely so that three predicates cannot disagree about where a file's test half begins), and a file whose boundary cannot be read is a failure rather than a pass. Populations are derived — the crate's directory from `cargo metadata`, the modules from the directory listing, the declarations spelled from the stems that listing produced — and the crate, the parent module and the two names are asserted to exist before anything is counted. Its honest limit is in the gaps below | ten fail arms, three green. The arm the row is named for is `fail_case_a_third_module_holds_the_shared_layout`, written as the diff that would actually land — a `layout.rs` holding the FourCCs both sides had derived separately, declared in `avi.rs`, imported by each — which is red under three claims at once, so the arm asserts the third-file sentence rather than the exit status. Then, per claim: the reader importing the writer; the reader reaching a writer constant by a **fully qualified path**, which adds no line to the header and is what a rule that only read imports would look straight past; the writer importing the reader in **product** code; the reader renamed away, so the pair's named halves no longer resolve; the directory gone entirely; a file carrying two `#[cfg(test)]` markers, so claim 3's exemption has no edge and would cover the whole file; a module no longer declared in `avi.rs`; the crate root no longer declaring `avi`; and `webcam-handler-imaging` leaving the workspace, seeded through `cargo metadata` because deleting the crate makes the metadata itself fail and an arm that goes red before the predicate runs proves nothing about the predicate. The green arms are the shipped tree; a comment that names the very reach the gate refuses, because `read.rs` and `avi.rs` argue about the split by name for dozens of lines and a gate that read prose as code would push that argument out of the modules carrying it; and `pass_case_the_muxers_tests_may_drive_the_reader`, the one that matters most — it is the `g6` criterion above, and a predicate that turned it red would be a predicate somebody turns off |

`selftest.sh` is table-driven over the whole population; adding a predicate without cases
fails the selftest itself (the table is derived from the directory listing, not
maintained by hand).

**The phase-gate mechanism** (built at P1, now the standing shape): one row per
criterion in `scripts/gates/phase-criteria.tsv` — `phase`, `kind` (`tests` = a nextest
filterset run with `--no-tests=fail`; `command` = run from the repo root), the
selection, and what it establishes in docs/7's words. `scripts/gates/phase.sh` runs one
phase's rows and counts them (`just gate-g0` … `gate-g6`), and `counted-selections.sh`
proves every `tests` row still selects more than zero — the predecessor's defect was a
held gate whose selection had silently gone to zero. Criteria accrete row by row as
docs/7's sub-milestones land; a criterion is added by adding a line, in the same commit
as the thing it proves.

### The battery (backend conformance)

`webcam-handler-testkit::battery` — the T1/T2 conformance suite every backend runs (design §2.11):
enumeration sanity, control-model invariants (unknown types round-trip; sparse menus
preserved; out-of-range values survive), write read-back, snapshot/restore inverse,
stream lifecycle, hotplug watch lifecycle, fault-menu coverage. Arms are walked by an
exhaustive match; a skip is a declared variant with a written reason and is checked in
both directions (an arm that ran while declared skipped fails; an undeclared non-run
fails). `webcam-handler-fake` runs it from P0. `webcam-handler-v4l2`'s relationship to
the battery is stated honestly (docs/7 G1): the read arms run over the fake replaying
the committed v4l2-captured profiles — proving the model against real-device shapes —
while the v4l2 crate's own ioctl truth lives on the R2 (vivid) and R3 (hardware) rungs;
its write arms run as R3 hardware twins, evidence-recorded per docs/7 G2, never
CI-gated (recorded: notes E3, thirteen hardware tests including writes, clamps,
INACTIVE flips, snapshot/restore, streaming and photo on two devices).

## Part 2 — Gates commissioned by later phases

Per rubric rule 1, each lands in the same PR as the structure it guards; per rule 6, each
lands with both-direction cases. Struck through as they land (the repo is authoritative;
this table is the commissioning record).

| Gate | Phase | Enforced by | Catches |
|---|---|---|---|
| ~~**Profile capture/replay inverse**~~ | P1 | **landed**: `crates/backends/fake/tests/corpus_replay.rs` replays every committed profile through the battery and asserts the fake rewrites only the id and the backend field; `hw_profile_capture_reproduces_the_committed_invariant_section` closes the capture half on hardware | `profile capture` and fake replay drifting apart — the corpus quietly ceasing to resemble devices (E5) |
| ~~**vivid rung skip accounting**~~ | P1 | **landed and executed**: four `vivid_*` tests, declared by `scripts/rung-vivid.sh`'s `wch-suite` marker and checked by `ignored-suites-have-recipes.sh`; all four green on their first run (evidence E2). The rung scripts additionally count the skips *tests* report at run time, so a hardware test that declines a claim is named rather than passing quietly | the virtual-driver rung decaying into green-by-absence on every runner |
| ~~**Miri over the sys-decode units**~~ | P1 | **landed, and corrected inside P1's own review cycle**: `scripts/miri.sh` selects `sys::decode` **or** `sys::payload` — 23 units over the captured ioctl replies in `crates/backends/v4l2/fixtures/`, including the two Miri-reachable `unsafe` blocks (`Payload::bytes`/`bytes_mut`). The original decode-only selection (19 units) covered no unsafe block at all, which the P1 review caught (E1's amendments). The decoders take `&[u8]` at `offset_of!`-derived offsets so the population stays real | undefined behavior in the one crate allowed to have any |
| ~~**PF regression fixtures loaded**~~ | P1 | **landed**: `scripts/gates/corpus-floor.sh` (three claims: non-empty, every profile reachable, at least one test *replays* rather than merely parses). `corpus_replay.rs` additionally asserts each device-behavior PF finding is exhibited by a committed profile, and names the ones deliberately absent | dead corpus — a profile nobody replays |
| ~~**`--json` validates against the bundle**~~ | P1 | **landed, wider than commissioned**: `scripts/gates/json-validates.sh` runs the built `webcam-handler-cli` over the fake backend and checks **every** verb's answer — writes included — against `#/$defs/<type>` in the committed bundle, with the verb population derived from `--help`; its honest limit (not a full JSON Schema validator) is in the gaps below | a renderer that wraps the answer in an envelope, adds a field, or hand-builds an object — `schema-artifacts-current.sh` proves the bundle matches the *types*, and nothing proved the *output* matched either |
| ~~**Guarded-set inverse**~~ | P2 | **landed**: property test + constructible-inverse fixture in the battery write arms | a manual write under live automation slipping through the planner |
| ~~**Snapshot-restore assertion**~~ | P2 | **landed**: battery arm perturb → restore → byte-compare; R3 twins asserted it on two devices (notes E3: "snapshot(15/22) → perturb → restore, every control back"), with N9's fourth outcome counted complete | restoration by assumption (rubric C smell) |
| ~~**EXIF read-back**~~ | P2 | **landed**: kamadak-exif reads what little_exif wrote, from the file on disk — the P2 review tightened the arm that asserted only on the report [S:E4]; PF:16's splice fixture rides the same suite | write-only EXIF claims |
| ~~**Store bypass gate widened**~~ | P3a | **landed**: `atomic-write-home.sh` now has two halves — the bypass scan, whose population learned D9's session-file names (`session.json`, `log.ndjson`, `sessions/`, the `schema::limits` constants) and is `gate_require_nonzero` rather than a named skip, and a new arm over the **home itself**, asserting six D9 properties of `engine::store`: `write_json_atomic` exists, the temp file is made in the destination directory, it is synced before the rename, the rename is what publishes, the parent is fsynced, and the one lock is an `fd_lock::RwLock`. Nine selftest arms, two of them green-direction. Its first run over the real home caught a false positive worth keeping: naming the home in a doc comment is deference, not a reach (`engine::photo` says why a photo is *not* written that way) | session writes dodging `write_json_atomic` or the lock — and, now, a home that quietly stopped being atomic or stopped taking the lock |
| ~~**Crash-recovery case**~~ | P3b | **landed**: `crates/engine/tests/crash_recovery.rs` — the parent spawns this test binary as a child, which takes the lock, performs a sweep write, announces itself on a pipe and blocks; the parent `SIGKILL`s it and asserts `status.signal() == Some(9)`, because a test that would still pass on a clean exit is not testing a crash. A fresh store then finds the lock free, resumes by fingerprint and task, and restores from the persisted snapshot. Selected by `just gate-g3` as `binary(crash_recovery)` | a crashed sweep leaving a camera mis-set with no recovery path |
| ~~**Vivid sweep arm**~~ | P3c | **landed and executed**: `vivid_a_calibration_sweep_sets_settles_captures_and_scores_through_the_real_ioctl_path` plans three step-aligned values from the driver's own range, writes them guarded, settles, captures, scores and records — probing the automation pairs first and restoring through the persisted pre-sweep snapshot afterwards, with the swept control's return asserted. Eight `vivid_*` tests green on `just rung-vivid-managed`, 0 skips. It also produced PF:17: `vivid`'s `u8_pixel_array` reshapes with the negotiated format, so the arm asserts the *control it swept* came back rather than that the whole restore report is complete | a sweep loop proven only against the fake's model of a driver |
| ~~**R3 calibration and motion arms**~~ | P3e | **landed and executed**: `hw_a_calibration_session_sweeps_a_brightness_control_selects_applies_and_restores` runs start → plan → sweep → select → apply → restore against every attached camera that offers a brightness-class control, and `hw_motion_a_bounded_ptz_sweep_returns_the_motor_to_where_it_started` is the one arm in the workspace that drives a motor — bounded to a few steps either side of home, asserting the planner's motion cap on the device's own range and the head's return at the device. `just smoke-hw` runs 15 tests green; `WCH_NO_MOTION=1` drops it to 14 with the exclusion as a named, counted skip. Evidence E5, and it produced PF:18 and N21 | calibration proven only against the fake's model of a camera; a motor nobody ever drove |
| ~~**Two gate populations widened at the P3 review**~~ | P3 review | **landed**: `json-validates.sh`'s verb population now recurses one level — for each top-level verb it scrapes `webcam-handler-cli <verb> --help` and requires a row named `<verb>-<sub>` for every leaf, with an *exact* row match instead of the prefix match that let one `calibrate-start` row answer for the whole seven-verb subtree; a selftest arm deletes one `calibrate-*` row from a copy of the predicate and asserts it goes red. `atomic-write-home.sh`'s raw-write pattern gained `File::options(` and `File::create_new(` — std's own aliases for `OpenOptions::new()` and `File::create()` — with a fail arm for each: two byte-identical bypasses used to get opposite verdicts on how the open was spelled | the third and fourth instance of note N10's family — a gate green while checking less than it claims. Before the fix six of the seven verbs P3d landed could vanish from the gate with it still green |
| ~~**The raw-write population widened at P4e-i**~~ | P4e-i | **landed**: `atomic-write-home.sh`'s raw-write pattern was `std`'s spellings only, and P4e-i taught this workspace another one — `daemon::server::open_destination` opens through `rustix` and turns the descriptor into a `std::fs::File` (note **N51**'s discharge). The pattern now also matches the write-shaped *flags*, `OFlags::(WRONLY\|RDWR\|CREATE\|TRUNC\|APPEND)`, and deliberately not `rustix::fs::open(` itself, because `SocketDir` opens the runtime directory `O_PATH \| O_DIRECTORY` with that function while naming `XDG_RUNTIME_DIR`. Two new selftest arms, one per direction, and the seeded bypass was measured not to match the old pattern before it was called a widening | the third instance of note N10's family: a state write spelled a way the gate's hand list had never seen |
| ~~**Mutation floor**~~ | P3f | **landed and run**: `just mutants` → `scripts/mutants.sh` over `cargo-mutants`, scoped by `.cargo/mutants.toml` to the six pure cores (`engine::{pairing, session, settle, store, sweep}`, `imaging::metrics`) and judged by the **whole workspace suite** per AGENTS rule 2. Survivors are compared against `scripts/mutants-accepted.txt` **in both directions** — an unlisted survivor fails the job, and so does a listed one that has stopped surviving, because an acceptance nobody re-checks is how N15's mistake gets made twice. The scope's exclusions are stated as absence from `examine_globs`, each with its reason, so widening the floor is adding a line — and P4a added three, `crates/api/src/{codes,photo,wire}.rs`, after the P4a review found two survivors there by hand that the floor could not see. First-run numbers, cost and triage: evidence E7; the widened run (478 mutants, 409 caught, 11 survivors all accepted, register clean both ways): evidence E8; posture and cadence in the gaps below. **P4d added the tenth file and the first from a backend** — `crates/backends/v4l2/src/hotplug.rs`, the uevent packet decision and the debounce fold — and the widening earned its keep on the first run over it: one survivor (`Tracker::next_deadline` replaced with `None`), which is a watch that hands a settled burst to its caller at the *caller's* deadline instead of at the burst's. Not accepted — killed, by a new behavioural test in `watch.rs` watched failing against a hand-applied copy of the mutant. The P4d boundary run (515 mutants, 431 caught, 11 survivors all accepted, register clean both ways): evidence E10. The socket beside it (`sys/uevent.rs`) stays out for the same reason the rest of `src/sys/` does, and `watch.rs` stays out because what is left in it after the folds moved down is timing, which is the class N52 records this floor being bad at. **The test timeout is pinned** (`minimum_test_timeout = 180.0`) since P4c, because the autoset floor of 20s made the verdict a function of how many jobs the machine ran: the same tree answered FAIL with 31 unaccepted survivors at 8 jobs and PASS with the register clean at 4, and all 31 were timeouts on healthy mutants. Note N52 carries the measurement and the reason it is a floor rather than a multiplier | tests that execute the cores without constraining them |
| ~~**T5 method-count walk**~~ | P4c | **landed**: `crates/daemon/tests/method_surface.rs`'s `every_method_the_daemon_registers_is_exercised_over_the_fake`, with a `g4` row of its own. Both sides are derived and neither is a list. The **registered** side is `method_names()` off the very `Methods` value the fixture serves — built by the generated `into_rpc()` over a real `Wchd`, which is the *daemon's* registration and not `api::METHODS` (note N28 draws that line). The **exercised** side is recorded at the transport by a `Recording` wrapper around the shared `Wire`: it writes down the method name the generated client handed it, on the way past, refusals included — so the spelling comes from `#[method(name = …)]`'s expansion on both sides of the comparison. Driven once per transport, and it is deliberately **one** test, because nextest runs each test in its own process and nineteen separate ones would each record a single name. Measured red both ways it can go: a twentieth method added to the trait and implemented but not driven fails the comparison naming it, and a daemon whose registration is missing a method the suite drives fails earlier, at the call, with `-32601`. Its four honest limits are in the register below | a wire method with no test |
| ~~**Schema-artifacts gate widened**~~ | P4a | **landed, and the script needed no logic change** — which is the interesting part. Its two loops name no filename (emitted-and-not-committed, emitted-and-different, committed-with-no-generator), so `schemas/webcam-handler-openrpc.json` joined the comparison by being written. "Covered by construction" is the kind of sentence this suite exists to distrust, so the coverage is spent in `cases/`: four arms for the OpenRPC document specifically — stale, emitted-but-not-committed, orphan, and the one this row actually commissions, a wire name edited in `crates/api/src/lib.rs` with nothing under `schemas/` touched and the **real** emitter left to find it (rubric rule 6, N10). The arms now assert the failure *message*, not just the status, because several seeds are red under more than one branch and the harness reads only the status (note N31); the Rust-editing arm builds into a directory of its own, because a mutated build sharing the checkout's `target/` poisons it (note N32). The P4a review then found the branch none of them reached — an empty `schemas/` answered `PASS … 1 named skip`, so deleting every committed artifact was green — and that skip is now `gate_require_nonzero` with an arm of its own. The document itself is emitted from `api::METHODS` and `api::rpc_code`, which are generated from the trait declaration rather than transcribed beside it (note N28) | OpenRPC drift from the wire trait |
| ~~**Netlink hostile-bytes fixtures**~~ | P4d | **landed and executed**: twelve `.bin` fixtures under `crates/backends/v4l2/fixtures/` — five real kernel datagrams and seven synthetic hostile shapes, each asserting a *different* answer — plus a fixed-seed walk over random and mutated packets, so "malformed" and "a shape this build cannot read" are never spelled the same way (rubric B10: a packet is attacker-shaped input). The R3 arm `hw_hotplug_a_uvcvideo_cycle_arrives_as_removals_then_arrivals_through_the_real_watch` cycles `uvcvideo` via the blessed helper with all cameras closed and never `--force`, watching while it happens, and asserts the events applied to the pre-cycle node listing reproduce the kernel's own listing afterwards; four preconditions decline as named, counted skips and two of them were driven for real. `just smoke-hw` runs 16 tests green, `WCH_NO_MOTION=1` 15. Evidence E9, and it consumed the design §8 item 10 measurement [PF:21] — the uevent bind needs no `CAP_NET_ADMIN`, so the arm spends no capability of its own. Mid-stream device loss stays the fake's (§3.3 item 9), which the interlock makes unarrangeable rather than merely undone | a parser that trusts the kernel socket; hotplug proven only against scripted fakes |
| ~~**CLI parity gate**~~ | P4f | **landed**: `scripts/gates/cli-parity.sh`, and it says more than it was commissioned to. It runs the two shipped binaries against one real `webcam-handler-daemon` replaying one committed profile and compares the bytes — six read verbs, byte-identical `--json`, both roots exiting zero. The population is scraped from `webcam-handler-cli --help` at **both** levels with an **exact** row match (`json-validates.sh`'s idiom, and the P3 review's finding), in both directions: a leaf in no row fails, and a row naming a verb the surface no longer offers fails too. Every one of the seventeen leaves falls in one of **four named buckets**, and the bucket vocabulary is closed, because a misspelt bucket read by a `case` is a verb that falls through every arm and is counted as present — note N10's family in one character. Three of the four buckets carry a check their own reason implies, which is what turns "named, never silently exempted" from a label into a claim: the six **session** verbs are asserted to be refused to `webcam-handler-cli` by D9's store lock **in that refusal's own words**, read out of `schema::error::LockProtocol::advice` rather than transcribed; **stamped** (`snapshot`, which mutates nothing but stamps `taken_at` with the instant of the read) is measured to differ in exactly that one field and no other, so a read verb hidden there fails the second half. The fourth, **device** (`set`, `restore`, `photo`, `profile capture`), rests on an argument rather than a measurement, and the header says so: the only checkable consequence of "it mutates the camera" is the double drive the exemption exists to avoid performing. **This row's own "local-only verbs (none at P4)" is tested rather than repeated** — every exempt verb is driven through `webcam-handler-client` alone and must answer, so a verb only `webcam-handler-cli` could run names itself there. It came back none. `terminate_holder` is out of the population **by construction and not by exemption**: it has no T4 spelling at all (note **N48**), so `--help` does not offer it and it is not a leaf. The two argv differ in their *global* flags — `webcam-handler-cli` is given `--backend fake --profile …` and `webcam-handler-client` must not be, since P4f made it refuse a composition root's decision — and that is upstream of the surface T4 shares rather than an exemption; the header argues it. One thing it exercises as a side effect is worth naming: `webcam-handler-cli` reads the very state directory a live `webcam-handler-daemon` holds, which works only because read verbs take no lock under either D9 protocol, and both halves of that are asserted, one per bucket | the T4 single-surface claim silently forking |
| ~~**Signal parity tests**~~ | P4e-ii | **landed, and it found the defect it was commissioned against**: `crates/daemon/tests/signals.rs` — one test per signal, delivered for real to a subprocess `webcam-handler-daemon` that is mid-sweep with a subscription open across it, selected by `just gate-g4` as `binary(signals)`. **P4e split in two** (note N58): this row is the shutdown half's, and its two ingredients are what made the split sequential — P4e-i's `crates/daemon/tests/subscriptions.rs` already holds a sweep inside a device write with a subscription open across it, and already ends by asserting `stopped()` resolves. Three things this row is worth more than its commissioning suggested. (1) **It was red on the shipped build about half the time, and it was right** [S:N61]: `stop_in_order` cancelled the subscriptions and stopped the transport in adjacent statements, but jsonrpsee sends a cancelled subscription's close frame from a task spawned *after* the body returns and holds no connection open for one — so the client got a closed socket instead of `SHUTTING_DOWN`, which is the cheaper half of that step bought by a race rather than by an ordering, and it was invisible to every in-process test because the socket outlives the process there. Step 3 now waits for the live subscription count to reach zero, on a deadline taken once and shared with the drain. (2) **The sweep is held mid-flight by a fifo**, at the second sample's own photo path derived from the `Session` the daemon answered with: a subprocess has no gate a channel can release, and `fake::Fault` is in-process only, so the wedge is `calibrate.rs`'s deliberately *blocking* `std::fs::write` for sample photos — which is a dependency of this suite and is recorded as one [S:N62]. (3) **The outcome is one record of eight fields, not two lists of assertions**, because the claim is that the two signals produce the *same* outcome and two lists can drift while both stay green: the ending's reason, the daemon alive with work in flight, the samples answered with against the samples on disk, the photo's bytes crossing the fifo, exit zero, the lock free, and the socket file still there. Watched red against a teardown that cancels nothing (both tests `TIMEOUT`), a stop that does not drain, a daemon that unlinks its socket, SIGINT handled alone (the SIGTERM test red in 0.076 s and the SIGINT test green — the asymmetry the comparison exists for), and step 3's wait first removed and then left unbounded. Residual, stated rather than hidden: an *idle* connection's subscription can still lose its ending under heavy load — 1/60 with eight spinners — so the suite rides its subscription on the connection carrying the sweep; stress was 0 failures in 80 runs under eight spinners and 0 in 5 full-workspace runs | the graceful path production triggers bypassing drain/release |
| ~~**UDS permissions**~~ | P4b | **landed, in two places and deliberately so**: the startup assertion is `daemon::uds::SocketDir::prepare`, which creates `0700`, *reads the mode back* and refuses — never repairs — anything else, with unit tests in both directions including one that asserts a rejected `0770` directory is unchanged afterwards [S:N39]. The other-uid half is `scripts/gates/uds-permissions.sh`, because nextest has no runtime-skip concept and `just ci` runs without `--success-output final`, so a declining `#[test]` is a skip that reads as a pass — the shell predicate's `gate_skip` is named and counted [S:N44]. Writing it also found the check's own vacuity trap: `mktemp -d` is `0700`, so a second uid is stopped by the *scratch* directory and the assertion never reaches its subject; the predicate widens everything above the socket directory and makes the parent's reachability a checked precondition. **Delta at P4d:** `SocketDir` now *holds* the directory as an `O_DIRECTORY | O_NOFOLLOW | O_CLOEXEC` descriptor and binds through it (`/proc/self/fd/<dirfd>/wchd.sock` — Linux has no `bindat(2)`), and checks `st_uid` against `geteuid()` as well as the mode, so the symlink refusal is the kernel's own and a directory substituted between the check and the bind is *defeated* rather than detected [S:N39] | a world-usable camera daemon socket |
| ~~**Token enforcement**~~ | P5a | **landed, in two altitudes, and the split is what the row is worth**: `crates/daemon/tests/http.rs` drives a real TCP socket, a real axum server and a hand-written client — 401 without a credential, 200 with, in **both** forms, because the `Authorization` header and the `?token=` query parameter are two mechanisms a build can lose one at a time and an operator's first request is a *navigation*, which can only carry the second. The credential every test presents is parsed out of `Serving::ready_to_open_url` — the line the daemon prints — rather than read off the `Token` value, because a build that published one secret and checked another would otherwise pass the whole suite, which is the one bug this row exists to be unable to survive. All four cells of D11's matrix are driven, and the two non-loopback ones only because `daemon::http::listener::serve` takes the **posture as a value**: the socket stays on loopback and the posture is decided about `192.168.1.10:8080` and `0.0.0.0:8080`, so what those two establish is that the gate is installed *because the posture said so* and nothing about where the posture came from — the suite's own header says so, and the other half is covered by `daemon::http::posture`'s unit tests (all four cells, both address families, plus the property that token-less happens exactly when the address is loopback and the flag was given) and by the composition root's clap tests. Two things landed that this row did not ask for and that a first draft of a token gate gets wrong. **The gate's rule is that every credential a request presents must verify, and it must present at least one** — not first-wins, not last-wins, no precedence, because those are different gates and the difference is reachable by anybody who can get one more `token=` appended to a URL an operator is about to open (HTTP parameter pollution; note **N74** carries the argument and the cost). And **in the token-less cell the middleware is not installed at all**, rather than installed and answering yes: a bypass branch inside the one function whose whole job is to say no is where an inverted condition serves a live camera. It was `Router::layer` and not `route_layer`, so an anonymous request for a path this daemon did not serve was **401 and not 404**, which is what stopped a stranger mapping the surface by watching which paths answer differently (note **N75**). Ten `g5` rows carried it. **Amended by the owner's ruling of 2026-08-12** (D11's amendment, note **N82**): the token is for the two resources that carry or drive the camera — the WS JSON-RPC endpoint and the MJPEG preview, which `daemon::http::CAMERA_BEARING_PATHS` names — and **not** for the static assets, which are this project's own open-source code, so "401 without, 200 with" is now asserted at those two routes and the client's files are asserted to come back to a request presenting *nothing*. Three things moved with it and each has a row of its own. The gate is `route_layer`, the exact tool N75 named as wrong and for the reason that made it wrong then: it maps over the routes and neither fallback, so an anonymous request for a path this daemon does not serve is now **404**, and that asymmetry — deliberate, priced, and bought a surface that is a directory in a public repository — is the price of the ruling rather than an oversight. Served responses carry `Referrer-Policy: no-referrer`, because the token rides the document's URL and an outbound link would hand it to a third party in a `Referer`. And **"every route is gated" stopped being a property of the composition and became a property of a list**, which is a defect class the ruling created and rule 1 has no exemption for: it is answered by the row below and by `every_camera_bearing_route_is_behind_the_gate`, which are the structural and behavioural halves of one partition — every route is named, and every name is gated. Four more `g5` rows carry the amendment: the predicate below, the assets-served-anonymously pair, the credential at its new address, and the referrer policy | the web listener shipping open, on any interface |
| ~~**Token comparison has one home**~~ | P5a | **landed, and this table did not commission it** — it exists because a mutant survived, which is the only reason a gate should appear here uncommissioned. `scripts/gates/token-comparison-has-one-home.sh`: replacing `daemon::http::token::Token::verify`'s constant-time body with an equality on the exposed secret leaves **every test in this workspace passing**, because the answer is identical for every input and the only thing that changes is how long the daemon takes to say no. `token.rs` had already written that down as a limit rather than a promise, and AGENTS rule 1 has no exemption for a defect class that is hard to see. The predicate cannot measure a clock, so it holds the four shape claims the timing argument stands on — one reader for the secret, no `PartialEq`/`Eq`/`Hash`/`Display` anywhere in the workspace, a hand-written non-printing `Debug`, and something still *calling* the comparison, that last one because every other claim is true of a tree where the gate stopped checking the token. Its honest limit is stated in its own header at length and repeated in the gaps register below: an early `return false` written *inside* `verify` passes all four claims. Note **N78** | a constant-time comparison quietly becoming a short-circuiting one — the one defect class in this daemon that no test in the workspace can see |
| ~~**Web routes are gated**~~ | 2026-08-12 ruling | **landed, and this table did not commission it either** — it exists because an owner ruling of 2026-08-12 took a property away, which is the second reason a gate may appear here uncommissioned (the first was a surviving mutant, one row up). Static assets are now served without authentication — the modules are not the secret — and only the WS JSON-RPC endpoint and the MJPEG preview stay behind D11's token, so `daemon::http::listener::router` gates with `route_layer` over the routes and leaves the asset fallback outside. What that cost is stated in note **N82** and is the whole reason for this row: "every route is gated" was true by construction and is now true by list, so a route added later that carries camera data and is not on `daemon::http::CAMERA_BEARING_PATHS` is a live camera served to strangers **with every test in the workspace still green**. `scripts/gates/web-routes-are-gated.sh` is the half a test cannot be — every route registration in the workspace's product code must name a path on that list, the one fallback must be the asset table's, and the composition must still install exactly one `gate::check` with exactly one `route_layer(` — and `every_camera_bearing_route_is_behind_the_gate` is the half a `grep` cannot be, driving every path on the list over a real socket against a real camera. Neither implies the other and both say so in their own headers; together they are the partition this claim now rests on | a route that carries the camera being added without its gate, in a build where nothing is left to notice |
| ~~**The R1-web Playwright rung**~~ | P5d | **landed and executed** (evidence **E16**): `crates/daemon/tests/web_browser.rs` starts the shipped listener over the fake backend and hands the URL its token is in to a child `playwright test` under `crates/daemon/tests/browser/`. **Pinned in three places, because a weaker pin satisfies two of them**: the package is `"@playwright/test": "1.62.1"` with no range operator and a committed `package-lock.json`, the browser is build `1234`, and the binary that actually launched is asserted to be that build and to report `151.0.7922.34` — a verdict that changed with somebody else's apt upgrade would not be a verdict. `devices["Desktop Chrome"]` is deliberately *not* used, because it sets `channel: "chrome"` and launches the host's own Chrome. Ten claims, 79 assertions, counted by a reporter inside the runner and compared against `tests/browser/claims.json` on every green run, so a claim added to a spec and not to the manifest is red and so is one that quietly halved what it asserts. What they establish is the half rubric B7 says was never established: a sparse menu becoming a `<select>` carrying the device's own indices and not an option's position [PF:2]; an INACTIVE control rendered **enabled**, badged and naming its owner [PF:3], beside a READ_ONLY one rendered disabled [PF:12] — so the first is not a claim about a panel that never disables anything; an out-of-range current with no `min` and no `max` on the field beside it [PF:4]; an unnameable type showing its raw discriminant [PF:1]; a clamp moving the slider back with both numbers [PF:6]; the preview `<img>` *painting* successive frames — asserted as an ordering over the fake's declared brightness gain, because Chrome fires one `load` for a multipart image and no more — and still painting after a photo (note **N83**), which is that mechanism seen from a viewer's side for the first time; the calibration view painting a sweep driven on a second socket; the client **loading and running with no credential** while an anonymous WebSocket upgrade and an anonymous `<img src="/preview?…">` are both refused and the same URL with the token paints (note **N82**); and a dropped socket reported, its preview ended, and the next socket working. **It found two defects in P5c's client and both are fixed** — one status line written by two views, so a dead calibration subscription looked healthy, and an `onClose` fired for a socket that never opened, which replaced the diagnosis a credential-less page needs with "the connection closed" (E16 §1, §2). Traces and screenshots go to `target/wch-scratch/r1-web/` — gitignored *and* pruned by every tree-walking gate, which the `scratch/` directory P5d chose was only the first half of (owner ruling 2026-08-12, note **N84**); node is never a build dependency and a test asserts the workspace runs no build script at all; the decline names its missing precondition, its remedy and every claim it cost, and `just gate-g5` records RAN or SKIPPED through `scripts/rung-web.sh` | the browser half asserted only from the API; browser regressions invisible until a human opens a tab |
| ~~**Web-fetch gate non-vacuity**~~ | P5a | **landed, exactly as commissioned, and it could not have landed earlier**: `no-external-fetch-in-web.sh`'s ten rules each quantify over the files its walk finds, so a walk that finds none makes all ten of them vacuously true — the greenest that gate could be was the state in which it was checking nothing at all. Until P5a there were no assets, and the honest answer was the named, counted skip the predicate printed; `crates/web/assets/index.html` is what makes the stronger claim assertable, so the skip is gone and `gate_require_nonzero` is in its place. A **missing** asset directory reaches the same failure as an empty one, because `gate_find` walks what is there and says nothing about what is not. Two new fail arms (an emptied directory, a deleted one) beside the existing one-per-pattern arms. What the row is worth is a function of the population it runs over, and that population is **one file** today — a skeleton page whose styles are inline because the token rides the URL and a browser does not carry a query string to a subresource (note **N76**) — so the criterion grows with P5c's modules rather than being finished here. A `g5` row runs it | the web gate going green by scanning nothing |
| ~~**Asset-crate T6 wall**~~ | P5a | **landed, and this table did not commission it either** — the crate it walls existed as an empty scaffold when Part 1's `dependency-walls.sh` row was written and had no dependency to lose. `webcam-handler-web` now links `rust-embed`, so it acquired one, and the wall says it links **neither** stack: no tokio, no axum, no hyper, no tower-http. It is its own rule in the predicate rather than a name added to one of the existing lists, because the pure wall does not cover `tower-http` at all and the wire wall allows tokio for a reason (N5) that has nothing to say about an asset crate. The crate's manifest already declined `rust-embed`'s `axum-ex` integration with this argument — "serving is `webcam-handler-daemon`'s half of the seam" — and a manifest comment is an argument until something can go red on it. Four new fail arms: the server half, the runtime half, `tower-http` specifically (the part in *neither* list this crate could have been added to, which is what makes a union-spelled wall worth having), and the crate leaving the workspace, since a wall whose subject is not a member has stopped being able to fail and stops silently. **Design §2.8's wall list did not name it** and is reconciled in the same commit as the `g5` row | "what these files are" and "how bytes become an HTTP response" collapsing into one crate (T6, design §2.10) |
| ~~**MJPEG drop semantics**~~ | P5b | **landed** (struck at P6e, which found the row still open against a test that has been green since P5b — a docs reconciliation, not a repair): `the_capture_advances_while_a_stalled_reader_does_not` in `crates/daemon/tests/preview.rs`, and it says **drop** rather than **backpressure** with three numbers, none of them inferred — the daemon's published count advancing while the client reads nothing, the client's own count staying at one, and the *gap* in the publication indices it then reads off the wire, cross-checked against the daemon's own skipped count. Both wrong builds fail in their own way: an unbounded queue returns contiguous indices and never moves the skipped count, and a bounded one stalls the capture so the wait never ends, which `.config/nextest.toml`'s timeout turns into a named failure rather than a hang | a slow browser tab backpressuring the capture path |
| ~~**Compression exclusion**~~ | P5b | **landed** (struck at P6e with the row above): `the_preview_is_uncompressed_while_compression_is_on_everywhere_else`, and it is **two** assertions because one of them proves nothing — a build with no `CompressionLayer` at all would pass "the preview is uncompressed" and fail the client it exists for. So the asset half is asserted to come back **gzipped** over the same listener with the same `Accept-Encoding` header, in the same test | CompressionLayer swallowing multipart framing |
| ~~**Muxer self-parse**~~ | P6a, P6d | **landed and executed**: P6a's independent re-parse (`imaging::avi::read`, written from the RIFF/AVI specification before the muxer existed), the size-field property test over 32 pseudo-random takes, the frozen byte fixtures for both containers, and `scripts/gates/avi-reparse-is-independent.sh` for the half a test cannot hold. P6d adds the **declared-vs-wall-clock duration bound on the R3 run**, and it landed **two-sided**: a constant-rate file declares `frames × interval` while the interval is the mean of `frames - 1` gaps, so a healthy take's file legitimately claims one frame period more video than the wall clock — measured at 1102 ms against 1016 ms on a working camera (note **N120**). What is exact is `span < wall < the test's own Instant`; what is measured and loose, with the table it came from, is `MAX_WALL_CLOCK_OVERHANG_MS` = 640 ms | a muxer that only our writer believes |
| ~~**Oracle rung accounting**~~ | P6d | **landed and executed**: `testkit::oracle` runs `ffprobe` and `mpv` over files this workspace wrote; `scripts/rung-oracles.sh` ends on RAN, on SKIPPED with each decline reprinted and its remedy named, or on one of two FAILs — the second being *neither line appeared*, which is a rung nobody can tell apart from one that was never invoked. Design §3.1 names no oracle letter and none was invented: the fake-generated half is an every-push suite (`crates/engine/tests/oracles.rs`, not `#[ignore]`d, `success-output = "final"`) and the real-camera half is part of R3. `scripts/gates/oracle-rung-accounting.sh` drives the four verdicts through the runner's recorded-log seam, nine arms, seven red-on-inverse. Note **N119** records what each oracle is *for* — they link one FFmpeg build, so `mpv` claims playability and never corroborates a demux — and why the exit code is not the answer | oracle checks silently not running |
| ~~**Agent-guide freshness**~~ | P6e | **landed, as two rows because it is two claims.** `docs/agent-guide.md` is emitted by `xtask/src/guide.rs` from the T4 command surface: the verb vocabulary and every flag are walked out of the clap tree `webcam-handler-cli-core` declares, each verb's `--json` document comes from `crates/cli-core/json-contracts.tsv` (which this sub-milestone gave a home, with three readers — the gate below had been the only one, and the guide would have been its second hand-written copy: note **N122**), and the D13 failure table is `ErrorKind::ALL` walked for `api::rpc_code` and `Error::sample`, its *what to do* column an exhaustive `match` so a nineteenth variant does not compile. **Claim one — regeneration is clean** — is `scripts/gates/agent-guide-current.sh`, whose commissioned arm seeds a value name in `crates/cli-core` and touches nothing under `docs/`. **Claim two — the examples work** — is `crates/cli/tests/agent_guide.rs`, which extracts every command from the *committed* file (a list in the test would be the second document this row is about), substitutes them against a replayed profile and runs them in document order against one state directory, so the walkthrough's ordering is part of the claim. Each is a `g6` criterion. The written half is marked as written *in the document*, section by section, because a reader who cannot tell a generated sentence from a typed one trusts the wrong one | the agent-facing doc drifting from the command surface it teaches |

## Recorded gaps and honest limits (v2)

- **The frame-content gate sniffs known image formats, and one container.** A camera frame
  embedded in a container the gate does not walk passes it; review carries that half
  (design §3.3 item 6). **Narrowed at P6a**, and the reason is worth keeping: P6a committed
  `crates/imaging/fixtures/avi/two-frame-64x48.avi`, which is two JPEG bitstreams inside a
  `RIFF`/`AVI ` envelope, and the gate reported it as "not an image" and stayed green — a
  whole new class of committed frame bytes, invisible to every predicate, with one in-crate
  regeneration assertion covering exactly one file by name. The gate now walks an AVI to its
  `avih` and holds it to the same home / provenance / extent conditions. The limit above is
  what is left of the hole, not a restatement of it.
- **Nothing walks `crates/*/fixtures/` for a byte fixture nobody loads.** `corpus-floor.sh`
  covers `corpus/profiles/` and asserts every profile is reachable from a test; the
  crate-local byte fixtures have no equivalent predicate, and P4d took
  `crates/backends/v4l2/fixtures/` from fifteen `.bin` files to twenty-seven. What carries
  them is the README's "what each file pins" table plus one `include_bytes!` per file, and
  the failure mode is one-directional: a fixture named by `include_bytes!` and *missing* is
  a compile error, so only the reverse — a fixture that stopped being loaded, still
  committed and still described — can rot unnoticed. Dead bytes, never a false green, which
  is why this is recorded rather than gated. The other reason is concrete: the table's one
  deliberately collapsed row, `querymenu-chicony-auto-exposure-{1,3}.bin`, is collapsed
  *because* the missing indices are the finding (PF:2 — 0 and 2 answer `EINVAL` and so have
  no file), and a name-matching predicate would have to be taught to read that brace form or
  would force the row to be flattened into something that no longer says it.
- **`dependency-walls.sh` reads declared edges and source greps.** A backend leaking
  V4L2 semantics through stringly-typed values (not types) is invisible to it. And it
  asserts only the **linkage** halves of the T6 walls: the behavioral halves — the runtime-free
  crates other than `engine` touching no filesystem, only `v4l2` touching `/dev` and `/sys`,
  only the engine and composition roots touching the state dir — are review-held (design §2.8
  says the same, so neither document's green overstates the other's). Membership of the
  no-runtime list means one thing only, and `engine`'s presence on it since P4b is about
  tokio and nothing else: the state directory is its job.
- **The T5 method-count walk counts calls, not depth, and is one suite's inventory.** Four
  limits, and the row is only worth what their honesty is worth. (1) A method reached by one
  call with one shallow assertion satisfies it. What pushes back is structural rather than a
  proof: the walk's answer record has one field per method and every field is read by an
  assertion, and an unread field is `dead_code`, which this workspace compiles as an error —
  so an answer nobody looked at does not build. Depth belongs to the sibling suites
  (`read_verbs`, `mutating_verbs`, `calibrate_verbs`, each with its own row) and to review.
  (2) `cargo-nextest` gives every test its own process, so the walk is one test and sees what
  that test drove; a verb covered thoroughly in some other binary and not there still stops
  the count, which is the intended direction, but this is not a workspace-wide coverage
  claim. (3) It cannot see "registered but still refusing" — a build where every method
  answered a refusal would pass it, because they were called and they answered — so it
  is only meaningful paired with `daemon::server`'s
  `the_pinned_routing_is_the_whole_wire_surface_and_nothing_answers_unimplemented` (note
  N43) and with the walk's own per-answer assertions. The refusal that made this concrete
  when the row was written, `Error::Unimplemented`, no longer exists: P4d deleted it (note
  N6, retired), so the limit now names *any* refusal rather than that one, and the paired
  test's name outlives the variant on purpose. (4) It cannot catch a rename: the
  client and the server are two expansions of *one* declaration, so a changed
  `#[method(name = …)]` moves both sides of the comparison together. `crates/api`'s
  `the_surface_registers_the_nineteen_methods_and_the_two_subscriptions_and_nothing_else` is
  what holds that, and it is why that pin is a hand-written list on purpose. (It was
  `the_trait_registers_the_nineteen_wch_methods_and_nothing_else` until P4e-i split the T5
  surface into two generated traits — note N57 — and this row is the authoritative statement
  of the mechanism, so the name here moves with it.)
- **A hang is bounded by nextest, not by a gate.** `.config/nextest.toml` gives the default
  profile `slow-timeout = { period = "60s", terminate-after = 3 }` (and the hardware group
  five minutes × four, because a bounded motion sweep legitimately takes minutes), so a test
  that stops finishing becomes a named `TIMEOUT` rather than a `just ci` that never returns.
  It is a deadline that turns a hang into a failure, not synchronisation —
  `uds-permissions.sh` makes the same argument for its own `timeout 60` — and it exists
  because the daemon suites bound themselves on the *other end* speaking: a child process's
  stderr, a camera actor's reply channel. A logging change that removed the line a fixture
  waits for used to turn `just ci` from red into never-finishing, which is the failure nobody
  debugs because it produces no message.
- **The UDS other-uid arm is a counted skip on a host with one account.** It needs a second
  account *and* a non-interactive `sudo`, neither of which is a property of the code; where
  either is missing, `uds-permissions.sh` reports a named, counted skip and the `0700`
  assertion is what carries the run. Counted is still not run — the same sentence this
  register already carries for the Playwright rung — and the mode assertion is the half that
  runs everywhere [S:N44].
- **`systemd-units.sh` is text over files, and does not claim systemd would accept the units.**
  It re-derives every value it checks from the crate that owns it, which is the transcription
  defect it exists for; `systemd-analyze verify` is the other check and is deliberately not
  run, because it needs a systemd on the host, it resolves `ExecStart=` against a binary that
  is not installed in a checkout, and its verdict about a missing executable is not a verdict
  about this project. The runtime half — a real `webcam-handler-daemon` under a real service
  manager — is `socket-activation.sh`, and between them the units are checked for what they
  *say* and the daemon for what it *does* with what they say.
- **`socket-activation.sh`'s three activation claims are a counted skip on a host with no
  `systemd-socket-activate`, and its journal claim on a host with no user manager.** On the
  host where P4e-ii landed neither skip was taken — every arm ran for real — but counted is
  still not run, which is the sentence this register already carries for the Playwright rung
  and for the UDS other-uid arm. The skips are exercised green by an arm of their own, so
  what is unproven on such a host is named rather than silent [S:N44].
- **The vivid rung proves ioctl plumbing, not device quirks** — vivid does not model
  INACTIVE-coupled menus with holes (design §3.3 item 4).
- **R3 hardware criteria are evidence-recorded, not CI-gating**: shared CI has no camera.
  The gate recipes assert the *recipes select tests*; the runs themselves land in
  the implementation notes with transcripts. This is the plan's largest honest hole and
  it is structural (design §3.3 item 1). The P1 run is entry E1 there; the P2 run is E3;
  the P3 run is E5; the P4d hotplug cycle is E9; every phase close adds its own (docs/7's
  sub-milestone shape).
- ~~**The R2 vivid suite is written and unrun**~~ — **retired 2026-08-08**. The privileged
  helper (note N8) loads the module, and all four `vivid_*` tests passed on their first
  execution; evidence entry E2 records the transcript and what the rung covers that the
  seed hardware cannot (77 controls against 18/24, and ten compound payload reads against
  one). The rung's own honest limit stands unchanged: it proves ioctl plumbing, not device
  quirks (design §3.3 item 4).
- **`json-validates.sh` is not a JSON Schema validator.** The offline toolchain has none,
  so it enforces the checkable core: every `required` property present, and no property
  the schema does not declare. Types, formats, nested shapes and array element schemas are
  unchecked. That catches the defect it was written for — an envelope, an extra field, a
  hand-built object — and this line is what keeps its green from being read as full
  validation.
- **Miri cannot cross an ioctl.** It covers the pure decode half of the unsafe module
  plus the two reachable `unsafe` blocks (`Payload::bytes`/`bytes_mut` — added after the
  P1 review found the original selection covered no unsafe block at all; E1's
  amendments record the correction); the ioctl calls themselves are exercised only on
  R2/R3. The split is deliberate (§2.5 shapes the code for it) and this line is what
  keeps "Miri green" from being read as "the unsafe module is verified".
- **The Playwright rung is Chromium-only and node-host-dependent** (owner ruling +
  design §3.3 item 7): Firefox/Safari are unexercised, and a CI host without node
  reduces the browser half to the protocol tests — the skip is counted, but counted is
  not run.
- **The selftest harness cannot test gates whose subject is the harness** (bootstrap
  limit); `selftest.sh` itself is covered by review plus the derived-table rule.
- **`token-comparison-has-one-home.sh` checks shape, and shape is not timing.** The claim
  it defends is that `Token::verify` compares in constant time; what it can see is that the
  secret has one reader, that the type refuses the operators that would short-circuit on it,
  that `Debug` does not print it, and that something still calls the comparison. **A
  hand-rolled short-circuit written *inside* `verify` — an early `return false`, a `!=` on
  the first byte — passes all four claims**, because from outside it is the same function
  with the same name called from the same place. Nothing in this suite can go red on that,
  and nothing in the Rust test suite can either: a timing assertion would be a benchmark
  pretending to be a test and on a shared runner it would be a flake, which is rubric rule
  2's other half — an assertion that cannot go red is worse than an argument that admits it
  is one. What defends the residual is the argument beside the code (`verify`'s doc states
  the claim, what it excludes, and that the length is deliberately public) and the person
  reading the diff. It says nothing about the layers underneath either: the HTTP parser, the
  router and the allocator have timing of their own, and `daemon::http::gate`'s header
  records that where it belongs [S:N78].
- **`web-routes-are-gated.sh` checks shape, and shape is not behaviour.** It sees that the
  composition holds one `gate::check` and one `route_layer(`; it cannot see *what that layer
  wraps*, so a composition that merged a third route **after** the `route_layer` line would
  satisfy every claim it makes. That is not a hole so much as the reason this predicate is half
  of a pair: `every_camera_bearing_route_is_behind_the_gate` drives every path on
  `daemon::http::CAMERA_BEARING_PATHS` over a real socket, and the two were measured against
  each other — a hand-applied route added ungated and left off the list is red here and green in
  every test in the workspace, and the same route *named* on the list is green here and red in
  three tests. Neither half is worth having alone, and each says so in its own header. It is
  also a `grep`: a route registered through somebody else's helper, or a router built by a
  macro, is invisible to the route claim, which rests on this workspace registering routes the
  way axum documents them, in three files a reviewer can hold in their head [S:N82].
- **`avi-reparse-is-independent.sh` sees imports and file layout, and neither is derivation.**
  What docs/7 P6a asks for is a re-parse path that was *arrived at* separately from the muxer,
  and what this predicate can see is that the two modules are the only two, that neither names
  the other in product code, and that both are still in the build. **A `chunk_header()` body
  retyped into the reader from the muxer's source, or a FourCC read off `write.rs` and pasted
  into `read.rs` as a fresh literal, passes every claim it makes** and costs exactly as much
  independence as a shared helper would — the two halves of one wrong idea would agree again,
  with no `use` line anywhere to see it by. Nothing in this suite can go red on that, and no
  test can either: a test sees the bytes both sides produce, and copied-in agreement is
  agreement. What defends the residual is the argument in `crates/imaging/src/avi.rs`'s module
  doc, the order the two were written in — the reader was derived from the RIFF/AVI
  specification before the muxer existed — and the person reading the diff. A second gap sits
  one directory up and is left there deliberately: a shared constant hoisted into `avi.rs`
  itself, which both modules already sit under, is outside the population the third-file claim
  quantifies over, and a rule that tried to tell a layout constant from the vocabulary types
  the muxer legitimately imports from that file would be a rule nobody could state in a
  sentence. It is review-held, and `avi.rs` is the one file whose own header argues why. It is
  also a `grep`: a reference built by a macro is invisible to both sibling claims.
- **`privileged-helper.sh` checks the mode, the dependency edge, the absence of a program
  runner and what a blessed copy carries — and no more than that.** The helper's real
  boundary is still "who has an account on this machine" (§2.13, N8, **N125**); no gate can
  defend against a second session as the same user, and none of the six claims makes a
  module load anything other than arbitrary code in ring 0. P6e's narrowing shrank *who can
  ask* and left *what a yes costs* exactly where it was, so this line still keeps the gate's
  green from being read as a security review of `crates/priv/`.
- ~~**Mutation testing is scheduled, not landed**~~ — **retired 2026-08-09**. `just mutants`
  is the job docs/7 P3f commissioned; its first run and the triage of every survivor are
  evidence entry E7. What the retirement leaves behind is a posture, a cadence and three
  limits, and they belong here rather than in a commit message:
  - **Posture: not a `just ci` step, and a G4 criterion.** The job rebuilds the workspace
    and runs all 643 tests once per mutant: 410 mutants took **21m17s** of wall clock on
    five parallel jobs (8-core machine, build
    trees in a `tmpfs` `$TMPDIR` — on disk the same run is an order of magnitude slower,
    measured). `just ci` costs minutes and must stay that way, so the floor is a rung. It
    is not left to memory either: `phase-criteria.tsv` carries a `g4` row running
    `./scripts/mutants.sh`, which is docs/7's "before G4, not after" made re-runnable.
    **Cadence: before a phase gate closes, and after any change to the six files in
    scope.** cargo-mutants is a dev tool — `just ci` never requires it, and the recipe
    reports a named, counted skip on a machine without it. **The parallelism is a space
    budget and it now holds a tree back** [S:N66]: each job is a whole build directory, and
    the job count was `free space / per-job figure`, which spends the whole filesystem. At
    the P4f boundary — a workspace one crate larger than when the figure was measured — that
    was five jobs at 3 GiB in a 16 GiB `tmpfs`, and the run died fifteen minutes in on `Disk
    quota exceeded` and reported **FAIL**, which is the word a surviving mutant gets. One
    job's worth is now reserved before the division, and both printed lines say how much was
    held back. The measurement and the budget are two numbers; the entry says why raising the
    first is not the fix.
  - **The floor is six files, and the imperative shell is not in it.** `.cargo/mutants.toml`
    names what is out and why: the unsafe V4L2 edge (only decidable against a device — R2,
    R3 and Miri own that half), the CLI renderers (survivors there say "the golden output is
    not byte-asserted", which is a decision docs/6 already made), and `engine`'s shell
    modules — `lifecycle`, `calibrate`, `discover`, `capture`, `photo`, `snapshot`, `write`.
    The last exclusion is the load-bearing one, and it is a deferral rather than a
    judgement: E6's largest P3 finding was in `lifecycle`, and the seeded-defect campaigns
    of P3b/P3c/P3d ran against those same shell modules, so **this job did not reproduce
    their counts and does not claim to** — the populations barely intersect. The one that
    does intersect is P3a's, against `engine::store`, and E7 records what the tool found
    there against what P3a's commit message claimed.
  - **A recorded acceptance is a claim about the fault that can be injected, not about a
    class of property** [S:N15]. `scripts/mutants-accepted.txt` is checked in both
    directions for exactly that reason: a mutant that becomes killable turns the job red
    until its entry is deleted and its test written.

## Coverage map — rubric gate → enforcement

| Rubric rule/principle | Enforced by |
|---|---|
| Rule 6 (gates can go red — inverse arms driven by the real tool) | `selftest.sh`, table-driven, both directions, derived population, real-tool arms where stubs stand in [S:N10] |
| Rule 2 (a test that cannot go red is not a test) | `selftest.sh`'s inverse arms for the gates; for the pure cores, `just mutants` — a machine that writes the buggy implementation 410 ways and reports the lines nothing noticed [S:E7]; elsewhere the seeded-defect campaign each sub-milestone records in its commit |
| Rule 3 (CI executes what it claims) | `counted-selections.sh`, `--no-tests=fail`, `ignored-suites-have-recipes.sh`, skip accounting on vivid/oracle rungs |
| A2/A3 (device authority, represent unknown) | battery control-model arms + PF corpus fixtures + the `query_controls` lint ban |
| A5 (one home) | `atomic-write-home.sh`, `dependency-walls.sh`, T5 method walk, CLI parity gate |
| A7 (leave as found) | snapshot-restore battery arm + R3 twins + crash-recovery case |
| A10 (requested ≠ applied) | battery write arms; sample-record schema fixtures |
| A12/§5 (privacy) | `no-frame-bytes-in-repo.sh`, `no-external-fetch-in-web.sh`, token + UDS gates |
| B7 (the browser half asserted in the browser) | the R1-web Playwright rung + its skip accounting |
| B9 (licenses) | `license-allowlist.sh` + `feature-posture.sh`, both self-tested |
| B10 (unsafe/ioctl boundary) [V] | `unsafe-scope.sh` + crate-root lint policy + cast lints on the backend crate + the Miri job |
| E5 (resemblance) | profile capture/replay inverse; fake-vs-probe-record assertions |
| E6 (byte fidelity) | verbatim-JPEG hash tests; muxer self-parse |
